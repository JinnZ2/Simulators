# CLAIM_TABLE — routing-data-layer

`MARKER.md` is a delivered marker: an envelope spec for the data layer
heavy-vehicle routing requires, with its own claim table **RDL-1..RDL-17**
(each carrying a REFUTED-IF). RDL-1..RDL-7 are the envelope / rate / upstream
claims addressed here; RDL-8..RDL-17 (Sections 5B–5F, the nominal-case cycle
accounting) are built out by the sibling `cycle-ledger/`. These `RDL_0NN`
claims are a different set — properties of the **instruments** built for the
marker (the envelope structure, the rate form, the F6 upstream
discriminator), each checked on **constructed** data with a known answer. No DOT feed, dock-geometry dataset,
or routing output is read (all egress-blocked), and every failure-class
instance in the marker is carried, not verified. Nothing here is a result.

## REFUTATION_PROTOCOL

A refuted claim is updated forward with a new id; the old id keeps its text
and gains an UPDATE paragraph. `MARKER.md` is delivered verbatim and is not
edited. `sustained_excess` is registered in `tools/known_answer.py` and its
cases are not loosened; the rate and upstream verdicts are checked in every
direction so no verdict is constant. Nothing is promoted from carried to
verified without a fetched source.

| id | claim | status | evidence | falsifier |
|---|---|---|---|---|
| RDL_001 | **The envelope is R1–R10, each carrying a record state, and R8/R9 are absent by construction.** `envelope.py` encodes the ten required contents with `record_state` in {INCOMPLETE, NEVER_CREATED}; `validate_required` refuses an unknown state. R8 (per-door dock geometry) and R9 (per-field update latency) are NEVER_CREATED — no originating record anywhere, so closing them means paying to CREATE, not to fund an existing chain. This is the `uninstrumented` / `generation-capacity` distinction (an absence built into the instrument, "not answerable at that instrument"), not a gap. | SUPPORTED | `never_created() == ("R8","R9")`; R1–R7,R10 INCOMPLETE; `BadRecordState` on an unknown state | an R8/R9 originating dataset located (which would also refute the marker's RDL-3) |
| RDL_002 | **The claim table is encoded with every claim carrying a refutation condition, and a claim without one is refused.** The seven RDL-1..RDL-7 claims are `Claim` objects each with a `refuted_if`; `validate_claim` raises `IncompleteClaim` on a claim with no refutation — a claim with no falsifier is a position, not a claim (the `falsifier-audit` discipline). The seven failure classes F1–F7 are carried with their named instances. | SUPPORTED | seven falsifiable claims; `IncompleteClaim` on a refuter-less claim; F1–F7 with F6 the load-bearing one | a claim in the table with no refutation condition passing validation |
| RDL_003 | **Section 5's rate form is the load-bearing runnable instrument: where dE/dt > dM/dt sustained, the null is STRUCTURAL, not a maturity gap.** `sustained_excess` (registered in `tools/known_answer.py`: all→1, none→0, half→0.5) drives `rate_verdict`, which returns STRUCTURAL (a sustained excess — "different answer"), MATURITY_GAP (the refresh keeps up — "not yet"), or UNDETERMINED (mixed). A single crossing is not structural; the sustained fraction is what the verdict turns on. This distinguishes the two claims the marker says are distinguishable, and is the repo's recurring rate-mismatch shape (`rigidification-sensor`, `closure-cost`, `revision-mechanism`) on a data layer. The marker's cheapest test (measure both rates for one county over one season) is named and **not run here** (egress-blocked). | SUPPORTED (instrument) / carried (the test) | the three known-answer cases PASS; STRUCTURAL / MATURITY_GAP / UNDETERMINED on constructed series | a rate form that returns STRUCTURAL when dM keeps up, or MATURITY_GAP when dE outruns it sustained; a real county measurement that closes the gap |
| RDL_004 | **F6 — two independent systems both wrong — is diagnostic of an upstream defect, not vendor maintenance (RDL-2).** `upstream_verdict` returns UPSTREAM_INCOMPLETE (both wrong, opposite error directions — the F6 signature: a source gap each vendor filled differently), SHARED_BIAS (both wrong, same direction — a common upstream systematic), VENDOR_DEFECT (one right, one wrong), or BOTH_CORRECT; `single_vendor_fix_closes` is True only for VENDOR_DEFECT. This is the `effective-redundancy-audit` shared-node shape: two channels marketed as independent, both failing on one input, point to a shared upstream node. | SUPPORTED | the four verdicts on constructed cases; `single_vendor_fix_closes` False for both upstream patterns, True for a lone vendor defect | a single-vendor fix that closes the F6 both-wrong pattern with no field survey (which would refute the marker's RDL-2) |
| RDL_005 | **RDL-5: the cost is STANDING, not capital — a one-time survey does not hold across one cycle.** `survey_decay` accumulates staleness at dE/dt; a one-time survey (never refreshed) falls below the accuracy floor within a 12-month season (final 0.40, held False), while a standing (periodically refreshed) survey holds (0.85, held True). A one-time survey therefore cannot be the answer; the cost recurs and grows with network size. | SUPPORTED (demonstration) | `survey_decay` one-time held False / final < floor; standing held True | a one-time survey that holds accuracy across a full freeze-thaw and construction cycle (which would refute the marker's RDL-5) |
| RDL_006 | **The two structural absences (Section 4) are the `uninstrumented` / `generation-capacity` shape, carried and cross-referenced.** INCOMPLETE records (R1–R7) have a reporting chain fundable in principle; NEVER_CREATED records (R8, R9) have no originating record — "the question was not asked and answered unremarkably; it was not answerable at that instrument." This is the same distinction the sampling-absence markers make, and it is recorded as a record-state on the envelope rather than restated. | carried (cross-reference) | Section 4 of `MARKER.md`; `record_state == NEVER_CREATED` on R8/R9; the `uninstrumented` / `generation-capacity` cross-link | an R8/R9 record shown to have existed all along (never-created was actually incomplete) |
| RDL_007 | **Framing, scope, and provenance honored; nothing here is a result.** The document is a marker, not a critique of any vendor; there is no author or working-style section (OUT OF SCOPE honored). The failure classes and their named instances (Minneapolis/Milwaukee bridges, Black Dog/Cliff Road, I-794, the ~2–3 in dock offsets) are in-service observations carried, not verified; the larger instance population is held in a separate operator repo, not here. No DOT feed, dock dataset, or routing output is read (egress-blocked). | carried / UNVERIFIED | no author content in any file; every instance marked carried; no external data in the folder | any instance asserted as verified rather than carried; an author-profile section anywhere |
