CONSTRUCTED fixtures. Not a measurement of any real corpus.

Only `.txt` is read by `p3_comprehension.py`, so this note documents the
corpus without entering it.

* `a_units.txt`      written here; units, scripts and protocols as
                     standards nobody privately owns.
* `b_protocol.txt`   written here; the same argument entered from the
                     protocol side, sharing many terms with `a`.
* `c_ciphered.txt`   `b_protocol.txt` put through the module's own
                     substitution cipher at seed 7. Same structure, same
                     compressibility, disjoint vocabulary. It is the
                     document that shares FORM with the other two and
                     shares no TERMS, so the pair readings against it
                     are expected to land on `SHARED_FORM_ONLY`.
