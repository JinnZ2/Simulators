# CLAIM_TABLE — quiet-failure

Ids are `QFA_`, permanent, never renumbered. Every number is printed by
`run_all.py` or `selftest.py` and pinned by `selftest.py`; the renders are
in `samples/`. Nothing here is a statement about any event, organisation,
regulator or public record: the instruments were built to WO-2 and run on
the order's own text and on constructed inputs.

| id | claim | status | evidence | falsifier |
|---|---|---|---|---|
| QFA_001 | The folder constraints hold, read from the files: every import is stdlib or this folder, every file parses under 3.9, every library module is under 300 lines and refuses `--selftest` with exit 2, no file names a network module, nothing imports across the folder boundary, `run_all.py` prints the contamination declaration before the first part, and no authored file carries a company or site name the order's evidence section carries. | SUPPORTED | `selftest.py` folder section | a non-stdlib import, a 3.9 parse failure, a `--selftest` exiting 0, a cross-folder import, a render with a number above the declaration, a company or site name in an authored file |
| QFA_002 | The four parts of the decomposition, the four cases, the three evidence findings, the step-3 null and the current read are parsed out of `WORK_ORDER.md` at call time and never retyped; a copy holding three parts, or lacking the Cases or Evidence section, raises. | SUPPORTED | `selftest.py` "retyped copy", "raises" checks | a part, case body or finding literal in any module |
| QFA_003 | The coder keeps three signal states apart and a missing field reads `UNSEARCHED`, never `ABSENT`: `ABSENT` on `signal_present` classifies `GENUINELY_UNSIGNALLED` while `UNSEARCHED` on the same field is `NOT_EVALUABLE` naming it, and all six classes are reachable. A holder count is never inferred and a lead time with no unit is `UNDECLARED`. | SUPPORTED | `selftest.py` decomposition section | a missing signal field reading as ABSENT; a class no record reaches; a bare number accepted as a lead time |
| QFA_004 | `base_rate()` refuses on every corpus, naming the frame and what lifts it, and divides nothing (AST-asserted): an accident-investigation corpus is selected on the outcome and carries no denominator over systems, which is the order's own "No base rate is claimed". `separation()` runs within the corpus instead and is `NOT_EVALUABLE` when the signal axis has one level, so a corpus with no genuinely unsignalled case cannot return a verdict. | SUPPORTED | `selftest.py` "base_rate REFUSES", "one level" | a rate emitted by any path; a separation verdict on a one-level corpus |
| QFA_005 | Coded from the order's own sentence, the anchor case classifies `REPORTED_NOWHERE` — the signal existed (*Fracture initiated inside a joint*) and reached no channel (*not inspectable without disassembly*) — so part 1 of the decomposition does not hold on the case the order opens with; it is the contrast class step 1 wants, not an instance of the structure. This is a reading of the order's text with the spans shown, not a statement about the event. | SUPPORTED | run_all decomposition block; `selftest.py` "anchor case codes REPORTED_NOWHERE" | a sentence in the order's bullet stating the fracture was reported through a channel |
| QFA_006 | The one case that instances the structure (`SIGNALLED_UNJOINED`) rests, for `join_assigned`, on a reading of *denied through proper channels* and *every step in procedure*: a request was decided by someone, and whether that someone held the join is the field the order's step 2 exists to code blind. Recorded as the coding most in need of a blind coder, not adjudicated. | SUPPORTED | README step 1; the coding's `basis` span | a blind coder reading the same sentence as `ASSIGNED` moves the case to `SIGNALLED_JOINED`, a counter-case to part 3 |
| QFA_007 | On the order's own four cases `separation()` is `NOT_EVALUABLE` (one signal level) and two of four are `NOT_EVALUABLE` on the text; holder count and lead time are `UNDECLARED` on all four. The order's scope limit ("the four-part decomposition ... has not been tested against a case set selected independently of it") is therefore also true of the cases it was fitted on: they do not carry the fields step 1 codes. | SUPPORTED | `selftest.py` "separation on the order's own cases", "UNDECLARED on all four" | a case bullet stating a holder count or a first-signal-to-event time |
| QFA_008 | The evidence section's flat reading has two clauses and the bullets state one: *the reasoning was not recorded* is stated by 2 of 3 (the bullets reporting *no documented information* / *no formal ... assessment*), and *no cost comparison was made* is stated by 0 of 3 — absence of record is what the bullets carry, absence of the act is the order's reading of the absent record. The one number is extracted as written with a span. | SUPPORTED | run_all evidence block; `selftest.py` evidence section | a bullet stating that no comparison was made |
| QFA_009 | The order's step-3 null ("Not found in two searches") is `UNBOUNDED`, lacking corpus, terms, date and hit count; it is carried and enters no count. A null is reportable as the order asks only when bounded, and zero hits in a stated corpus is `BOUNDED` while an absent hit count is `UNBOUNDED` — zero is not absent. | SUPPORTED | run_all ownability block; `selftest.py` "order's null is UNBOUNDED", "zero is not absent" | a corpus, terms and date stated in the order's sentence |
| QFA_010 | The open question is derived from role records and never picked: one `EXISTENCE_PROOF` (a named role holding the join with authority) makes it `NEVER_ASSIGNED`; no evaluable record, or only roles that hold something other than the join or hold it without authority, leaves it `UNDETERMINED`; `UNOWNABLE` is returned by no path, since absence of a proof is not a proof of absence. The regulator and role NAME reach no branch. | SUPPORTED | `selftest.py` ownability section | a path returning `UNOWNABLE`; a verdict moving on a rename |
| QFA_011 | Step 2's gate refuses every coding in this folder by the one field the step turns on (`saw_decomposition: True`) and refuses an undeclared one rather than admitting by default; agreement between admitted codings is per field with `UNSEARCHED_ON_ONE_SIDE` kept apart from `DISAGREE`, and no composite is emitted. | SUPPORTED | run_all blind_coding block; `selftest.py` blind section | a session coding admitted; a composite key in the readout |
| QFA_012 | The suite's own number scan fired on the literal it carried, the third check in three folders this session to fire on its own text (`CHP_016`, `ASC_017`); repaired by reading the number through its span. Recorded because a check that fires on its own text is the `UNI_010` loop arriving in a suite written after that finding was recorded, again. | SUPPORTED | README "Found by running"; `selftest.py` comment at the number check | the literal reappearing in `selftest.py` |
| QFA_013 | The order's sentence placing agentic AI infrastructure "at the pre-Tacoma stage: designed to calculated load, no accumulated margin" is carried as DERIVED and scored by nothing here. The author is an instance of the class named, and the interest direction is stated: accepting the sentence is the humble reading, so it is left unresolved rather than accepted on that ground. | SUPPORTED | run_all declaration, position line | a function in this folder returning a margin, a safety factor or a verdict on any AI system |
| QFA_014 | UNVERIFIED: no accident corpus was read (four hosts answered 403 to CONNECT, measured and timestamped, `github.com` the control), no finding in the evidence section was re-fetched, no regulator record exists in the folder, and no blind coder has run. Nothing here bears on whether any real failure was signalled, on whether the decomposition separates real cases, or on whether any regulator assigns the join; every instrument is shown reachable in both directions on constructed inputs only. | UNVERIFIED | `decomposition.EGRESS`; every fixture named CONSTRUCTED | a coded real corpus supplied to `--corpus` with both signal levels present; a bounded null; a real role record |
