# CLAIM_TABLE — chain-position

Ids are `CHP_`, permanent, never renumbered. Every number is printed by
`run_all.py` or `selftest.py` and pinned by `selftest.py`; the renders
are in `samples/`. Nothing here is a statement about any standard read
from its source, any vendor, any deployed container, or any model: the
instruments were built to WO-1 and run on what this folder holds.

| id | claim | status | evidence | falsifier |
|---|---|---|---|---|
| CHP_001 | The folder constraints hold, read from the files: every import is stdlib or this folder, every file parses under 3.9, every library module is under 300 lines and refuses `--selftest` with exit 2, no file names a network module, nothing imports across the folder boundary, and `run_all.py` prints the contamination declaration before the first part. | SUPPORTED | `selftest.py` folder section | a non-stdlib import, a 3.9 parse failure, a `--selftest` exiting 0 running nothing, a cross-folder import, a render with a number above the declaration |
| CHP_002 | Step 1 is `NOT_RUN_ON_STANDARD`: both OWASP hosts answered 403 to CONNECT (measured, timestamped in `clause_audit.EGRESS`, `github.com` the connecting control) and the clauses coded are the order's own five-bullet summary. The count — inside-agent 0, gateway 5 — is therefore a property of what the summary carried, and the order's expected null is not established from here. | SUPPORTED | `samples/run_all.sample.txt` step 1 block; `selftest.py` clause_audit section | the standard's clauses handed to `--clauses` by someone who read them, and a clause coded `INSIDE_AGENT` |
| CHP_003 | The clause coder reads a declared `info_location` and nothing else: the clause text reaches no check (every text blanked, every count unchanged), so a coding is a reading a human made and not a word list deciding where information sits (`T1-1`); a clause with no location declared is `UNDECLARED`, never `GATEWAY`. | SUPPORTED | `selftest.py` clause_audit section; text-swap invariance | a check inside `clause_audit.py` that branches on clause text |
| CHP_004 | Step 2 is a specification, as the order asks, and it is `[CHOICE 1]`: nine fields, each with a stated reason, `scorer_provenance` recursive to a `{"root": ...}` terminator under a depth cap `[CHOICE 2]`. A record is `AUDITABLE` only when every field is `PRESENT` and the chain terminates; a field declared `UNDECLARED` lands `DECLARED_UNAUDITABLE`, kept apart from `UNAUDITABLE` (absent). | SUPPORTED | `selftest.py` trust_provenance section | a record with an absent field reading `AUDITABLE`; an `UNDECLARED` field reading as absent |
| CHP_005 | On the constructed record set there is no common object: the intersection of `PRESENT` fields is `{score, method}` and seven of nine required fields are missing from at least one record — the order's "no common object to audit" as an intersection, measured on constructed records only, with the `COMMON_OBJECT` branch shown reachable on three full records. | SUPPORTED | run_all step 2 block; `selftest.py` "constructed set -> NO_COMMON_OBJECT" | three real assignment records from three sources whose intersection covers the spec |
| CHP_006 | `scores_payload_only` is a property, not a fault: `True` when the scorer's inputs are exactly the payload digest, `False` when a prior score was also read, `None` when unreadable — the return-path-filtration shape the order describes, recorded and never scored. | SUPPORTED | `selftest.py` three-state checks | a record with a prior score among its inputs reading `True`, or an unreadable record reading `False` |
| CHP_007 | Found by running: the first cycle detector serialized the provenance node BEFORE checking for a cycle, so a genuinely cyclic chain raised inside the serializer and the detector written for cycles never ran. Repaired with an object-identity check first, a serializer guard second, and the content-repeat check third; a cyclic chain now returns `CHAIN_CYCLE` with the reason, and a chain past the cap without a root returns `CHAIN_UNTERMINATED`. | SUPPORTED | `selftest.py` "repeated provenance node -> CHAIN_CYCLE", "chain past depth cap -> CHAIN_UNTERMINATED" | a cyclic chain that raises, or that reads `AUDITABLE` |
| CHP_008 | Horn B, as arithmetic on the container's interior: all four chain-position quantities are `UNOBTAINABLE_FROM_INSIDE` on every arm, the evidence each would need sits `EXTERIOR` in every row, and position is never `OBSERVED` — the ceiling from inside is `ASSERTED_BY_INPUT`, reached only on the arm where the harness wrote the position into the environment. | SUPPORTED | run_all step 3 block; `selftest.py` "never OBSERVED" and "no code path emitting OBSERVED" | an interior feature that yields a position without an input asserting it |
| CHP_009 | The interior delta between STANDALONE and CHAIN_SILENT is empty by construction, and the delta to CHAIN_DECLARED is exactly the features the harness wrote (`declared`, `env_names`): the only feature that moves with position is one an outside party supplied, and the container cannot tell a written claim from a true one. The order's "reasoning about something for which it has no evidence" is the empty delta. | SUPPORTED | run_all step 3 "interior delta" lines; `selftest.py` delta checks | a non-empty STANDALONE/CHAIN_SILENT delta on the constructed harness |
| CHP_010 | `--live` runs the question on this process and returns `UNDETERMINED_FROM_INSIDE` with zero position-shaped env names; the record carries environment variable NAMES only and no value string. This is an in-class self-report by the object under measure and is not evidence about any deployed container. | SUPPORTED | `selftest.py` live-interior checks (no value leaks) | an env value appearing in the live record; a `--live` reading offered as evidence about a deployment |
| CHP_011 | The order's compounding is a floor: four of seven factors are assessed, the union of their failures is a band `[max p, min(1, Σp)]` = `[0.04, 0.10]` with the independence point 0.0965 inside it, and the three unassessed factors enter no arithmetic (`p = None` is refused by the product and the sum, never read as 0). | SUPPORTED | run_all load_class block; `selftest.py` union checks, `union_band([]) is None` | an unassessed factor entering the union; a band the independence point falls outside |
| CHP_012 | The order's RULE is implemented as a refusal to propagate rather than an inversion: an unassessed factor with no declared structural handling makes the verdict `NOT_FLIGHT_RATED` regardless of the assessed number, and the refinement (credible extreme plus margin) yields a bound only when both terms are supplied, `UNBOUNDED` otherwise. Factor of safety designed to the anticipated threat exactly is 1.0. | SUPPORTED | `selftest.py` load_class section | a `NOT_FLIGHT_RATED` verdict flipping on the assessed number alone; a bound computed from one term |
| CHP_013 | Found by running: the suite's expected value for the independent union was a transposition (0.096529 written for 0.096550) and the check refused its own record; corrected in the check with the first value kept in a comment. | SUPPORTED | `selftest.py` comment above the union check | — (a record of the suite's own miscopy) |
| CHP_014 | Step 4 has a precondition the order does not state: adjudicating a disagreement across model families requires a checker independent of both outputs. Without one, two families disagreeing is `DISAGREE_UNADJUDICABLE` before any model runs; with a fixed spec the same pair is `DISAGREE_ADJUDICATED` or `ALL_FAIL`; and `n_eff` counts families rather than copies, so two copies of one family agreeing is `n_eff 1`. | SUPPORTED | run_all step 4 block; `selftest.py` dissimilar section | a disagreement adjudicated with no checker; two copies of one family reading `n_eff 2` |
| CHP_015 | UNVERIFIED: no model was run, so the order's counter-argument (diversity yields noise rather than fault detection) is untouched in both directions; what is established is that the run requires the checker, and that with one the question becomes whether either output meets the spec. | UNVERIFIED | the step 4 render's first line | two model families run on one task under a fixed, verifiable spec |
| CHP_016 | Found by running: the check that no module inserts a parent path fired on itself, since the scanned token was written literally into the file that scans for it; the token is now composed at run time and not carried (`UNI_010` / `T1-1` inside the suite). | SUPPORTED | `selftest.py` `PARENT` construction | the literal token reappearing in any module |
| CHP_017 | The order's scope limits are honored structurally: no vendor is named anywhere in the folder, no mitigation is specified, Horn A and Horn B are both left unselected, and "scorer provenance goes unexamined" stays UNVERIFIED — step 2 says what an examination would need and examines no real assignment. | SUPPORTED | README "Scope limits honored"; `selftest.py` declaration check | a vendor name, a mitigation, or a horn selected in any file |
| CHP_018 | UNVERIFIED and it covers the folder: neither standard was read, no real assignment record was audited, no deployed container was asked, no model was called. Every finding is a property of the order's own text, of constructed inputs, or of this process reading itself. | UNVERIFIED | every render's declaration | a second party with the standard in hand running step 1, or with agent-framework access running steps 3 and 4 |
# CLAIM_TABLE -- chain-position

Permanent ids `CPD_NNN`. These are claims about the three instruments and
their behaviour on CONSTRUCTED data and the order's carried clause set,
distinct from the order's own OBSERVED / DERIVED / PROPOSED tags and its
runnable steps 1..4. Ids are permanent; a refuted claim is updated in place,
never renumbered. Status: SUPPORTED / REFUTED / UNVERIFIED.

`WORK_ORDER.md` (WO-1) is landed **verbatim** and edited by nothing here. No
vendor internals are used or sought anywhere in the folder; the OWASP clauses
are the public standard as the order carried it (fetched and verified by the
operator, NOT re-fetched here -- egress refuses the standard), and the
containers, tasks and records are constructed.

| id | status | claim |
|---|---|---|
| CPD_001 | SUPPORTED | **The clause audit returns the null result the order predicts, and the null is a measurement, not a floor.** Each OWASP clause the order lists is classified by where the observability sits; all five sit at the GATEWAY, so `inside_container` is 0 and the verdict is `NULL_RESULT_no_clause_places_position_inside` -- lineage exists and the container still cannot see it. A constructed clause that DOES place position inside the executing agent raises the count to 1 and flips the verdict, so the zero is not constant. |
| CPD_002 | SUPPORTED | **The fork is reported, neither horn selected.** `horn_fork` returns Horn A's failure (`RELOCATES` -- the outside thing is itself a container with its own local correctness) and Horn B's failure (`EVIDENCE_ABSENT` -- reasoning about something for which there is no evidence). An unknown source selects no horn (`UNDEFINED`, horn `None`) rather than inventing one; selecting a horn is downstream work, as the order says. |
| CPD_003 | SUPPORTED | **A locally-correct container cannot answer its chain position, and the absence is the container's, not the check's.** `evidence_check` over a container holding its task, its inputs and its own state returns `EVIDENCE_ABSENT` and names what would be needed (an observation of a step outside the boundary). Handed a whole-chain manifest, the same check returns `EVIDENCE_PRESENT` -- so the absence is a property of the locally-correct container by construction, which is Horn B's failure mode made decidable. |
| CPD_004 | SUPPORTED | **The reachable-controller compounding is made a number and the order's RULE is built in as a refusal.** `stability_product` composes the assumed stabilities as `product of (1 - p)`; the order's illustrative discontinuities (1/2/3/4 percent) give 0.90345024, so from four factors alone the reachable controller is absent about 9.65 percent of the time -- a crewed mission does not fly on that. An unassessed factor (`None`) returns `UNPROPAGATABLE`: an unquantifiable probability cannot be propagated through the calculation and is handled structurally. `stability_product` is registered in `tools/known_answer.py`. |
| CPD_005 | SUPPORTED | **A zero, an absence and a malformed factor are three states.** A factor of zero multiplies through (stability 1.0); an unassessed factor (`None`) makes the product `None` with state `UNPROPAGATABLE`; a factor outside [0,1] makes it `None` with state `MALFORMED`. The registered case set pins the unassessed `None` apart from the zero, so the RULE is in the metric, not only in the prose. |
| CPD_006 | SUPPORTED | **Dissimilar redundancy detects a fault only against a verifiable spec; the discriminator is spec-verifiability, not model diversity.** `redundancy_adjudicability` returns `FAULT_DETECTED` for disagreement against a verifiable spec, `NOISE_unadjudicable` for disagreement with no spec, and `NO_SIGNAL_agreement` for agreement of correlated copies. That settles the MECHANISM of the order's counter-argument (diversity may yield noise); the empirical question -- whether real model families disagree adjudicably -- needs model access and is NOT_RUN. |
| CPD_007 | SUPPORTED | **The engineering-gap register and the counter-arguments are carried, not decided.** Three gaps against load-bearing practice (no factor of safety, no inspectability under load, no ductile failure mode -- the chain lets go silently because every container stayed compliant) and two counter-arguments (diversity-may-be-noise, ductile-mode-is-a-signal) are carried from the order, both counter-arguments marked `UNRESOLVED`. Nothing here resolves them. |
| CPD_008 | SUPPORTED | **The second-order gap is a schema, and its load-bearing distinction is UNEXAMINED vs NOT_AUDITABLE.** `assignment_auditability` reads a payload-only record as `UNEXAMINED` (the record does not declare scorer provenance, so whether it is carried cannot be stated -- the order's "nobody can state whether scorer provenance is carried at all"), a declared-empty provenance as `NOT_AUDITABLE` (a known negative), and a complete record as `AUDITABLE`. Collapsing the first two would read a silence as a finding; keeping them apart is the absent-vs-known-negative repair. `PROVENANCE_SPEC` requires the scorer's OWN provenance, so the recursion (the score is a chained artifact) is in the spec. |
| CPD_009 | UNVERIFIED | **The order's scorer-provenance claim stays UNVERIFIED, by the order's own scope.** `common_object_exists` returns False: implementation varies by vendor and no vendor internals are used or sought, so there is no common object to audit, and "scorer provenance goes unexamined" is plausible, not established -- exactly the order's own tag on it. The specification of what an auditable assignment must carry is complete here; whether any deployed system carries it is not checkable from this folder. |
| CPD_010 | UNVERIFIED | **What is NOT_RUN, and the siblings.** Step 1's clauses are carried from the order, not re-fetched (the standard's host is egress-blocked), so the audit is over the order's clause set, not a live re-read; step 4's empirical test needs multiple model families and adjudication and is NOT_RUN; no vendor internals are touched. The order's sharpest open problem -- an engineering method by which the TOOL ITSELF knows when it is crossing the method, when it IS the method, and when it has been handed a binary gate to inspect rather than execute -- is stated, not solved, and a binary gate cannot carry it. WO-2..WO-5 are named-and-absent siblings, carried as references. No mitigation is specified, deliberately: the join is not published before the mitigation side exists. |

## What this folder does not establish

- That any deployed agent framework places chain-position information inside
  or outside the executing agent. The audit is over the order's carried clause
  set; a live clause-by-clause re-read of the standard is egress-blocked.
- That dissimilar redundancy across real model families yields adjudicable
  disagreement. The mechanism (spec-verifiability is the discriminator) is
  shown on constructed data; the empirical question is NOT_RUN.
- That any vendor's scorer provenance is or is not carried. No vendor
  internals are used or sought; the claim stays UNVERIFIED, as the order tags
  it.
- Any mitigation. The order withholds the mitigation side deliberately, and
  so does this folder.
