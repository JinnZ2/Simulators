FABLE WORK ORDER 2 — R2 kill-closure (post-2fdbcd4). CC0.

SCOPE / STANDING CONSTRAINT (read before tasks):
Same-builder pair. Every return is a same-node computation, NOT
P3-verified. DBK_014 stands — dissimilarity established on no axis,
so no task is load-bearing on Fable being a different builder. Each
task self-verifies by parse / arithmetic / set-intersection / measured
code behavior / adversarial construction. REFUSED-BY-§3 is valid.
Do not label output "verified," "confirmed," or "P3-passed." State
counts, intersections, flip points, refusals. Forward-dated: produces
findings with today's date, re-rates no prior artifact.

T1 — HARNESS THRESHOLD MAP  (closes §8[4], QUALIFIED)
  sweep full integer grid (dissent,total) the harness accepts; record
  verdict per cell; find EVERY verdict-flip boundary, not just (4,3).
  RETURN the flip map. verdict changes inside the constant's unpinned
  range → not a test until pinned; give the flip values.
  NOT: don't pick the constant. enumerate.

T2 — CUSTODY-CHAIN DISJOINTNESS  (tests the provenance P7-pass)
  each load case's source set = a channel. intersect pairwise. colophon
  claims sources "disjoint by construction" → convergence PASSES P7.
  test arithmetically: any body/dataset/report under two load cases?
  RETURN the intersections. non-empty pair = disjointness claim partly
  false, P7-pass partly unearned, name the shared node. all-empty =
  factual floor (still not a P3 pass). NOT: don't rule on self-flattery.

T3 — COVERAGE RE-AUDIT (edited outline) + CLAUSE CONSISTENCY
  (a) re-run null-injection coverage parse on the outline as it stands
  (D reopened, kills folded): every load has a named carrier or honest
  "uncarried" — no "attack-only" as carried. (b) check effective-date
  clause against P0.3 append-only + colophon; verify nothing re-rates a
  prior entry. RETURN coverage table + any contradiction. D must read
  uncarried/P1-bounded.

T4 — P0.3 RETENTION ACCOUNTING ENUMERATION  (feeds §8[2])
  from the three states (copies 3 / inherited 3 / pricing 2): enumerate
  every internally-consistent retention accounting, compute N_eff under
  each. RETURN accounting→N_eff table, name the intersection that drops
  each below 3. NOT: don't select — that P0.2 declaration is yours.

DELIBERATELY NOT ASKED: no different-builder verifier (DBK_014 gap out
of scope, uncloseable here); no redefine of distinct-upstreams/AX bands
(dead pending your proposal); no retroactive edit to 2fdbcd4.
