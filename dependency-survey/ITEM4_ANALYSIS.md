# ITEM 4 — the two small decisive tests, decided and recorded

Handoff item 4 asks for two decisions against `RESULT_repair_adjacency.md`
and `RESULT_taxonomy_crossmodel.md`. Both are decided here **only to the
extent the evidence in hand allows**, and the limit is stated where a full
decision needs data this repository does not hold — the 19-cell corpus is
external model output (Kimi / Perplexity / DeepSeek runs), and the RESULT
documents themselves decline to close either question, one calling the next
step "a second independent name-search, not a decision." Nothing external is
fabricated to force a closure.

---

## NLS-3 in K3 — the separation is DECIDED; the kind is not

```
question   both external systems removed scale-from-correlation from the
           proxy cluster independently. Kimi's assignment is a correction
           candidate. Decide and record it.
```

**Decided: the separation is adopted.** NLS-3 (scale-from-correlation,
transcribed as component `C7` = T08/T14 in `repair_adjacency.py`) does NOT
belong grouped with the speedup core (`C2` = T02/T09/T10/T12/T17). Two
independent sorts — Perplexity by subject matter, DeepSeek by repair —
removed it from that cluster, and my transcription already places it in a
separate component. Two-system replication is this folder's own standard for
a membership result (`DS_013`: *membership replicated*), and it is met here.
So Kimi's grouping of NLS-3 *with* the speedup core is a confirmed error, not
merely a candidate.

**Not decided: which kind NLS-3 belongs in.** Kimi assigns `C7` to the `K3`
kind (no-conversion-in-principle); `repair_adjacency.py` carries `C7 → K3`
flagged `CORRECTION_CANDIDATE`. Whether the correct kind is `K3`, `K1`
(scale supplied by an explicit parameter is a conversion that exists), or its
own kind is **UNVERIFIED** — deciding it requires reading the NLS-3 record
(what quantity scale-from-correlation actually is), which is external and not
held. The two-system evidence establishes *separation from the cluster*, not
*the target kind*. Recording the separation as decided and the kind as open
is the honest split.

---

## F-TETRA-SCOPE (T13) — the reduction is PLAUSIBLE with an argument; NOT closed

```
question   can it be restated as "two referents present, the governing one
           not declared"? YES -> the gap closes, the record joins C1.
           NO -> name what it has that the two-reference case does not.
           Do not close on DeepSeek's placement alone.
```

**The restatement fits, and here is the argument** (more than DeepSeek's
placement alone, which the handoff and the RESULT both forbid relying on).
From `RESULT_repair_adjacency.md` §4, T13's shape is: *two frames both yield
SHAPE.TETRA; refutation is defined by component count, which differs between
them* (≠4 vs ≠3). Mapped onto the two-reference-temperature case TMP-2 (*two
reference temperatures co-present in one module; the wrong one is passed into
the correction*):

```
TMP-2                          T13
two referents ...............  the two frames
co-present .................... both yield SHAPE.TETRA
governing one not declared ... which refutation criterion (≠4 vs ≠3) governs
                               is not declared
shared repair ................ identify which of two co-present referents
                               governs, and pass that one
```

The reduction holds on the shape as given: the two frames are the two
referents, and which one's refutation criterion governs is undeclared.

**But it is NOT closed.** Both the handoff and `RESULT_repair_adjacency.md`
§4 say the next step is *a second independent name-search, not a decision*,
and that search — whether "reference disambiguation with two co-present
referents" is a named object in several fields — is a literature query the
egress allowlist blocks (403 CONNECT), and I will not fabricate field names
from memory. So the gap **stands, sharper**: the specific reduction above is
now on the table to be confirmed or refuted by the name-search, rather than
an unstructured "NO NAME FOUND." Closing it needs either the T13 record or a
reachable second independent check; this session has neither.

---

## Status

```
NLS-3 separation from the speedup cluster   DECIDED (two-system replication)
NLS-3 target kind                           UNVERIFIED (needs the record)
F-TETRA-SCOPE reduction to TMP-2's shape     PLAUSIBLE, argument recorded
F-TETRA-SCOPE closure                        NOT CLOSED (name-search egress-blocked)
```

Recorded as `DS_018` (NLS-3) and `DS_019` (F-TETRA-SCOPE); `DS_015` and
`DS_016` gain UPDATE pointers to this file.
