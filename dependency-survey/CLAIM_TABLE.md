# CLAIM_TABLE — dependency-survey

Every number is computed by `selftest_ds.py` against the seeded
`CELLS.md`; the render is in `samples/report.sample.txt`. This is a
survey instrument, so the claims are properties of the instrument and of
the seed, not conclusions about any substrate; the order's own non-goal
forbids concluding shared structure and the folder does not.

| id | claim | status | evidence | falsifier |
|---|---|---|---|---|
| DS_001 | The instrument is the E7 cross-substrate term table of `cooperative-substrate/` made runnable, with the units requirement enforced: a cell coded MEASURED or SCOPE-DIFFERENT whose MEASURED_AS states no units is INVALID and cannot seed a gap as the measured side. `CSP_019` recorded the E7 table as a schema that refuses a filled cell without a basis; here the basis is units, checked by `has_units`. The 25-cell grid defaults UNKNOWN and the cell store is `CELLS.md`, parsed so a cell recodes without touching code. | SUPPORTED | `grid()`, `validate()`, `has_units()`; 25 cells, 22 UNKNOWN | a MEASURED cell with no units that seeds a gap |
| DS_002 | **The seed fails the survey's admissibility bar for SCOPE-DIFFERENT.** `T3 x S5` is coded SCOPE-DIFFERENT with a scope note and no admissibility fields; the instrument caught it on first run. **UPDATE (ADDENDUM_01):** the original rule required MEASURED_AS-with-units for MEASURED *or* SCOPE-DIFFERENT, and the seed failed that. The addendum **rescopes** rather than patches: SCOPE-DIFFERENT now requires a SCOPE_TRANSFORM (`reference` / `maps_to` / `breaks_at`), not units, since a "measured, but the frame differs" status reports frame information which is not in the quantity's units. The seed **still fails** — a prose note, no transform — and now downgrades to UNKNOWN (not MISSING), counted on its own line. The claim holds under both rules; only the reason it fails and its downgrade target moved. Detected by the validator, left in `CELLS.md` as delivered, not repaired. | SUPPORTED | `validate(T3xS5)`: valid False, reason names the absent transform fields; `scope_incomplete_cells()` returns it; `effective_status` "UNKNOWN (downgraded: incomplete transform)" | a reading under which SCOPE-DIFFERENT needs neither units nor a transform |
| DS_003 | Exactly one gap falls out of the seed: `T1` (cost asymmetry) validly MEASURED in `S1` (energy intake per handling time, J/s) and MISSING in `S2` (multiagent harnesses), emitted PROVISIONAL (the order marks `T1 x S2` a claim to check) and OPEN (no transfer stated), with the transfer question posed in `S2`'s own units. It is `CSP_019`'s "experiment sitting there" with the units carried on the measured side and the transfer question left as the research queue. NO-TRANSFER (the projected-frame result) and TRANSFER-STATED are both reachable by a coded flag on the target cell, so the gap list is not `CONSTANT_SILENT`. | SUPPORTED | `gaps()`: 1 gap, state OPEN, provisional True; the constructed no_transfer and transfer cases flip the state | a second validly-MEASURED / MISSING pair in the seed |
| DS_004 | UNKNOWN is kept apart from MISSING throughout: 22 of 25 cells are UNKNOWN, reported as a count and listed, and the report states they are not-yet-looked and NOT an absence of the term. The absent-vs-known-negative repair (`PB_004`, `GC_004`, `CC_002`, …) designed into the order and honoured — an unsurveyed cell must not read as MISSING, and MISSING (`T1 x S2`) and UNKNOWN (`T2 x S3`) are distinct statuses. | SUPPORTED | `counts()`, `unknown_block()` | an UNKNOWN cell that reads as MISSING anywhere in the output |
| DS_005 | The units check is lexical: `has_units` accepts a slash rate or a token from a fixed set `[CHOICE 1]` and rejects a unitless phrase, so it passes `J/s` and fails `cost asymmetry between the parties` — but a MEASURED_AS naming a quantity in words a reader would grant units to is rejected, and one carrying a stray `/` passes. Stated at the callsite as a heuristic, null-tested both directions; a false accept or reject is a coding signal, not a silent pass. **UPDATE (ADDENDUM_02):** the units bar now has a second lexical arm — `names_type` + `has_cut`. A MEASURED_AS naming a data type (`boolean`, `integer`, `unitless`, `dimensionless`, `float`) satisfies the bar only when a CUT is also named (a comparison operator, or a threshold/band/cut word — not required to be numeric, so `cut at non-finite` counts). `has_units` is unchanged; the type-only arm runs first, so a bare `dimensionless` now fails where it used to pass on the unit-word list. Both arms remain heuristics, null-tested against the addendum's own examples. | SUPPORTED | `has_units`, `names_type`, `has_cut`, `measured_units_ok` on the addendum's PASS/FAIL examples | a type-only MEASURED_AS the second arm mis-grades that a reader cannot re-check from the flagged output |
| DS_006 | `T5` — does the accounting stance preserve or destroy the measurement it depends on — is `extraction-blindness-sim`'s mechanism (an optimizer whose stance degrades the signal it reads) named as a survey term, and `T3 x S5`'s scope note is the same self-model-accurate-within-its-boundary shape as that folder's frame blindness. The survey carries the terms; whether they transfer quantitatively is what the gap list leaves open, not what it asserts. | SUPPORTED (as a cross-link) | the term text; `T5` coded nowhere yet | — |
| DS_007 | UNVERIFIED: nothing beyond the three seeds is coded, so the discriminator the instrument serves — real shared structure (transferable quantitative predictions) vs projected frame (analogies that stop at the vocabulary) — cannot be applied here, because it runs against transfer *results* and none exist. The survey is a store and a gap-deriver, not a conclusion, which is the order's own non-goal. Filling cells and running transfers is the operator's keyboard work. | UNVERIFIED | the 22 UNKNOWN cells; `gaps()` returns questions, not answers | coded cells with transfer results that resolve a gap on one side of the discriminator |
| DS_008 | `ADDENDUM_01` is a RESCOPE, not a narrow: the units rule inherited a two-status frame (measured / not measured) and was correct inside it; it keeps its truth value for MEASURED cells and loses generality across the third status. A complete SCOPE_TRANSFORM validates **without** units (checked: `reference`/`maps_to`/`breaks_at` present, no units, valid), and a missing field is inadmissible (not `CONSTANT_FIRES`); a SCOPE-DIFFERENT downgrade lands on UNKNOWN, distinct from a no-units MEASURED downgrade landing on MISSING, and the scope-incomplete count is reported apart from the never-coded UNKNOWN count (`report.py`, per §3). The taxonomy test (K1 frame / K2 boundary / K3 homonym) is **OPEN and is itself a survey output** — no new module is built, per §3, because it consumes the survey's own transforms and there is nothing to sort until SCOPE-DIFFERENT cells carry transforms; on the seed the transform set is empty. The kinds are not offered to the coder as a menu (the addendum's constraint), so nothing in the instrument assigns a kind. **UPDATE (`RESULT_taxonomy_crossmodel`):** the taxonomy question is no longer wholly open. A cross-model run — Kimi Run 2 (which produced the K-list) against a Perplexity blind sort of the same 19 cells with no K-list and no repo access — settles the *membership* half (`DS_013`) and leaves the *count* half open. `OPEN (taxonomy)` becomes `PARTLY SETTLED`: SEVERAL, membership replicated; the count is grain-dependent and unfixed. | SUPPORTED (rescope) / PARTLY SETTLED (taxonomy: membership SEVERAL, count OPEN) | `validate()` on complete/incomplete transforms; `effective_status`; `counts()` splitting `scope_incomplete` from `UNKNOWN`; `RESULT_taxonomy_crossmodel.md` §6 | a transform set that sorts into kinds (a result), or that does not (also a result); a build that assigns a kind during coding |
| DS_009 | `ADDENDUM_02` is a NARROW, not a rescope (its own frame note, and the contrast with `DS_008`): the units frame is intact — a measurable is still required — but the boundary was drawn too wide by leaving "units" ungraded, so a units field naming a data TYPE (`boolean`, `verdict`, `integer`, `unitless`) with no CUT passed. The instrument now rejects a type-only field: `names_type` ∧ ¬`has_cut` → inadmissible, downgraded to **MISSING** with a reason (`type-only units, no cut`) distinct from the no-units downgrade, and counted on its own `measured_type_only` line in `counts()` and `report.py`, visible as a zero when zero (the addendum's instruction, the same shape as the scope-incomplete line). A CUT is a comparison operator or a threshold/band/cut word and is **not** required to be numeric, so the addendum's own repair `float magnitude; cut at non-finite` passes while `boolean isfinite check per step` fails. The four FAIL and four PASS examples the addendum gives are null-tested both directions; `dimensionless` alone now fails where the bare unit-word list passed it, and `J/s` (no type word) is unaffected — `T1 x S1` stays validly MEASURED and `DS_003`'s gap survives. | SUPPORTED (narrow) | `names_type`, `has_cut`, `measured_units_ok`, `measured_type_only`, `counts()`, `report.py`'s type-only line; the addendum's 4 FAIL / 4 PASS examples | a type-only units field with no cut that the instrument admits as MEASURED; a dimensional-unit or scale+cut field it rejects |
| DS_010 | UNVERIFIED for the external corpus: the addendum's RE-CODING scope is the 96 of 537 MEASURED cells in the Kimi falsifier survey Run 2, which is not in this repository. The rule adjustment lands here (where `ADDENDUM_01` lives and the shared units bar is implemented) and the count is exposed, but the survey's own seed carries **zero** type-only cells, so `measured_type_only` is a visible zero and there is nothing here to re-code. Re-coding Run 1 and Run 2, and the expected legitimate downgrades, are the operator's keyboard work against a corpus this instrument does not hold; nothing is fabricated to stand in for it. The addendum's "not changed" list holds here too: `SCOPE_TRANSFORM`, MISSING, and UNKNOWN are untouched, and no verdict field is added. | UNVERIFIED (external corpus) | `counts()["measured_type_only"] == 0` on the seed; `report.py` emits the zero line; no Run-2 cells present | a type-only cell appearing in this repo's `CELLS.md` (re-code or downgrade it, do not delete) |
| DS_011 | The RESULT's headline — *the two taxonomies are strictly nested, zero cross-cutting* — is a property of the delivered §1 kimi-kind ← perplexity-group map, and that property is checkable **here** without the corpus: strict nesting means the map is a FUNCTION, every Perplexity group under exactly one Kimi kind. `taxonomy_replication.py` transcribes the map from §1 (the per-record memberships §1 does not print are **not** invented) and `strictly_nested()` verifies it — 11 groups, each under one kind, the straggler `G9` under none. It is a **transcription-consistency check, not a reproduction of the sort**: the 19-cell corpus is external model output (Kimi, Perplexity) not held in this repo, so what is verified is that the delivered table says what its headline claims, not that the underlying sort is correct. Null-tested: a constructed cross-cut (one group placed under two kinds) is detected as NOT nested, so the check is not `CONSTANT_SILENT`. | SUPPORTED (transcription) | `strictly_nested()`, `group_to_kinds()`, `perplexity_groups()`; the constructed cross-cut null | a delivered §1 map in which a group sits under two kinds and the check passes |
| DS_012 | §6's instruction — *do not report a kind count as a finding until a grain criterion is argued and adopted; report membership* — is encoded as a **refusal in code**, not a note. `kind_count()` returns the string `"UNSETTLED (grain not fixed…)"` and never an integer; `standing_answer()` returns SEVERAL / UNSETTLED / MEMBERSHIP; `grain()` reports the 4-vs-11 disagreement explicitly as a grain disagreement rather than as a count of the thing. This is the `domain-ledger` no-composite discipline (`DL_001`) applied to a kind count: the number that would be the tempting headline (4, or 11) is the one the instrument refuses to emit as the answer. | SUPPORTED | `kind_count()` (string, not int), `standing_answer()`, `grain()` | a code path that emits a bare kind count as the finding |
| DS_013 | The taxonomy question `DS_008` left open now **splits**, from a run designed to break the contamination confound: Runs 1 and 2 shared a system so their agreement could not tell *converged* from *remembered*, and Perplexity's blind sort had neither the K-list nor repo access. **MEMBERSHIP replicated** (an independent sorter with no menu put the same records together — the strongest single result is the proxy/wall-clock K3 core, called the sharpest discriminator by one and given the largest group by the other), so SCOPE-DIFFERENT is **SEVERAL**. **The count did NOT replicate** (4 Kimi kinds vs 11 Perplexity groups, six singletons — close to an identity map), so *how many* is grain-dependent and **UNSETTLED**. `K4` drew zero members under both sorters (carried as a Run-1-only candidate artifact); the straggler `G9`/F-TETRA-SCOPE is the record neither sorter could place and no field could name (a candidate genuine gap, next step a second name-search, not a decision). All of §2–§5 — the K5 confirmed-at-record-level-not-as-a-kind reading, the over-split/over-merge signature, the `T09` merge Kimi is right to reject, the literature audit with `[nih]`/`[arxiv]` tag defect and the G2 EXACT→PARTIAL downgrade — is carried verbatim and **not verified at record level**, because the corpus is external model output this repo does not hold and does not fabricate. **UPDATE (`RESULT_repair_adjacency`):** a third system (DeepSeek, sorting by repair) makes the *count* half legible without settling it — the disagreement was never about which records belong together but about **cut height on one shared tree** (`DS_014`). "UNSETTLED" stands, but its character is now known: not a conflict, a choice of level. | SUPPORTED (membership) / OPEN (count, now understood as a cut height) / carried (record-level detail) | `RESULT_taxonomy_crossmodel.md`; `RESULT_repair_adjacency.md`; `standing_answer()`; `KIMI_KINDS_ZERO_MEMBERS` | a fourth independent sort that returns one group (refutes SEVERAL), or a grain criterion that fixes the count |
| DS_014 | The repair-adjacency RESULT's headline — *the three groupings do not conflict; they NEST in one order, Kimi (4-5) ⊃ DeepSeek (9) ⊃ Perplexity (11), zero cross-cutting* — is a **refinement-chain** claim over the delivered memberships, checkable here without the corpus. §1 gives DeepSeek's 9 components at record level in full (19 records), §2 the two places Perplexity splits a component (C1 → T01,T07 \| T13; C9 → T15 \| T19, yielding 11), §3 the components each live Kimi kind contains. `repair_adjacency.py` transcribes these and verifies each link is a refinement: `perplexity_refines_deepseek` (every group inside exactly one component) and `deepseek_refines_kimi` (every component inside exactly one kind), both zero cross-cutting, the 4 kinds covering all 9 components. **So grain was never a disagreement — it is a cut height on a tree all three independently found**; the count (4-5 / 9 / 11) is the class / operation / operation-plus-referent level. A **transcription-consistency check, not a reproduction**: the corpus is external model output (three systems). Null-tested — a group spanning two components is caught, a genuine sub-partition passes. | SUPPORTED (transcription) | `perplexity_refines_deepseek()`, `deepseek_refines_kimi()`, `refines()`, `cut_heights()`; the constructed span null | a delivered membership set in which a component spans two Kimi kinds, or a group spans two components, and a refine-check passes |
| DS_015 | The repair reading confirms `K4` is **dead as a repair class** and flags two Kimi edges as unsettled. K4's three Run-1 blind members land in three different components (C1, C6, C7 — `k4_dead()` reports 3 distinct, `dead=True`), a **second independent failure to replicate** after both sorters gave it zero members (`DS_013`). Two live edges are carried **present but flagged, not dropped**: `C7 → K3` is a **correction candidate** — NLS-3 (scale-from-correlation) leaves the speedup core and both external systems removed it from that cluster independently, so Kimi's assignment of it to K3 is questioned; `C8 → K5` is **contestable** — adjusting for covariates (T11) is arguably also a change of comparison set, and DeepSeek said NONE for it without argument. The structural refinement holds *with* these edges (Kimi does place them there); the flags record where the tree is contested, which is a different thing from where it cross-cuts. **UPDATE (`ITEM4_ANALYSIS.md`, `DS_018`):** the `C7 → K3` correction candidate is now decided at the level the evidence allows — the *separation* of NLS-3 from the speedup core is adopted (two-system replication), the *target kind* stays UNVERIFIED (needs the record). | SUPPORTED (K4 dead) / carried (contested edges) | `k4_dead()`, `CORRECTION_CANDIDATE`, `CONTESTABLE`, `component_to_kind()` | a third sort that gives K4 a connected repair cluster; an argument settling C7→K3 or C8→K5 |
| DS_016 | The straggler `T13` (F-TETRA-SCOPE), unassignable to Kimi and unnamed to Perplexity (`DS_013`), was **placed by DeepSeek in C1** with the two-reference-temperature record TMP-2 — the shared repair being *identify which of two co-present referents governs, and pass that one*. This is the **one cross-cutting event** in the whole exercise (it joins a K1 member to a record Kimi left outside every kind) and it puts pressure on Perplexity's NO-NAME-FOUND, since reference disambiguation with two co-present referents is named in several fields. **It is NOT closed here**: one system, one placement, no argument given. The document's own cheap decisive test — *does T13 reduce to TMP-2's shape (two referents present, the governing one not declared)?* CLOSE if yes, sharpen the gap if no — is carried as the next step, a **second independent check**, not a decision. `taxonomy_replication.py`'s `G9` straggler and this `T13` are the same record under two labels; neither module marks it resolved. **UPDATE (`ITEM4_ANALYSIS.md`, `DS_019`):** the reduction to TMP-2's shape (two frames as the two referents, which refutation criterion governs undeclared) is now argued and found PLAUSIBLE — more than DeepSeek's placement alone — but still **NOT closed**, because the second independent name-search the RESULT names is an egress-blocked literature query; the gap stands sharper. | carried (placement) / OPEN (the gap) | `RESULT_repair_adjacency.md` §4; `STRAGGLER`, `STRAGGLER_COMPONENT`; `G9` still unassigned in `taxonomy_replication.py` | the T13-reduces-to-TMP-2 test run and settled either way |
| DS_017 | §6's standing answer is extended and its refusal sharpened: the count *depends on the cut height, and the tree is now known* (4-5 class / 9 operation / 11 operation+referent), so `repair_adjacency.kind_count()` returns **a cut-height statement, never a bare integer** — *a single number is a cut, and a cut with no stated height is the thing the instrument exists to catch* (the `DS_012` refusal, now with the tree behind it). `standing_answer()` reports both endpoints with the nesting stated and membership fixed at every level. §5 (the recurring external error: three systems, three collapses of increasing severity of the five speedup records, DeepSeek declaring all ten pairs mutual restatements, Kimi alone reading the transforms not the prose — the `T09`/ENG-3 sign-inversion case) and §7 (asymmetric adjacency permitted and never used; CANNOT FALSIFY permitted and never used; the tree built from ONE repair reading) are carried verbatim and **not verified at record level** — the corpus is external, and §7's own caution is that a second repair pass is what would test whether 9 is stable or is DeepSeek's grain the way 4 was Kimi's. | SUPPORTED (cut-height refusal) / carried (§5, §7) | `kind_count()`, `cut_heights()`, `standing_answer()` | a code path emitting a bare cut count as the finding; a second repair pass that returns a different component count |
| DS_018 | **NLS-3 in K3, decided to the extent the evidence allows** (`ITEM4_ANALYSIS.md`): the *separation* of NLS-3 (scale-from-correlation, component `C7`) from the speedup core (`C2`) is **adopted** — two independent sorts (Perplexity by subject matter, DeepSeek by repair) removed it from that cluster, which meets `DS_013`'s two-system-replication standard, and `repair_adjacency.py` already places them in different components. So Kimi's grouping of NLS-3 *with* the speedup core is a confirmed error, not merely a candidate. The *target kind* (K3 vs K1 vs its own) stays **UNVERIFIED** — deciding it needs the NLS-3 record (external corpus, not held), and the two-system evidence establishes separation, not the kind. The split — separation decided, kind open — is the honest reading; nothing external is fabricated to close the kind. | SUPPORTED (separation) / UNVERIFIED (target kind) | `ITEM4_ANALYSIS.md`; `repair_adjacency.py` C7 ≠ C2; `DS_013` standard | a sort that regroups NLS-3 with the speedup core; the NLS-3 record fixing its kind |
| DS_019 | **F-TETRA-SCOPE (T13), argued and left open** (`ITEM4_ANALYSIS.md`): the restatement *two referents present, the governing one not declared* is found **PLAUSIBLE** with an argument — the two frames are the two referents and which refutation criterion (≠4 vs ≠3) governs is undeclared, mapping onto TMP-2's two-reference-temperature shape — which is more than DeepSeek's placement alone (the handoff and `RESULT_repair_adjacency.md` §4 both forbid closing on that). It is **NOT closed**, because the RESULT's own next step is a second independent name-search (is reference-disambiguation-with-two-co-present-referents a named object in several fields?), which is an egress-blocked literature query, and no field name is fabricated from memory. The gap therefore stands **sharper**: the specific reduction is on the table to confirm or refute, not an unstructured NO-NAME-FOUND. | PLAUSIBLE (argued) / NOT CLOSED (name-search egress-blocked) | `ITEM4_ANALYSIS.md`; `RESULT_repair_adjacency.md` §4 | the name-search run on a reachable source: the reduction confirmed (gap closes, T13 joins C1) or a distinguishing feature named (gap stands) |

## REFUTATION_PROTOCOL

A falsifier that fires updates the claim's status in place with the
evidence that fired it; the checks in `selftest_ds.py` are not loosened
to keep a claim. `WORK_ORDER.md` is not edited, and the seeded cells in
`CELLS.md` are carried as delivered — `T3 x S5`'s invalidity (`DS_002`)
is recorded, not repaired, because repairing the seed would put a
MEASURED_AS in the author's mouth.
