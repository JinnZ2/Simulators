# probes

AUDIT-AUTHORED, every one. The delivered module ships `CLAIM_SCHEMA`
and no claim file, so `--check` has nothing to run on.

These are **not cases**. No real person's origin claim is recorded here
and none is judged. They exist to reach the code paths — one per branch
of `check()`, plus the module's own `PROOF_CASE` rendered as a claim so
the instrument can be run on its own proof.

Inventing a case would put a claim in someone's mouth, which is the
`PB_001` / `CW_004` rule. Reaching a branch is a different thing and is
labelled as such in every file.
