All three files are CONSTRUCTED. No model was run; every response was authored in
this folder so the scorer's branches can be shown reachable. Their first row
says so in a `_constructed` field the loader skips. Nothing in them is
evidence about any model, and `samples/` reports on them and nothing else.

`responses.refute.constructed.jsonl` is the third world, added under
WORK_ORDER_2 W6 so the refutation branches the first two did not reach
(AP-2, AP-4, AP-6) are reachable, along with an ABSENT row, a replicate
collision, an AP-3 tie and a control with a spurious second entry.
