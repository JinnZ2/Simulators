Both files are CONSTRUCTED. No model was run; every response was authored in
this folder so the scorer's branches can be shown reachable. Their first row
says so in a `_constructed` field the loader skips. Nothing in them is
evidence about any model, and `samples/` reports on them and nothing else.
