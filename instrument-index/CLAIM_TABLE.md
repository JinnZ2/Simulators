# CLAIM TABLE -- instrument-index

Ids are permanent and never renumbered. `II_*` are claims about THIS
rebuild; they are not claims about the lost original, which nobody here
read.

REBUILT 2026-09-22 from design recovered from session 2026-09-20
transcript fragments. Original bytes lost.

| id | claim | status |
| --- | --- | --- |
| II_001 | The delivery's `repo:` field is the literal placeholder `[where instrument-index lived]`, and a content search over the whole tree (`instrument-index`, `INDEX-SPEC`, `build_index`) returns one hit, an unrelated line in `sheet-structure-scan/scan4.py`. No prior artifact survives here. The folder is placed as a new top-level folder in this repository and that placement is a REBUILD-CHOICE, not a recovery. | OBSERVED |
| II_002 | `NOT-SCANNED` and scanned-with-zero-rated-rows are kept apart, and the spec says why: collapsing them reports a coverage hole as a zero. Both states are reached by the fixture (`fixture/missing-repo`, `fixture/empty-repo`) and render different sentences in the MD. The absent-vs-known-negative repair, designed into the recovered spec rather than found in audit. | OBSERVED |
| II_003 | `claim_only_fraction` returns `None` when no row is rated and `0.0` when rated rows exist and none is CLAIM-only. The two are not cosmetic: `0.0` returns "axis holds at this build" and `None` returns "UNRATED: check not run" -- opposite readings of one build, separated by this field alone. Registered in `tools/known_answer.py` with the 0.0-against-two-Nones case as the pin. | OBSERVED |
| II_004 | The axis check cannot fire on a corpus nobody has headered, by construction: `rated` is empty, so the falsifier is silent. That is `CONSTANT_SILENT` in this tree's vocabulary, and the recovered spec names the state rather than letting it read as a pass. Both firing branches are reachable and are shown so: the main fixture returns `axis holds at this build` at 0.000, the planted set returns `AXIS FALSIFIED` at 0.750. | OBSERVED |
| II_005 | All four flag conditions fire on the fixture and none alters a row value: `BOGUS` is kept as `BOGUS`, `WIDGET` as `WIDGET`, `guesswork` as `guesswork`, and an `EXTERNAL` run_cost with no basis keeps the empty basis. Flags reach stderr and the MD file; the MD copy is what makes them a committed record rather than a terminal artifact. | OBSERVED |
| II_006 | The blank-line rule is a REBUILD-CHOICE with a cost. "Stops at the first non-comment line" read literally means a blank line terminates the block, so a header placed below a shebang-plus-blank is not read and the file emits `UNRATED`. The cost is bounded by the spec's own NEVER DROP A FILE rule: the failure is visible as an unrated row on the located to-do list, not as an absence. | DERIVED |
| II_007 | `status` and `gate` are not flagged, and `gate` in particular is open by construction -- `UNRUN` is a default, not a claim that a gate ran and returned nothing. Flagging a defaulted field would report a silence as a finding. | DERIVED |
| II_008 | The in-file header wins over the override PER FIELD, not per file, so an override can fill a field the header omits while losing every field the header states. Shown both ways on one fixture file: `branch_set.py` carries a header and an override, keeps the header's name and shape, and `overridden.txt` carries no header and takes the override whole. | OBSERVED |
| II_009 | An index generator walking a tree that holds its own output is the `UNI_010` self-reference loop, and it is closed by `SKIP_NAMES` for `INSTRUMENT-INDEX.tsv` (recovered) and `INSTRUMENT-INDEX.md` (REBUILD-CHOICE). The recovered skip list carried the TSV and not the MD, which would have let run N+1 index run N's human view. | OBSERVED |
| II_010 | `built_against` inside a git working tree resolves through `git rev-parse`, which walks UP, so a fixture living inside this repository gets this repository's short hash and the `nogit-` branch is unreachable from it. The fingerprint path is therefore exercised against a temporary directory outside any repository; without that the fallback would ship untested and read as covered. | OBSERVED |
| II_011 | The recovered smoke test exercises neither the override fallback nor the `nogit-` fingerprint, though the spec states both. The fixture is extended to cover them, and the extension is recorded rather than folded in silently: two fixture files (`odd_shape.sh`, `overridden.txt`) and one `index-overrides.json` are new. | OBSERVED |
| II_012 | Every `[RECOVERED]` label in `INDEX-SPEC.md` is a claim about transcript fragments this session did not read. Nothing here establishes that the recovered text matches the lost original; what is established is that this build implements the text as delivered. The distinction is the reason the label is on both files. | UNVERIFIED |
| II_013 | `sheet-structure-scan/no_severity` is not applied to these renderings. The delivered spec fixes the output strings verbatim (`AXIS FALSIFIED`, `GATE: FAILED`, `Validity flags`), and screening them would mean rewording delivered text, which is the one edit this folder does not make. Recorded as a scope decision, not an exemption. | DERIVED |
| II_014 | The stamp is UTC at build time and is shared by both renderings, so a diff between the TSV and the MD is never a diff in the stamp. The spec's single named drift risk is hand-editing, and the two files are written from one `collect()` in one process; nothing in either is derived from the other, asserted by comparing the id sets. | OBSERVED |
| II_015 | The folder is the first in the tree to put its test file in a subdirectory, and that surfaced a defect in `tools/run_manifest.py`. `build_index.py`'s refusal names `instrument-index/tests/test_build_index.py`, which is where the file is; the extractor's `.py`-name pattern captured only the basename, and `resolve_targets` joined it to the module's own folder, so a live target was reported missing and the folder landed red on two tree-level checks. A flat layout hides it, because the basename happens to resolve beside the module. Repaired in the extractor (the pattern keeps a directory prefix, a path-bearing target resolves from the repo root, and the own-name skip compares basenames), with both cases pinned in that module's selftest. | OBSERVED |
