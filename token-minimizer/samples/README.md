# Sample outputs

A small geometry file written by `python3 compress.py` from the parent
directory. The `.geo` extension is the local-reference format described
in `../NOTES.md`; a query that names this reference costs ~1 token
instead of inlining the full structure.
