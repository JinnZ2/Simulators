# frame-instruments -- claim table

Ids `FI_NNN`, permanent. Status is one of SUPPORTED / REFUTED /
UNVERIFIED; a finding whose defect was repaired stays SUPPORTED as a
finding about the delivered build, with the repair named. Every
computed number is reproduced by `python3 audit.py`.

| id | claim | status | number | falsifier |
|---|---|---|---|---|
| FI_001 | Under one Python process, `b4`'s `import report` resolved to `b1/report.py` and 3 of 13 B4 tests failed; a repo-wide test sweep would have hit it. Repaired: each test file purges its own module names before importing. | SUPPORTED, repaired | b1 then b4 in one process: 3 failures before, 0 after | a single-process run of all four suites with a failure |
| FI_002 | Adjacent-L stability on the top-decile set is identically 1.0 by construction, since `div_D` does not depend on L, so N3's L half could never fire. Repaired: a second position set (`separation_resync0`) carries the L dependence and N3 reads both. | SUPPORTED, repaired | top-decile L Jaccard values {1.0}; separation-set L Jaccard < 1.0 on the fixture | a top-decile L Jaccard below 1.0 on any input |
| FI_003 | The cyclic Latin square balances position and confounds carryover: 4 of 12 ordered successor pairs occur, each 3 times per block. Repaired: Williams square, every pair once. | SUPPORTED, repaired | before 4 distinct / max 3; after 12 distinct / max 1 | a block with a repeated successor pair |
| FI_004 | A commit appended after release validated a rewritten D1 and the anchoring count read 0 where the truth was 1. Repaired: D1 has to hash to the commit recorded in `released.jsonl`; every D row requires a release record. | SUPPORTED, repaired | rewritten D1 refused: True | a D1 row accepted whose hash differs from its release record |
| FI_005 | `resync` compares position-aligned, so a continuation rejoining the base shifted by one token reads `resync 0` at every D and L while `div` falls to 0.0156 at D=128. The two readouts disagree on the alignment [CHOICE]. Not repaired: an offset-tolerant rejoin is a definition change owned by the reference spec. | SUPPORTED, recorded | resync {0}; div 0.25 / 0.125 / 0.0625 / 0.031 / 0.0156 | the spec author fixing the definition either way |
| FI_006 | N4 compares the real stability to ONE permutation draw. Measured before the FI_008 repair: real 0.5, permuted 0.278 to 0.583 across 30 seeds, N4 firing in 5. Measured after it (ties included): real 0.672, permuted 0.469 to 0.546, firing in 0. The margin is one seed wide either way and narrowed once ties stopped selecting by name; the spec asks for one permuted copy, so the note is added to N4 and the spread is printed here. | SUPPORTED, recorded | after repair: real 0.672; permuted min 0.469 max 0.546; triggered 0 of 30 | zero spread across seeds |
| FI_007 | With one auditor per side the A-vs-D1 check reported `failed: False`, a vacuous pass. Repaired: `evaluable: false`, `failed: null`. | SUPPORTED, repaired | evaluable False, failed None | a one-auditor input reading `failed: false` |
| FI_008 | When `div_D` saturates the top decile was the first `ceil(n/10)` rows by position string, so adjacent-D overlap was 1.0 by tie-breaking. Repaired: rows tied at the cutoff are included and `n_tied_at_cutoff` is printed; a saturated stratum reports itself whole. | SUPPORTED, repaired | 12 tied rows: top_decile_count 12, n_tied_at_cutoff 10 | a saturated stratum with top_decile_count < n_rows |
| FI_009 | N2 assumes the traced positions are the stage-B high-entropy selection, and `separations.jsonl` carries neither N nor the selection rule, so a traces file built by any other rule reads identically. Structural, no probe. | SUPPORTED, recorded | schema: no `N`, no selection field | a selection field in the schema |
| FI_010 | Builder and auditor are one system. Every finding above is computed by `audit.py` and re-checkable; six repairs run in the build's favour and two recorded items are left because a repair would change a definition the spec owns. | declared | 8 computed, 6 repaired, 2 recorded | a finding here that `audit.py` does not reproduce |
| FI_011 | Nothing here is evidence about any model, reader, auditor or case set; every number is on constructed fixtures. | UNVERIFIED | -- | a real `traces.jsonl` or `cases.jsonl` run |
