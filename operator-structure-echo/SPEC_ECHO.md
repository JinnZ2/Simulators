# ADD TO: handoff-provenance spec (or its own module —
# operator's call which)
# STATUS: marker under exploration.

## OPERATOR-STRUCTURE ECHO

Observed: instruments built in this ecosystem recur to the same
structural shape the operator uses to process the domain by hand.

Instances so far:
- s4: no doe (selecting agent absent from the model; absent from
  the literature the model was built against)
- allocation_coupling: no untenured continuous observer
  (presence derived from tenure)
- allocation ledger: the module's cost accounting is the same
  accounting the operator runs manually before a social retest

The third is the one that raises it from a per-repo note to a
subject. The first two are the instrument missing a case. The
third is the instrument REPRODUCING the operator's own
processing structure in a domain the operator did not intend
to model.

Not a defect claim. Not a discipline failure. It is a property
of a single-builder instrument and cannot be removed by
effort — only counted.

## WHAT TO DO WITH IT
Do not correct. Log.
For each module, one line: does this shape match a procedure
the operator already runs unaided? YES / NO / UNEXAMINED.
Default UNEXAMINED. Only the operator can fill it.

## WHY IT MATTERS
If YES is frequent, results across modules are not independent
— they share a generator. Any agreement between two modules
is then weaker evidence than it appears, because agreement may
be inherited rather than found.

## OPEN
- base rate unknown; no count yet
- no method proposed for detecting it from inside
- unknown whether it is separable from ordinary domain
  expertise
