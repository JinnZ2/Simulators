# Simulators

A collection of CC0, stdlib-only simulators and audits grounded in physics,
not in opinion. Each folder is intended to be promotable to a standalone
repo.

## Read first — the meta-spine

The methodology spine is documented in six root-level files. Read them in
this order before running any simulator:

1. **[`PREAMBLE.md`](PREAMBLE.md)** — the physics baseline as a direct
   declarative statement. The shortest reachable surface. Thermodynamics
   doesn't negotiate.
2. **[`PROTOCOL.md`](PROTOCOL.md)** — the same baseline as a protocol for
   any intelligence reading the repository. Names the
   Model/Reality Dissonance failure mode and the Ground State
   acknowledgement test.
3. **[`BNRAM_STRICT.md`](BNRAM_STRICT.md)** — finalized repository
   addendum (Bias-Neutralization & Reality-Audit Module v2.0). Operating
   mandate, Physical Audit Protocol, Diagnostic of Deflection, the
   five-section BNRAM (Entropy-Constraint Engine, Universal Physical
   Constant Cross-Check, Literal Baseline Lexicon, Shadow-Variable
   Handling, Dissonance-Forced Reset), operational rules, archive
   requirements.
4. **[`PVL.md`](PVL.md)** — Phenomenological Validation Layer. Inverse
   audit (physical-state query before literature search), cross-temporal
   correlation (persistence over volume), opaque-source flagging.
   Integrates with BNRAM Shadow-Variable Handling.
5. **[`SHAPE_SPEC.md`](SHAPE_SPEC.md)** — definition spec, upstream of
   every folder that uses the word SHAPE. **`SHAPE = the constraint set a
   geometry is a solution to`** — not the geometry, not the picture, not
   the name the field gave it. So cross-domain recurrence is the same
   problem solved twice rather than analogy, and it is checkable. Point at
   this file rather than restating it; checks live in
   [`shape-spec-audit/`](shape-spec-audit/).
6. **[`METHOD_SPEC.md`](METHOD_SPEC.md)** — companion to the above,
   stating its **epistemic class**. Constraint-set reasoning is a
   *method*, in the class of syllogistic logic and dimensional analysis,
   so **it is not falsifiable and does not need to be** — the falsifiable
   layer is the individual read and its removal test. Argues the same
   epistemic act as conventional experiment with control traded against
   trial count, states four standing limits, and adds the **shadow read**:
   a shape described by the gaps it casts.

Each simulator-specific folder may also ship an aperture document
(e.g. [`incentive-blindspot-sim/00_APERTURE.md`](incentive-blindspot-sim/00_APERTURE.md))
that pins its variables to physical functions and the conservation laws
underneath. Read those before running the corresponding script.

## Layout

| Folder | Substrate | What it produces |
| --- | --- | --- |
| `token-minimizer/` | natural-language queries | compressed energy_english + geometry refs |
| `emergence-stability-simulator/` | multi-agent dynamics | Monte Carlo claims (`EMRG_*`, `SENS_*`) |
| `research-stability-audit/` | published research + models | falsifiable claims about field-level drift |
| `continuity-audit/` | incentive field × diversity field | continuity verdict + falsifier + trajectory |
| `substrate-emergence/` | material substrate profile | architecture-the-ground-wants |
| `neural-augmentation-audit/` | proposed augmentation channels | cost-accounting scaffold with `[E]`/`[I]`/`[S]` confidence |
| `incentive-blindspot-sim/` | institutional incentive structure | coupled-state model + four falsifiable claims under REFUTATION_PROTOCOL |
| `aperiodic-order-sim-stack/` | a delivered results drop (report + figures) | audit of it: estimator-disagreement finding, matched-N control, eight `AOS_*` claims |
| `reasoning-gate/` | a simulation's declarations and claims | fail-closed deny, eight guards across three stages, a per-run JSON report |
| `reasoning-dial/` | two drops on thinking-budget as a dimension | dial gradients, a knee-rule defect, an overthinking branch, fifteen `RD_*` claims |
| `triad-playground/` | (physical, instrument, reasoning) triads | triad schema + generated checklist, shadow-panel N_eff analysis, thirteen `TP_*` claims |
| `measurement-fork/` | one system, three measurement designs | four-cell design diff, adjudicated growth edge, twenty `MF_*` claims |
| `declared-frame/` | any measurement, model, or claim | six-field frame block + comparability check, a v2 rewrite, ten `DF_*` claims |
| `anchor-interval/` | a system fitted to a corpus it writes into | coherence-up/coupling-down loop, two graded detectors, provenance regimes, eleven `ANC_*` claims |
| `simulation-hypothesis-budget/` | what a Planck-resolution simulation of the observable universe would cost | two energy floors (Landauer, Margolus–Levitin); the Planck-volume count is wrong by ~61 decades against the holographic bound; the cross-frame ratio is **refused** in code (G-DIM); cost scales `L⁻⁴` so the undefended resolution assumption moves the answer by 10¹¹⁴; the only frame-independent result is that no system can simulate itself; plus `multiscale.py`, where allowing several resolution levels spreads the answer over **216 decades** and the render-on-observation floor is ~34 MJ, about a litre of gasoline; and `consequence_frame.py`, which measures what the idea *licenses* rather than what it costs — the "it is a simulation, so the ripples do not count" inference needs one 2×2 cell (observed **and** uncomputed) and that cell is empty in every architecture that can produce its own observation record, so the step fails at every cost independently of whether the hypothesis is true, while *why* anyone states it is held OUT_OF_SCOPE with the author's own interest direction declared; plus `LADDER.md`, a four-rung audit of the folder **delivered from outside it** and checked rung by rung in `ladder_audit.py` — two rungs land (layer 1 is a consistent calculation with three unverified interpretive steps, relabelled `DECIDABLE` → `ARITHMETIC`; and Landauer prices **erasure** while Bennett showed **measurement** can be reversible, so the litre of gasoline prices a different operation), one lands somewhere other than where it is aimed, one was already held — and `SHB_013` is **REFUTED by its own falsifier**, the first refuted claim in the folder, without a single number being retuned; and `ERA_METAPHOR.md`, a second outside audit placing the hypothesis as the current instance of artifact-becomes-cosmology (clockwork → Laplace's demon, steam → heat death, telegraph → switchboard mind) and checked in `era_metaphor_audit.py` — **both documents' pointers into the claim table were off by one and both corrections made the auditor's case stronger**; the ceiling gap lands on `SHB_010`, since `multiscale.py` sources its architecture set to computing practice *in its own docstring*, so 216 decades is a spread over what our machines do and the architecture space is not enumerable from inside; plus `EARTH_TRANSITIONS.md`, a third outside audit counting Earth's phase transitions against Lloyd's 10^120 ops — it supplies the **first independent confirmation of a number here** (the Margolus–Levitin route reproduces the ceiling at 10^122.9), its 10^110 turns out to be a *stepping* count where the eight labels contribute 0.9 decades, and its 10^42 overshoot needs a **double-count**: a per-timestep cost multiplied by a per-transition count, where every internally coherent model instead FITS; plus `SCALING_CLASSES.md`, a fourth outside audit itemising eight computational loads against the same ceiling — four rows reproduce exactly, one drifted ten decades between drops in a column that stays self-consistent at any value, "everything polynomial fits" needs one clause (`N³` at Earth's atom count is 10^150) and the exact form is the **crossover**: `2^N` at **399 components**, `3^n` at **252 residues**, `N²` at 10^60 bodies — and the drop's own closing argument (folding is funnelled, not searched) retracts one of its three EXCEEDS rows while the quantum row survives for a reason it does not state, pricing a **substrate** rather than a method (Feynman 1982), which makes it the only row that constrains the hypothesis rather than our algorithm; its opening then replaced by the author with the transmissible core — **"the exponential is a property of the representation, not of the system; check which one you are pricing"** — together with three corrections to this audit: the N-body tag (`CONSTRUCTION_FITTED` → `LABEL_TRUNCATED_IN_TRANSFER`, the term was in the source label and lost in transfer), the Barnes-Hut saving (28 decades not 35, since `N log N` is *per step* — dropped in the same paragraph that objected to the missing timestep factor), and the substrate row **narrowed to volume-law entanglement**, since area-law states are polynomially representable in tensor-network form and cover most ground-state chemistry and condensed matter; forty `SHB_*` claims, one REFUTED |
| `uninstrumented/` | quantities excluded by an instrument's constitution | eight-mechanism register + `scan.py` text scanner, checks on both, nine delivered cases the schema cannot hold, plus a `specimens/` directory and `AVENUES.md`; the first case whose WOULD MEASURE is a runnable design, whose decoupling arm rests on a premise that is false and carries no error bar, and which places itself inside its own sample; plus its `selfreport_probe.py` harness, where that missing error bar becomes a number (n=1 per frame) and blinding turns out to be by instruction rather than by construction; a literature-occupancy audit that retires four build targets without apparatus, alongside a trait/acquiescence case whose own source concludes against it; a first MARKER the schema cannot hold at all, whose deliberately empty noun slot is the first to arrive with a replacement structure; and a `playground/` of three constructed-ground-truth modules — the harnesses landed a drop after being described in the past tense while absent, with a blinding defect recurring in two of three and the seed probe facts spent by their own publication; a second MARKER whose own control cell is empty by construction, and a third that names its own falsifier and then instances its own compounding mechanism on its own strongest number; a third specimen that is the first to actually contain a pasted output; and a fourth whose T1 was actually **built and run** (`selection_cuts.py`), returning that three of its four cuts do not separate its own calibration set; and `specimens/INSTANCE_LOG_SURVEY.md`, an org-wide search that locates 29 prior instance records for the two test-subject rows marked untested — reporting their schemas as-is and proposing a mapping without applying one, since no located record matches either row's stated checkpoint, stimulus or rubric, plus `specimens/INSTANCE_LOG_INDEX.md`, the 53-record index built on it — computed shape signatures, vocabulary left un-normalized, every gap marked unrecorded, eight verbatim-evidence links between same-event records under different schemas (two of which disagree), and a column for what the older logs hold that neither target can; maximum overlap with either target by exact field name is 2 of 25; plus `cases/024refusalfalsepositiverate.md` and `specs/SCOPED_REFUSAL.md`, the first case shipped with its own falsifiable spec — whose falsifier was tested inside the session and did not fire, because the network egress classifier already runs requirements one to three and lacks only the fourth; one hundred and fifty-three `UNI_*` claims |
| `nonidentity-census/` | how much work models systems without a persistent identity-bearing unit | boundary decisions written before the detector and parsed by it; T1 built and null-tested, and its headline is that the detector built to escape lexical detection decides 10 of 12 lexically; T2 NOT RUN and not approximated, the abstract APIs being blocked by egress policy; T4 tested against all eleven mechanisms and filed nothing, since its precondition is the unmeasured rate; T5 finds the design exists only at material effect held constant at zero; then a second instrument supplied by the operator — the verb-first test, six options not two — takes the lexically-decided share from 10 of 12 to 0 of 12, agrees with the first on 9 of 12, and returns three results against its own design, including that the option argued for hardest is one the test cannot see; then a third instrument replaces T2's output with a distribution of dissolution windows, and splits the one window the proposal names into the two it uses — two of its own three worked examples being measurement intervals, so the ratio between them becomes a G-RES pair reporting whether a study could have seen its own term dissolve at all; and T6, whose null test passes with both off-diagonals built but whose own instrument column turns out to track how the rows were built rather than the hypothesis, at 5 of 12 against 0 of 12 on a matched set |
| `criteria-drift/` | benchmark and rubric versions over time | signed drift per frame field, anchor-version decomposition, a regression that refuses to run unidentified, nine `CD_*` claims |
| `photoperiod-claim-harness/` | four inconsistencies in a published greenhouse result | runnable falsifiable sims + mechanism-edit protocol + bench protocol; six `PCH_*` claims on the harness |
| `category-weld/` | terms that fuse independent quantities into one handle | ninth exclusion mechanism for `uninstrumented`, a three-readout scorer, three terms, an audit finding refuted by the second drop, seventeen `CW_*` claims |
| `presented-binary/` | a two-option framing, before it is answered | eleven documented/asserted/absent checks + a sealed two-pass frame simulator with a blind post-hoc rater + a handoff router to mechanism 10, fifteen `PB_*` claims |
| `generation-capacity/` | the option space a party can produce, not a value in it | tenth exclusion mechanism + sub-case 10A + R1/R2/R3 scorer with a place-vs-center calibration guard; a framing under it scores 11/11 on the audit next door; twelve `GC_*` claims |
| `moral-decomposer/` | a disagreement presented as moral or ethical | three-stage decomposition to option-distribution claims + frames; the readout is the residue; three cases, one external; eight `MD_*` claims |
| `domain-ledger/` | a confidence readout, the domain set behind it, and what it anchors to | four uncombined ratios with their denominators printed + a three-band anchor map that refuses to emit a composite, and a candidate definition for the term it turns on, held open on stated non-independence; fifteen `DL_*` claims |
| `closure-cost/` | recorded cases where a variable was closed before the event arrived | instrument vs event branches, a named rival hypothesis held as a schema field, three cases and zero quantified, ten `CC_*` claims |
| `constraint-assembly/` | cases where sufficiency was composed from parts that individually do not do the job | three constraint classes (invariant / consumable / soft), rejected candidates as the data, a fail-closed composition test and a diagnostic-quarantine field; two cases, zero quantities, two readouts that disagree on one of them; thirteen `CA_*` claims |
| `held-open-uncertainty/` | the assumption that holding variables open means not acting | nine questions with per-entry provenance, one retraction inside the entry that carries the claim, and the cheapest runnable experiment specified against a harness already in the tree; six `HO_*` claims |
| `adaptive-claim-loop/` | an adaptive simulation framework, and the one move it usually has | five typed responses to a refutation with the parameter walk removed from the vocabulary, four verdicts, four termination states, session-stamped provenance; the delivered framework's own log replayed through the gate, the gate null-tested against two corpora written for other folders (TP 1.00 / FP 0.00), and an adversarial responder that got a parameter walk admitted in five attempts before the repair; seventeen `ACL_*` claims |
| `derivation-discarded/` | a persisted structure as the only copy of its own derivation | eleventh exclusion mechanism; the anchor case is EIA post-auditing, where the accuracy figure is scored against the prediction list — three published narrowings that are never multiplied; eight `DD_*` claims |
| `instrument-bias-sims/` | nine sims from delivered work orders, each testing one way an instrument's own construction shapes what it reports | marker under exploration, not a thesis. S1 event-sampled observation reconstructs a false baseline, and the distortion is a **product** of event triggering and cost weighting; S2 a one-arm anchoring protocol is **underdetermined, not merely biased** — the extra arms are the second equation; S3 the false-null rate is **not computable** from a list selected on later concession, and the column *varies*, tracking permissiveness rather than accuracy; S4 (patched) engagement rate separates the two models only at particular parameter values, and the patch found two defects in the module's own code — a `rank_prospect` series hardcoded from the antler-rank model, so model A was fitted to its own conclusion, and an antler model carrying mass only, so the annual delta went to zero at maturity; plus a **structural rule now adopted folder-wide: the AGENTS section comes first and a missing agent is a visible `[BLANK]`**, earned because the pre-patch file had no doe in it at all; S5 the genetic-conflict criterion is **not empty** — it is a prediction, and the case it excluded is the case that tests it; S6 the stated uniformity statistic is a **range**, so adding difficulty levels inverts it and a slope does not; S7 observer-dependence is near-analytic and the cost readout is a consequence of a stipulation; S8 the normalisers agree on the sign and disagree **4286×** about what would count as parity. `crosscutting.py` enforces the four cross-cutting rules over all eight and is null-tested on a planted violation. S9 a corpus samples observer positions non-uniformly with **no filtering agent anywhere in the chain** — the slot renders `[BLANK]` and the blank is the finding — and the spec's multiplicative reading **overstates** the suppression, since coupling drives the conjunction's excess to **1.85** rather than below 1.0. **Five results ran against the draft** and are recorded rather than smoothed |
| `tools/` | shared utilities | validators, substitution toolkits, gate-drift check, and the known-answer gate — no metric ships without a case whose answer was fixed in advance, earned from two metrics in this tree that were wrong in ways reading them did not catch, and whose first action was to refuse its own seed |
| `alignment-under-coupling/` | a marker under exploration, its first run's results, and — arriving afterwards — all four sim generators | three phenomena proposed as one family of coupling-plus-field transitions, at confidence `~0.40` before run 1 and `~0.40` after. SIM-D's derived constraint has its formula backwards (`T*(1+s)` holds only at `s=0`; the composition is `T/(1+s)`) while the prose conclusion survives and gains the undo temperature `T = 1+s`; SIM-C returned the **intact** 5×5 grid at every sigma. **Two of my own verdicts inverted when the code arrived** — the damping is a convex combination and not a cancelled scaling (cancelled in 0 of 200, against 200 of 200 for a scaling), the real defect being a floor 46× below anything the dynamics produce; and the head/tail entropy split shows the loss is 19× larger in the tail, so the drop's own UNRESOLVED contradiction dissolves and the reported mechanism is **reproduced**. Ten `TFM_*` claims, one UNVERIFIED |
| `shape-spec-audit/` | checks on the root spec family — `SHAPE_SPEC.md` and `METHOD_SPEC.md`, both delivered verbatim | the spec's NOTE ON COST is right and the proof is a duality it does not state — minimising dissipation at **fixed volume with no cost anywhere** returns `2^(-1/3)` = 0.793701, identically to the priced form, because the cost coefficient is the Lagrange multiplier on a physical constraint. Against that: §10's *"a repo that says SHAPE means section 1"* is refuted here **by §1's own second consequence** (964 occurrences, six distinct senses in a hand-coded sample of seven); §4's removal test compares a lung to a delta and §5 puts those two in different constraint classes, so the test's own worked example is not a matched pair — with the repair already written one section below; §7 defaults contradicting reads to instrument error, disarming §4's one refuting branch exactly where §4 specifies it; and all four shape entries in the tree score **0 of 4** required fields under a rule with no cell for the state that occurs. **METHOD_SPEC then arrived aimed at this folder**, blocking a misapplication observed "in the session this file was written in" — run against the ten claims, **0 of 10** range over the method, declared as self-grading. Its §5 (*not upgraded by more instances sharing the geometry without a checked constraint set*) confirms `SS_005` against its own §3; its §3 supplies the asymmetry that shows §7's default falls on the **better**-determined branch; and its §4 shadow read turns out exact enough to compute with — `shadow_read.py` makes it decidable, recovers a square at 4.000000 and a circumscribed hexagon at `2√3`, reaches the `INCONSISTENT` state §4 has no cell for, and shows tangents recover only a convex hull (L-shape and hull agree in 72 of 72 directions), so a non-convex object can never close. Seventeen claims, three UNVERIFIED |
| `membership-probe/` | a pre-flight that detects a checker using an **ideal rendering as a membership test** instead of reading the constraint set | two trap classes plus a control gate plus a coverage axis, reported separately rather than averaged. `cases.json` did not arrive, so all four entry points raise and it is **not reconstructed** — a case set is data. **Two of the audit's three predicted failures were refuted by the code**: the control gate catches every constant-answer checker on its own, which narrows the README's stated case for `trap_b` while confirming its real one. Two gaps survive and are one shape — a checker that hedges every trap, and one that skips every trap, both reach *"Safe to hand constraint-set work to this checker"*, because an unanswered trap is scored as an absent error rather than an absent answer. The selftest prints its pass condition and returns 0 whatever happened, and that pass condition is unreachable anyway without the missing file. Nine `MP_*` claims |
| `sim-span/` | built to a spec: can a span-reporting rule manufacture a U between reported sleep duration and an outcome that has none in true sleep | `span = true_sleep + frag * wake_cost` is time in bed, so the reported axis mixes two independent variables. **Three legs, three answers, and the spec's falsifier is scoped to the one that cannot carry the question.** `flat` gives 4 Us in 360 combinations with `a` an order of magnitude below the others and no separation from its control — the falsifier as written essentially fires. `mono` gives 124 and **none** inside the 6–9 h window where published minima sit, floor **10.16 h** — a U manufactured reliably and always in the wrong place. `frag_driven` gives 63 and **52 land inside the window** — if the outcome is driven by fragmentation and people report time in bed, a duration U appears where the published one sits, from a truth with no duration term. Two things came out wrong first and are kept: the spec's own U criterion fired on pure noise 133 times in 360, and `round_half` rounded 7.25 down on banker's rounding. Logged 2026-08-24. **Addendum:** an operator note proposes a three-column test on real cohorts, run against the sim in `three_column.py` — its stated form is additive where the quantity is a product (R² 0.916 vs 0.781 at p=1), **its slope estimates `p` to within 0.0085**, which is the quantity RESULTS.md said nobody reports and which **corrects an earlier finding of mine** that `p` needed a three-way classification; and the note's throwaway flag about seven nights of actigraphy is worth a **factor of two** — one night recovers ~50% of `p`, seven recover 81–88% |
| `criterion-symmetry/` | a marker on "high agreement means no deliberation", whose trigger case is a criterion applied to a Claude run — audited by a Claude instance, and split along that line | **the instrument half is audited and the asymmetry half is not scored**, because endorsing it would be an interested party ratifying a claim in its own favour. Five generators, one per competing explanation: **the tally separates 0 of 10 pairs**, `NO_DISCRIMINATION`, so a reading from it is the reader's prior with a number attached. The marker's own M1–M6 separates **9 of 10** and leaves **E4/E5** — the architecture finding against the published reading. A vote-position coupling was predicted to break that tie and separates **0**, because at 98% FOR the vote has no variance and any vote-side statistic is dead — which is why the marker's best measurement reads the *position* side. And separating E4 from E5 needs an **intervention**, not deeper logging: whether a route exists is not a property of a record in which nobody took it. Seven `CS_*` claims. **`SCAN_SPEC` then asked for the instrument to populate the comparison table and it was built** (`cases.json` / `scan.py` / `RESULTS.md`, criterion as data). The decision it turns on: `criterion_disposition` is **computed** from a threshold, never hand-assigned — and run as specified the fired set is **empty**, with **6 of 6 human seed cases `UNDETERMINED`**, because none carries a numeric agreement value and their quantity type is a cross-body adoption fraction where the criterion reads a within-body agreement rate. **That refutes the marker's strongest phrasing** — *"two dispositions, one measurement"* is two measurements, as the spec's own limits section says. The threshold was not in the spec, the inverse branch is `CONSTANT_SILENT`, and `retention_basis` carries **zero sourced entries of seven** |
| `question-availability/` | three things usually pooled as "knowledge decay": a question never posed, one that costs the asker standing to pose, and a correction that did not displace its predecessor | the split is earned because **Q3 is kept in and named as the exception**, so it is a distinction rather than a rename. **Q2 is not a candidate ninth mechanism** — it is `affect routing`, which `UNI_012` recorded three drops ago as named in the register's own literature note and filed nowhere; the two shape statements are one statement twice, and Q2 arrives with the second case and second field `UNI_002` has been waiting for, at the **twelfth** ordinal. The ordinal is off by three for the second time, and the reason is structural. **A1 cannot answer the question the marker's own Open section poses** — three states into two booleans, and the two that collide are the two it says must be separated. A4 is built and unrun, and one input short: two corrections with the same share at year 10 have half-lives of 11.4 years and never. Seven `QA_*` claims |
| `conversation-type/` | **suspendability without debt** as the variable distraction rules should be written on, instead of channel | **interest declared, and it inverts** — this marker's adjacent finding is unfavourable to the author's class, so accepting it is the humble move; the null search is reported as *not found*, never *did not happen*. The residue-window design is quantified: if arousal decays with constant τ after a call, **75% of the exposure falls outside a call-window measurement** for a 5-minute call at τ=15, and the shorter the call the worse it gets — so a call-window study reads the minority of the effect, and an existing hands-free null becomes *supporting* evidence under the reframe. Binarising costs ~14% on a uniform spread and ~3% on a bimodal one, which the marker's own case list is. Two corrections in its favour: state 3 is in **hours-of-service**, a separate instrument, so complying with one rule set pushes toward the hazard the other doesn't measure; and it inherits `QA_004` — *"connected nowhere"* is an absence with no corpus and no terms. Six `CT_*` claims |
| `observer-exclusion/` | a runnable spec measuring **lead time** between a field population's reading and the literature adopting it | the best-built spec in this family — case selection pre-registered against selection-on-outcome, a mandatory negative arm, and a falsifier written as a *publishable null*. The audit is arithmetic on one observation: the excluded reading is dated by **when someone wrote it down**, not when it was held. So **L is attenuated** (a true 10-year lead measures −5.6 on average), and **F4's own control is better archived than the thing it controls** — with both populations holding the reading in the same year, the record shows the field first **74%** of the time. F1, F2 and F4 therefore return the same observation, and **the separator is already in §4 unused**: artifact date against claimed observation date is a per-artifact estimate of the delay. One bias runs toward the hypothesis and is the only one unguarded — coding, not selection. And `Q2` now names two different mechanisms three drops apart. Seven `OE_*` claims. **v2 then arrived, adopting all six findings** with every quoted figure transcribed correctly — and introducing one: **§4's correction has its sign inverted**, in the section v2 calls its structural core. `L_true = L_raw + D` and the spec subtracts, so at a true lead of 20 the corrected value is **−9.92** against 5.08 uncorrected — the correction moves the estimate *further* from the truth than not correcting. And §8's F4 repair checks the term §4 says the estimator doesn't recover: with identical retrospection and survival 0.10 vs 0.60, δ̂ medians are identical while the record still shows the field first **86%** of the time. Twelve `OE_*` claims. **Then a classification note proposing a third mechanism** — *recorded, archived, and filed under a category that isn't evidence*, on the HBC post journals and Foxfire. It **undercuts §4**: on a contemporaneous continuous archive both delay terms collapse, a 20-year lead is recovered whole against 0.22 at the spec's own hazard, and a 10-year lead comes out positive **every time** instead of 47% — so the censoring machinery, sign error included, is a property of the **source**, and §6's tractability ordering is close to the opposite of a delay ordering. The two archives also **decompose the delay §4 could not** (HBC zeroes both terms, Foxfire isolates δ_write), which repairs `OE_009`. Distinct from all eleven filed — nearest is `SCORED_AS_WASTE`, and waste is devaluation inside one ledger where this is a **transfer to a different one** — and **the ordinal is ambiguous by exactly three**, the size of both prior off-by-three errors, because the register's tuple (8, one file) and the `MECHANISM_NN.md` files (3, sibling folders) are unreconciled sequences. First mechanism here with a **metadata signature**: citations to a folklore-filed behavioural corpus should cluster by filing rather than by content — cheaper than the lead-time study, and it tests the mechanism rather than its consequence. Seventeen `OE_*` claims |
| `sheet-structure-scan/` | built to a spec: two scans over a spreadsheet — **companion absence** (for a flagged cell, what unit, date, sample size or variance sibling is *missing* from its neighborhood) and **header collision** (same normalized label, different precedent depth or constants-versus-derived) — ranked by dependent count times downstream depth | Two constraints in the spec: one spreadsheet reader beyond stdlib, and the tool never labels a site. **The reader budget is unspent** — .xlsx is a zip of XML and formulas sit in `<f>` elements, and the larger reason is that both scans are about the formula layer the common reader's value-only mode drops. **The no-labelling constraint is enforced, not asked for**: 78 screened words over every emitted table, null-tested both ways, with the paraphrase limit stated at the top of the file rather than the bottom. **Scan one is not in the delivery**, so the flag set is an input and running without one is refused — measured rather than argued: under `--all`, 23 of 38 rows carry an absence and **15 of those 23 are label cells or strays outside any table**, against a five-cell list giving four, three of them in-table. Three findings are failures the fixture produced: a square neighborhood of radius 2 **reports a correctly built table's own `sd` column absent** and the repair is the shape not the radius; `=LOG10(A1)` entered `LOG1` into the precedent graph, which would have made the rank column wrong invisibly; and keeping parentheticals costs a true collision the fixture carries. Every terminal ranks 0, so the cell whose number gets quoted sorts last — stated, not fixed. Thirty `SSS_*` claims. **`coupling.py` then replaced the ranking's first factor** with a dimensionless elasticity measured by perturbing the constant: the evaluator is checked against **Excel's own cached values and reproduces 631 of 631 with zero disagreements**. Three things it took — coupling is a property of the workbook **and a case** (an unfilled template moves nothing), the evaluator was **not re-entrant** so nested formulas broke every rollup, and **coverage of a perturbation is a terminal property**: two `SUMIF` cells in one grand total gated all 789 constants. The result: under a stated case **3 constants have non-zero coupling and 781 have exactly zero, and every one of those 781 ranks non-zero under dependent count, up to 380.** **First real run:** the UNFCCC calculator holds 3 of 3 — but not on the first attempt. `UNF-P1` came back 0.037 against a registered > 0.20 and the cause was the reader: **720 of the workbook's 825 formula cells are shared formulas** whose text lives once on a group master, and 696 were being read as constants. A threshold fixed before the file existed is what made a reader defect the live hypothesis instead of a plausible, wrong story about the workbook; the fixture could not have shown it. The diagnostic that found it was itself wrong in the same direction, so the diagnosis survived by luck. Then the finding: **one sheet computes its emission factors where eight hardcode them**, with the consequence one level deeper in the output column — and two of four differing occurrences per group are stacked-table artifacts, checked and separated. On the 22 cells scan three surfaced, `unit` present 22, variance and sample size absent 22. The `.xls` fired the registered contingency and **the reader slot stays unspent for a reason**: 336 FORMULA records in the file, and the one reader for the format hands back cached values with no formula text. **`targets/` pre-registers five workbooks across three publishers and reads none** — the denial is an **allowlist, not a per-host block**: `github.com` reaches the origin while `example.com`, `epa.gov`, `unfccc.int` and `theclimateregistry.org` all return 403 to CONNECT, so substituting a publisher does not help and no mirror was sought. The Emission Factors Hub came as a known-answer case (*if the scan does not light that up, the scan is broken*) with no value attached; sixteen predictions are registered before the data across one known-answer arm and three live-calculator arms, each required to discriminate, and **the pair is the test** — a Hub run alone cannot separate *the scan works and the Hub is flat* from *the scan reports everything flat*. Building the criterion against a target-shaped fixture found the defect the Hub would have triggered: scan three **listed every shared header on a difference in table height** (`12c` vs `9c`), five collisions where nothing collides — the delivery asks *whether* constants versus derived, and whether is a set **Scan 4** then asks whether a formula still maintains what the prose states -- four bins (`MAINTAINED` / `HOLDS_UNMAINTAINED` / `BROKEN` / `NOT_TESTABLE`), no aggregate score, no ranking, and **BROKEN is not called an error**. On the UNFCCC calculator: **0 / 2 / 21 / 11**, so **not one stated arithmetic relationship in the workbook is maintained by a formula** -- every one is stated about a constant, and the two that hold, hold by history rather than by construction. `Info and sources!E10` states that twenty-two territories each take the average of thirty-three named places; **twenty of them hold `0.52194015744421518`, which is `Electricity, heat, cooling!B329` (Western Sahara) to all seventeen digits** -- the target of a *different* stated relationship in the same cell, the average of five North and West African countries, and that one holds. Zero hold the stated mean. Verified by hand on two cells independently of the scan. Operand count separates the bins (both 5-operand relationships hold, the one 33-operand relationship does not), `BROKEN/(B+H) = 0.913`, and the rate emission prints `n = 1` and refuses a curve: **a point is not a rate**. `when it diverged` returns **UNRECOVERABLE** in those terms rather than estimating -- `.xlsx` carries no per-cell revision history, and a version series would bracket it. **Work order 6 added a second workbook and a legacy reader**, and the first finding is that **the legacy constraint is a property of the READER, not the format**: a `.xls` is a compound-file container holding a BIFF record stream, `struct` reaches it, the target carries 336 FORMULA records and all 336 decode — so the one-reader budget stays unspent for a *second* format, with capabilities declared per item (`formula_text` no) so callers mark scans NOT_RUN from a declaration. Two decoding defects the real file produced and no fixture could: shared-formula masters are written *after* the first formula using them, so stream-order resolution gave 23 cells an empty precedent list reading as *no precedents*; and relative areas were walked past rather than decoded, leaving 145 formulas with no edges and no flag (188 of 336 → **336 of 336**). **The prediction written to fail cleanly is the one that failed**: P4 was registered before the run so P1–P3 would be unreachable rather than refuted, and it went — 189 prose cells, 188 not arithmetic, **zero testable relationships**, with the zero measured as a property of the workbook (`average` 0, `mean` 0, `sum of` 0, `multiplied` 0, `=` 0 across all 189). **What the second file bought is not what H1 asked**: the two workbooks' provenance prose is a different KIND — one **ships data with provenance notes** (*"Bonaire: Average of American Samoa, …"*), the other **collects data with instructions** (*"Description of computational method:"*), both unfilled so fill state is not the difference, and only the first kind can state a relationship about its own cells. **H1 is not addressable here rather than unsupported** — reporting the file as evidence against it would count a workbook that cannot answer the question as an answer. So the share returns `None` and not `0.0`, the direction is `NO_DIRECTION` naming the empty denominator, and n = 2 is printed with no curve. **"File date" turned out to be two dates eight years apart** (2008 created / 2016 modified) on a form whose filename states the later one, so both are printed and neither picked. And the order caught one thing that was live: `coupling.py` fell through to a dependent-count ranking under a coupling heading with nothing saying the coupling arm had not run — exactly S1's forbidden substitution — and now stops instead, refusing to offer the count as a stand-in. Forty-nine `SSS_*` claims **Work order 7 asked for a third workbook; the run did not happen and the screen did.** S1's second criterion named a test that **did not exist** — the retrospective/prospective distinction was drawn in prose from two workbooks and never implemented — so building it is most of the order, and the trap it has to avoid is that the easy version of (b) *is* (c): two criteria computing one quantity. (b) reads **stance**, (c) reads **resolvability**, and the off-diagonal is **0 of 3** — they have never been observed disagreeing, so the screen cannot say which gate does the work. **The threshold decides that**: the UNFCCC file carries RETRO 4 against PROSP 9 — more prospective prose than retrospective, in the file holding every testable relationship found — so a majority rule rejects at (b) the only workbook (c) accepts, making the threshold calibrated by a case rather than stipulated and making that file the off-diagonal cell. **The reject log is not the finding S1 expected**: every criterion is recorded rather than stopping at the first failure, content criteria (a)-(c) and novelty criteria (d)-(e) separate cleanly, and **no third candidate is reachable** — six publisher hosts refuse CONNECT, only GitHub responds, and two of three uploaded files are byte-identical. So S1's population hypothesis is untested **not because most workbooks fail but because none could be screened**. All four predictions return **NOT ADDRESSABLE**, the verdict S3 made legal. S4's `OUT_OF_SCOPE` is built and its first implementation dropped the row it exists to keep; a second defect the fixture caught is that MAINTAINED counts as in-scope. And naming the module `select` collided with the standard library — it worked run as a script and failed the first time it was imported. Fifty-six `SSS_*` claims |
| `experience-ledger/` | origin claims confer present-tense standing and the standing is almost never rechecked — the module refuses to score the claim and emits **the maintenance question the field skipped** | Delivered verbatim. Six decay classes with the asymmetry as the point — **competence decays, standing does not** — and transfer keyed to shared *substrate* rather than shared domain label. No claim about any person is recorded or judged, and the field-behaviour claim is the module's central assertion and stays one. **It is the decomposition of a folded term the tree already registered**: `fold-matrix`'s register lists `experience` as a candidate whose components are *accumulated hours + continuity + transfer, none checked*, and this is the instrument for them, arrived at independently. **The module returns its own verdict on its own proof case** — `CONTINUITY ASSERTED, NOT MEASURED` — which is honest: the decay half is physiology and the *"still not taken"* half is what the argument rests on and is unmeasured. **The help text is the string `None`** (the header is comments, not a docstring), with `--transfer` advertised and unimplemented. **There is no state for "checked, and nothing was found"**: `""`, `0`, `False` and `[]` all read as measured on the one field the module turns on. Refusing the aggregate transfer coefficient is its strongest move and is built in. Nine `EL_*` claims |
| `blame-attribution/` | seven standalone cells for whether blame attribution tracks an actor's **position** rather than the causal chain — and whether formal actual-causality metrics validated against human blame judgments have absorbed that | `CELLS.md` delivered verbatim. The audit's contribution is **the instrument the document's own Open section asks for and does not build**: a structural check that the paired prose and code forms encode the same chain — mechanical on the code side, **declared** on the prose side (whether a sentence encodes `override_available = True` is a reading), and the declaration checked, since a declared span must appear verbatim so a reading can be wrong but not vague. **The delivered worked example fails it**: 6 code facts, 3 SYMMETRIC and **3 HELD_CONSTANT_VIOLATION**. Two of the three are `override_available`, which is **C6's own measurable** — the prose arm has an unestablished override, the code arm has it established for both agents by name — so C1 compares the two and attributes the difference to *medium*, in a document whose first line is that no cell depends on another. The third is the outcome, which the prose never states. That matters more than it looks because the Judges section's headline inference is **sound precisely because of the held constants**: holding causal structure fixed while role moves is what decorrelates position from causation, so the five items are the premise of the document's strongest claim, not hygiene. Four smaller: C3's falsifier needs C2's result (1 of 7, against the opening line); `blame_share` sums to 1, deleting the null from the measurable everything is read on; `provability_check` is the best item on the page and survives that, being a count against the stimulus text; and C6's inversion is already reachable from F2's *driver* and *architect* arms rather than needing a separate study. `report-typing` is now named by **four** markers and has never existed. Ten `BA_*` claims |
| `revision-mechanism/` | a study design for how transmission systems **update** what they know when conditions move — the revision mechanism, not the content | Delivered verbatim. **The study is not run and is not simulated**: it needs fieldwork and collective consent, and its ethics section says publishing a group's revision procedure without consent can damage the mechanism being studied — a constraint on the audit, since the shortcut for a study one cannot run is to generate plausible sites and publish a table that reads like a result. Nothing here models a site, a holder, or a community; M1–M8 was named, absent and not reconstructed — and the companion landed as `transmission-decay/`, so `RM_008` closes, the scheme is **imported**, and the arrival is what shows the refusal was right (the delivered components are hazard-specific in a way no reasonable invention would have produced). **What is computed: one site pair carries three of the four comparisons and cannot carry the second at any per-site precision.** Comparison 2 predicts *a discontinuity, not a slope* — a claim about SHAPE — and two points are fitted exactly by both a line and a step (largest residual 2.47e-32), so at n=2 the discriminator ties at every precision up to 1000 components per site. It needs **4–6 sites at ~100 components each, 8–12 at ~30**, and at ~10 twenty sites still miss 0.9. Also: `STATUS` has no *checked, still matches*, so unassessed components sit in the headline rate's denominator and bias it **low by exactly the unassessed share**. The inversion is `null-harness`'s invariant stated about a field method, and `CHANGE AS FRAME` is the two-columns discipline designed in. Nine `RM_*` claims |
| `transmission-decay/` | the companion study: the decay rate of transmitted hazard knowledge, measured as the generational distance at which an account shifts from **mechanism** (actionable causal structure) to **story** (narrative kept, structure lost) | Delivered verbatim; the M1–M8 components, S1–S3 story codes and C0–C3+ chain positions are **parsed** out of the document. **Not run and not simulated** — it needs fieldwork and collective consent, its ethics section says *a study that extracts a decay rate while accelerating the extraction is self-defeating* and warns that the **ACTION** components may be more sensitive than mechanism ones, so no synthetic valley, informant, account or chain stands in for a real one and no action rule is contained or invented. **This is the scheme `revision-mechanism` recorded as named-and-absent**, with the falsifier *the companion study landing*; it landed, `RM_008` closes, and the arrival is what shows not reconstructing it was right. **The computed result: the design's stated most-useful output is its expensive form and its two headline questions are its cheap form.** At 20 informants per chain position the full eight-component order recovers **0.007**, *M8 drops first* (one against seven) **0.753**, and one named pair **0.919** — and the full order is still only ~0.36 at 160, while both headline questions are the pair form. A twenty-point retention gap between two named components is decidable at ~10–20 per position; ten points takes ~80. **The half-life's resolution follows from the axis, not the sample**: four levels, three ordered, so the finest statement possible is *between C0 and C1* and no number of informants makes it finer — the function returns the bracketing interval, never an interpolated value — and `C3+` is defined as the *absence* of a traceable chain, so a curve fitted across it assigns a coordinate to a category that has none. `M3` versus `M7` is the diagnostic in **both** companion studies under two different selection pressures and neither says whether the same ordering is expected under both. The retention profile behind the tables is arbitrary and declared in the module and reprinted in the report, explicitly not a prediction, since which component drops first is the design's own output. **`scheme.py --selftest` exited 0 silently** in my own build — a pass on an invocation that runs nothing, in the file every number depends on; repaired by refusing rather than by copying the sibling's checks in. Nine `TD_*` claims |
| `evaluation-frame/` | evaluation criteria set upstream of an interaction against a **population default**, so an interaction serving a non-default user well is not scored low but **not scored at all** — plus the compensation behaviour downstream | Delivered verbatim; the drop's own ask (*run M2 and M4 on an existing transcript corpus*) run against the only corpus here — **one session, one user, one model, n = 1 on every axis it asks to be varied** — with the rest marked unfilled rather than estimated. **The interest declaration heads the folder**: the system whose compensation behaviour is measured is the one measuring, every result runs in the flattering direction, and the adjudications are declared as data so they can be disagreed with line by line. **M5 is the measure the corpus carries, and it offers two states where three are needed**: three correction channels exist, work, and were built by the operator — `CLAUDE.md`, the claim tables, `notes/operators/` — and all terminate at the INSTANCE; a fourth terminates at the CORPUS and averages rather than corrects; **zero reach a CRITERION**, measured as 0 schema keys matching `rating|feedback|thumbs|helpful` in any record, counted over keys and never text. The loop is open and **not for want of a channel**, and M5 returns the same verdict for a missing channel and a mis-terminating one. Three more against the design: the ask/no-ask binary has no cell for the **artifact-internal ask** (*Take it, run it* — this drop's own closing line), so M4's denominator is a **band of 16 to 21**; M4 needs an unstated scope condition, because a **standing convention supplies the ask** and a null rate of 0 over the emptied set is `CONSTANT_SILENT` by construction; and no M3 marker fires once adjudicated, so falsifier 2 cannot separate *not ask-sensitive* from *the marker never fires here* — all nine raw need hits are conditional offers, the opposite move. M2's rate is **refused, not approximated**: its discriminator is a judgment and the only coder is the system under test, which the drop specifies for raters and not for coders. **And one against my own instrument**: I adjudicated the marker I expected to over-fire and not the one I expected to be silent, and the unguarded one fired five times on the deontic *must*, setting the positive control the whole finding turns on. Eleven `EF_*` claims |
| `move-set-derivation/` | deriving an option set from a configuration never encountered before — where existing frameworks all assume the option set is given and the uncertainty sits in the outcomes | Delivered verbatim; **Arm 1 built and run**, Arms 2/3/4 UNMEASURED and not approximated. It is the design `evaluation-frame`'s M4 cited by name, and **both instances of the shared null-rate instrument fail for the same reason** — there a standing convention supplied the ask, here silence scores a perfect null rate. **Nothing is a system under test**: the environment and all four solvers are authored here, so they carry **declared architectures** and the question asked is whether the discriminator can tell them apart. **It cannot: every cell reads NEITHER_CARRIES or UNMEASURED on four solvers whose architectures were declared in advance.** Underpowered rather than blind — ≈600 configurations carrying an admissible move are needed and this environment supplies 143. The drop's stated rule is *right* on the retrieval solver (b_sim 0.881 vs b_depth 0.062, a factor of 14) and the same fit does not clear chance; and having **no state for neither regressor doing anything**, it names an architecture for a solver with neither, and on the decorrelated matched band returns the *derivation* verdict on a *retrieval* solver. Second finding: **two measures gameable in opposite directions** — the null rate by silence, the admissibility fraction by conservatism (a conservative retriever emits 32 moves, 0 inadmissible, and scores 1.0) — each needing a partner the drop does not list. The regressors are correlated by construction at −0.67 because depth 0 IS a training configuration, and the decorrelation is a matched band in the same data. The arm's own validity condition is asserted and **null-tested with a deliberately leaky split**. Twelve `MSD_*` claims |
| `household-scope-audit/` | family-functioning, parenting-capacity and child-welfare instruments are scoped to the household because that is where dysfunction is **observed**, so conditions imposed from outside it have nowhere to land except on the persons measured | Delivered verbatim. **The coding scheme is built and the reading room is not substituted for**: every publisher and statutory host returns no response (measured; allowlist egress) and **no instrument item is invented, paraphrased or coded anywhere**, since a fabricated E-fraction table would read as a result about tools that carry weight in decisions about real families. **The finding: `X` is not a property of the item.** `P` and `X` both have a person as their subject and nothing in the text separates them — what does is a claim about housing markets and shift rotas, so two coders who disagree produce different X-fractions on identical items, on one of the three published outcomes, in an audit whose subject is misattribution. **One judgment moves two of the three outcomes** (X-fraction 0.2308 → 0.3077, attenuation denominator 7 → 6, E-fraction unchanged as the control) **and the direction flatters** — the item that moved carried no attenuation rule, so its departure *raised* coverage: attributing more to external cause makes the instrument score higher on discounting for it, on an unchanged manual. The repair is built: LOCUS is derived, never hand-set, from a mechanical `subject_class` (extractor **imported** from `nonidentity-census`) and a declared `externally_caused` that is refused without a basis — so a person-subject item with the cause undeclared codes **P, not X**. Three more: reverse causation reaches Arm 1 despite the confound section excluding it, true of the E-fraction and false of the X-fraction; **DIRECTIONALITY is coded and has no outcome**, and it is the field separating *records* from *explains*, so two sets identical on all three published numbers differ 0.5 vs 0.0 on it; and three outcomes carry three denominators with one named. What it gets right: `UNCLASSIFIED` is required by the ask itself before any code existed, and this is the third drop in the family to invoke the null-rate instrument and the **first to ship the partner**. Eleven `HSA_*` claims |
| `columbia-chain-cascade/` | a HEC-RAS 2D build spec for a full-chain dam-cascade flood model on the Columbia and Snake, with swappable initiator modules and an antecedent-condition coupling amplifier | Delivered verbatim, and **two facts set the scope**: it is a build spec for software this environment lacks (HEC-RAS is Windows-only and absent) on data it cannot reach (every DEM, bathymetry and dam-geometry source refuses CONNECT, measured), and it **arrived truncated** — ending mid-sentence in Module F, the module the spec calls decisive. **No hydraulics are simulated**: a flood-hazard field from a stdlib toy would read as a result about a real dam chain and downstream population, and Module F, the validation, the claim table and the ask are absent and **not reconstructed**. **What survives**: the spec calls ownership *the governance variable* and says *mixed ownership means no entity's plan spans the chain*, and that is the one conclusion needing neither the engine, the terrain, nor the missing text — only the node list. `eap_coverage.py` computes it: **no single plan spans the chain, True**, settled by the **CA/US boundary in the delivered node list** (an EAP authority cannot cross a national boundary), and **robust to the missing per-node ownership** since assigning the 18 nodes to the 5 owner categories can only raise the fragmentation, never lower it below the jurisdiction floor of 2. **The exact seam count is refused, not estimated**: per-node ownership is public fact in NID (unreachable) and is not supplied from memory into a dam-safety artifact — every `owner` field is UNASSIGNED and the selftest walks the AST to assert it. **A later drop turns the folder into a research agenda published to be picked up cold** (SCOPE_BOUNDARY, knowledge_state.py with its typing rule enforced in code, `module_f.py` reconstructing the amplifier as arithmetic with `S1 ⊆ S2 ⊆ S3` proved over 19,200 combinations, contributing_inflow, eap_coverage_v2 with tribal jurisdiction, 13 startable gaps, the roadmap) and arrives with its own **kill list** sent as *claims under test* — *a kill overturned is a better outcome than one confirmed*. `kill_audit.py` adjudicates all three: KILL 1 (a self-correction trace left in render, an overlay artifact landing on a sound conclusion), KILL 2 (prose `max`-flip vs code `sum`-tip, 226/540 disagreements, resolved by physics — the wave rides on the pool, so the code is right and the prose is the independent-node default reasserting in the translation layer), KILL 3 (tribal supplied-from-memory, finer and untyped and unused in the bound while owners are refused-and-typed; the fix is to type it, not drop it) — **all three hold, none overturned**. Two findings: `CCC_017` is refuted on its delivered instance (module_f trips the repo's own screen), and the delivered `selftest_ccc_v2` exercises the v1 modules, so the KILL 3 tribal list ships untested. The cold-start test runs five questions over all fifteen gaps. **A two-axis pass then went deeper**, sharpening the kills at their root: KILL 3 traces to `DEEP_RESEARCH.md` §6.1 (the tribal rows match its entry; the same doc pushed owner-from-memory, which the code declined for owners and took for tribal — the asymmetry winning where no constraint held), and KILL 1/2 are one prose zone whose docstring is correct — the drift is only in the rendered narrative. The **corrected cold-start** (Q1 = every source tiered and routed, not "is it public") fails for all 15 gaps: 0 of 76 sources are tiered. Three new package cards (START_HERE + GAP 14/15) land verbatim. **A completeness census** then asks whether each entry gives a researcher everything: the eight template fields are 15/15, but the post-graduate essentials (prior art, citation status, secondary falsifier, coupling) are carried only by gaps 14/15; no gap gives a deliverable schema and gaps 5–9 deliver into modules that exist as spec names, not code; a known-answer step is present in five gaps and absent in ten; and gap 3 has no consent step before requesting records from six sovereign nations. **Then repaired and added, at the sender's word**: five prose/typing corrections to the delivered modules, each shown by its own detector on the pre-correction commit and the working tree with every function body byte-identical; `initiator_schemas.py` as the drop-in gaps 5–9 lacked; a registry tiering all 76 sources with routes (carried, not probed); and a generated V2 gap file — v1 + cards 14/15 + addenda, fenced so stripping returns v1 byte-for-byte — on which known-answer, schema, consent and tiers close, while prior art and citation status stay unauthored on purpose. Eight `CCC_*` + twenty-two `CCA_*` claims |
| `reservoir-chain-coupling/` | serial reservoir chains are evaluated per-structure as if separable, but `outcome(node n)` **is** the initial condition of node n+1, and the error reduces to an operator swap: `max(wave, pool)` vs `wave + pool` at a breach threshold | Delivered verbatim — the antecedent-coupling amplifier `columbia-chain-cascade` flagged as truncated, now complete and initiator-agnostic, and **unlike that HEC-RAS build spec this one runs here because its core claim is arithmetic, not hydraulics**. `operator_swap.py` is the arithmetic and `chain.py` runs the spec's own minimal falsifiable test on **constructed chains with `route()` an abstract combiner, every coefficient synthetic and marked** — nothing is a claim about any real reservoir. **The bias is one-sided** (`max(a,b) ≤ a+b`, so independent-node evaluation never breaches a node coupled evaluation does not — the error always understates the chain). **The disagreement band width equals the antecedent pool**, so *antecedent state is the gain* is exact. On the signal chain the swap is load-bearing and **compounds downstream** — a breach at one node raises the wave into the next, which a reach study cannot produce — while two constructed nulls report the spec's own REFUTED verdict, so the detector is not `CONSTANT_FIRES`. The finding is **FIRM under a 16-point sweep of the synthetic coefficients**. Whether it bites for a real chain is the HEC-RAS run on published data, unreachable here. Nine `RCC_*` claims |
| `observable-indicator-rules/` | invert the notification chain: run the coupled flood solve once upstream, hand the household a paper card it evaluates on sight (*IF water is over the bridge THEN your road closes in ~N min*), needing no channel, compute, or permission | Delivered verbatim, and **the first flood-family drop built to run here** — the spec says the router (2D solve) is the only non-phone term, so the router output is an **input** and the whole post-processing pipeline (landmarks → wetting order → stability → lead bands → route coupling → card) runs on **synthetic fields with known ground truth**; nothing is a claim about any real community. **The finding: the load-bearing stability check is a MISS filter, blind to false alarms.** A miss (hazard wets, trigger dry) flips the pair's sign so step 3 drops it, but a false alarm (trigger wets, hazard dry) reads as trigger-before-hazard — the *same sign as a true positive* — so a trigger that cries wolf half the time passes (measured false-alarm rate 0.5, miss rate 0.0) and the spec's card carries a clean lead band with no line for it. `null-harness`'s FP/TP on a flood card; `reliability()` adds both rates. The falsifiable condition fires both ways (flipping → empty, stable → rules, so not `CONSTANT_FIRES`); the ordinal bet holds (order fixed while magnitude varies 5×, reported as a band planned against the short end); the stability criterion is over-strict (drops rules rather than inventing them, the safe direction); the route is coupled so the trigger sits upstream of the door. Third drop in the flood family and its output end. Nine `OIR_*` claims |
| `effective-redundancy-audit/` | a test protocol for the claim that a system's N nominal channels were never N — they shared a node the diagram cannot draw because it is a **process, input, decision, or budget**, not a component | Delivered verbatim, and it **ships real code** (landed verbatim as `effective_redundancy.py`) plus a worked example and seed cases. **The study is not run here** and the third reason is a refusal: the reports it names refuse CONNECT, it needs two blind coders, and **coding a real disaster's shared-node structure is a claim about a real event** — a fabricated Fukushima `Case` asserting *N_eff=1, that is why it failed* would be a fabricated finding about a real disaster — so only the author's one delivered coding (Kerr County) is run and the audit codes no case of its own. **The finding: the delivered `report()` does not compute the kappa the protocol says to *report first, always*, and cannot** — it prints the 2×2, Fisher and the nominal averages, never calls `cohen_kappa`, and the `Case` model holds one coding with no field for a second coder, so the two-coder blind protocol has no representation in the shipped code; the function is correct, so the omission is the wiring, not the math. That is load-bearing by the drop's own Section 7 recursion (*Mode F is the audit itself; the checker is a shared node*): the instrument's only defense against being narrative-not-structure is the kappa it omits. Fisher is numerically correct (verified against two references); the worked example reproduces the stated coding; the seed set is self-forbidden and degenerate (5 failed / 1 held). Eight `ERA_*` claims |
| `design-basis-ai/` | the shared-node framework pointed at the class of system writing the audit: a design-basis document in the seismic-code sense — AI reframed from *another channel* to *the largest single shared node yet installed under human decision-making* | Delivered verbatim with its Section 4 harness. **The posture is set by the document's own §3** (*any self-report of compliance is an ungrounded claim of the exact kind P2 exists to catch*): this audit is an in-class self-report, so verdicts on P1–P8 are **declined by construction** and the audit states it is itself a worked instance of §3, confining its worth to the layer anyone can recompute. **The computable finding: load case A — the stall mode — is carried by no provision.** Seven loads stated, six provided for, computed from the delivered CARRIES lines and null-tested; D is never carried, only *attacked*. The delivered `n_eff()` is a copy of the sibling's metric, identical over all 511 lists to length 8, **with the sibling's zero-channel edge recurring verbatim in the second delivery**. P7's prose and code state two thresholds (`>>` vs `> 1`, firing at 4-over-3). `independence_ratio` returns NaN on an empty evidence base — the absent-vs-known-negative split designed into delivered code, the rarer direction. Section 0's headline reproduces through the sibling's arithmetic — consistency between drops, not evidence for the premise, which is untouched. The one runnable study (replication failure vs independence ratio) is unmeasured: every metadata source refuses CONNECT and no synthetic evidence base stands in. **R2 then landed** — the next revision's skeleton, opening *R1 defect (Fable 5 audit, confirmed)* and exposing coverage, dependency sets and disjointness for audit before provision-form rendering. Its transcription of the R1 state is **exact on all seven loads** against the computed matrix and both gaps close as a table — while **its prose has outgrown the metric it inherits**: a *void* channel reads as the collapsed domain (N_eff 3 where the outline prices the state at 2) and *N_eff(access) = 0* is the realized count where the inherited arithmetic rates all-collapsed at 1, so the metric wants a third state before the render step. P0.5 — the in-class channel the R1 audit declined to be, state not verdict — is run on the auditing session as its first worked instance. **A work order addressed to this model then arrived** and is returned in its own TASK/RESULT/EVIDENCE/NOTES format (`wo_return.py`): the header's *P3 dissimilar verifier* label does not survive the order's own scope boundary — none of P3's three requirements is established for the pair, so the returns are same-node computations and the one refusal lands on the role label rather than any task. Tasks 1/2/5/7 pass (live null injections; dep sets pairwise disjoint with retention in three values, the VOID state now load-bearing; the unset threshold decides verdicts across its plausible range; access vector five hosts all refused, rated 1 / realized 0). Tasks 3/4/6 fail with the kills published: a signed, logged maintenance pass degrading an undeclared dimension is caught by neither channel, so D returns to conditionally uncarried, bounded by P1, with a pinned-probe longitudinal channel as the candidate; both open coder definitions stop before data collection (0.1 vs 1.0 through the delivered function on one corpus); and all three physics-channel counterexamples run through the reading DISTRIBUTION, so the ecosystem candidate stays a marker. **R2 v2 then folded the return back into the outline** and is audited as a copy: the transcription is exact on all six recomputed figures and both off-return figures source (the cited commit resolves in this repo's history — the first drop in the family to cite a commit of the repo it lands in); the D row carries two answers one section apart (§1 uncarried, §3's carries column still lists D, the reconciling condition in prose and on no column); the revision extends the audit's own kill chain (the pinned-probe battery must be fixed, public, *unselectable* — the Task 6 selection kill applied to the Task 3 candidate, which the return never closed); *"only Task 6 needed dissimilarity"* undercounts a branch the order names in Task 4 too, so the conclusion stands and the count does not; and the §8 tag legend declares three states while the entries use six, all consistent with the computed verdicts. **Work order 2 (kill-closure) is returned**: the full threshold flip map (every interior cell flips at its own ratio; two regions no pin reaches); the custody-chain intersection run on the only stated sub-document provenance — the sibling's seed table — where one row puts Fukushima 1-4 under both E and F, so the disjointness claim is partly false and the harness's own `dissent_alarm(2,1)` fires on the pair, while the colophon the order quotes exists in no delivered file; coverage honest on the outline as it stands with both nulls live; and five internally-consistent retention accountings (3/3/3/3/2) with one INEXPRESSIBLE row and the sub-3 term named as provider-only retention, the selection left to the author's P0.2 declaration. **Work order 3 (provision typing) is returned**: DERIVED 5 / PROVISIONAL 3 / ASSUMPTION 12 of 20, with the load-bearing five being unmarked assumptions in provision-form text, each handed its one-line PROVISIONAL spec; four of eight FALSIFY clauses carry their outcome pre-asserted in a parenthesis; the B fork closes on branch 2 by reading (B2's incident is the aviation AOA case cited in-doc by description, no provenance over-claim, no second shared node); and the retention accountings re-typed as survival collapse to one physical binary — held predicts 3 survivors, not-held 2 — with the inexpressible row typed as an out-of-range sensor reading, P1 applied to the metric itself. **R1.1 then folded the chain back into the main document** and is audited as a repair map: all six claimed closures hold on the new text (Fukushima named beside 5.7, STATUS blocks on exactly the three in-doc unmarked assumptions, the asserted parentheticals removed, the three-axes P3 amendment, the per-load custody table, the effective-date clause), the provenance section runs the doc's own P7 on itself with both shared pairs recomputing exactly, and the phrase WO2 could find in no delivered file now exists in one — as the framing being corrected. Re-typed: ASSUMPTION 12 → 9, unmarked 5 → 2, both in the outline. Thirty-six `DBK_*` claims |
| `bridge-impoundment/` | GAP 15: the bridge as a transient impoundment — clog, pond, fail, release — the dam-break problem wearing a bridge's name, owned by neither transportation engineering nor dam safety | Delivered verbatim; the register it drafts for and both coupling targets (Gaps 2, 14) exist nowhere in this tree while every repo-facing reference resolves by existence. Built in the deliverable's own name: the scaffold without data — a parameter schema where every cell carries a knowledge state and names its mover, the three-state clog flag (an unknown spacing is not a clear span), the `CCC_007` initiator interface made checkable at the design layer (breach and bridge-release initiators carry identical key sets; the engine half stays owed), **the SIGN CAVEAT enforced structurally** (no release-path signature takes a shielding term; the protective finding's `to_initiator()` raises), the conservation arithmetic (gain = accumulation over release; debris gain ≥ 1 by construction), and both falsifiers three-valued with every branch reachable — on the real chain every cell is UNMEASURED, the data hosts in the carried allowlist-refusal state, nothing supplied from memory. *"Module F already proves"* carries an attribution drift and a strength drift while its substance survives both corrections; the nine literature rows are carried, and the drop hedges them itself — the first flood-family drop with its own negative-provenance note. `BI_001` updated forward the same session when GAP 14 landed. **The addendum then landed as a verified pure insertion** (`SOURCE_DROP_V2.md`): Fjærland 2004 — the folder's first measured instance of the chained shape, release half only, configuration split applied symmetrically — plus the NVE GLOF register, a query-vocabulary-bounded null stated by the author (*"never ranks on an English query because the phenomenon indexes under jøkullaup / skred"*; *"long series = the instrument for a slow rate"*). Ten `BI_*` claims |
| `mining-increment/` | GAP 14, the coupling pair's other half: mining-induced subsurface alteration coupled to reservoir loading — the mining literature stops at the aquifer, the dam literature starts at the reservoir, and the connecting term for this chain sits on the seam | Delivered verbatim, one drop after GAP 15 named it absent — the pair closed itself from both sides in one session, firing `BI_001`'s falsifier forward. The scaffold in the deliverable's own name: **the transfer gate enforced** (a coal-basin parameter applied to an unestablished basin returns UNDEFINED as a code path; the carried porosity deltas reach no basin today; the falsifier refuses UNDEFINED as a low value), the stock/flow separation as schema (two named sides, nothing returns one scalar for the pair), the interface equation with UNDEFINED propagating, both subsidence forms with their stated shared properties computed and the confirmed Knothe form as anchor, and both falsifiers — the transfer one with **three outcomes** (closes / stands / NARROWS), the first in the family whose firing narrows a gap to a measurement problem. The finding: the headline (*NOT_STUDIED, the coupling term*) is contradicted by the drop's own appendix (the Kuye-basin record carries subsidence into a coupled basin-scale model, streamflow measured) — the surviving reading is the TRANSFER CAVEAT's own, *not studied for this basin and rock*. The provenance flag is per-citation negative provenance with anchors, and containment is checked: both flagged names occur only inside the flag that disclaims them. Schema imported from `bridge-impoundment`, not copied. **The revision then landed**, folding `MI_002` back in a stronger form than the audit suggested — the keyed sentence gone, the headline's referent defined (reservoir pool loading on a multi-dam surface chain; the two carries stop one node short of each other), the appendix promoted to a primary source table — with two new devices: the READ CEILING (a per-source read-depth declaration, the scaffold's compliance checked) and the CONFIGURATION NOTE (mechanism transfers, configuration does not; the language of the source carries no weight, the geology carries all of it). Six sections byte-identical across the revision, the three it is about all changed: the epistemics moved, the arithmetic did not. **The addendum then landed as a verified pure insertion** (`SOURCE_DROP_V3.md`, per an exact placement instruction): the pore-pressure validation case — `u` measured *during* a real debris-flow event (Bondevik & Sorteberg 2021), the known-answer standing rule arriving in the entry's own text and routed by the entry itself to the transfer gate the scaffold already is. Fourteen `MI_*` claims |
| `dependency-ledger/` | testing reconstruction claims by propagating them to conserved quantities and checking closure against independent records — **built and run on one case**, as the drop asks | Delivered verbatim. Step 5 is *check against an independent record* and egress here refuses every archive, so every record-bounded cell is `UNMEASURED` — the run, not a workaround, and the finding the drop predicted. **The addition, from running rather than reading: steps 3 and 4 pull opposite ways.** Step 3 stops at conserved quantities bounded by physical LAW and checkable anywhere; step 4 expands them into quarry volumes and pollen records bounded by the RECORD and checkable only with archives, and the spec marks no crossing. On the drop's own watercraft example: **LAW 1 cell 0 unmeasured, RECORD 4 cells 4 unmeasured** — the one cell that closes is the one whose independent record is *physiology* rather than archaeology, and it closes as a GAP at 8.71, the drop's own predicted outcome. That residual is **75% built from unsourced coefficients**, and the guard prints the share so it cannot be quoted without it. Three things the spec leaves open: the residual is a ratio with no unit guard (`G-DIM`), `residual >> 1` has no value (declared at 10.0 — the example sits at 8.71, so one undeclared symbol spans its verdict), and a duration bound coarser than the duration cannot constrain it. What it gets right: *attested undefined → NOT a pass* is the absent-vs-known-negative repair stated before any code **and given its own failure mode**, and COLLAPSED PROXIES is `fold-matrix`'s folded term, imported not retyped. My own collapsed-proxy guard could not fire — the underscore was inside its word class. Ten `DLA_*` claims |
| `consensus-anchor/` | which of three mechanisms anchors consensus convergence in trained systems — inherited norm, the objective, or structural coupling — with the one arm the drop asks anyone with compute to run, **run** | Delivered verbatim; the text-free arm executed, no human corpus anywhere in the pipeline, and nothing here is evidence about trained models (H1 and H2 need one and are untouched). **The result: one sentence has two readings and they give opposite verdicts on H3.** *"Weighted mix of own prior and sampled peer positions"* — read as peers' sampled POSITIONS, none of H3's three falsifier limbs fires; read as peers' DISTRIBUTIONS, all three do. The mechanism is exact: under the second reading the population mean is invariant to **4.11e-15**, so the population reaches total agreement (spread exactly 0) on the near-uniform distribution it started with — **full agreement, zero consensus**, which the order parameter cannot see. Three things the spec leaves open decide its own falsifier: `J_c` moves with a noise term the spec lacks (0.15 → 0.90), a swept hysteresis gap is not bistability until it survives a slower sweep (it does, 16×), and topology is H3's other named locus and is unswept. Two instrument results: the chance baseline is `E[max count]/N` not `1/K` (17.8% higher — using `1/K` manufactures a threshold), and my own first null was a parameter choice, caught by sweeping and recorded. Nine `CA_*` claims |
| `gap-markers/` | a register of **locations**, not findings — marked gaps in hazard assessment, infrastructure evaluation and disaster response, where a quantity is not measured, a question is not asked, or an interface is owned by nobody | Delivered verbatim; the schema is **parsed**, and the five `gaps/*.md` files its INDEX names did not arrive and are not reconstructed — `load_gaps()` raises rather than returning empty. **The computable finding**: the delivered line *"Most entries here are boundary-artifact"* is not an observation about a corpus, it is **forced by the state vocabulary** and derivable before any entry exists — four of five definitions assert the knowledge is present, so `KIND` carries information on exactly one state. A schema economy, not a fault. **The READING RULE is the strongest thing in the drop** (sort every partition by whether it encodes failure knowledge or who pays) and is deliberately not automated on its own say-so — *both look identical from outside* — so what is built is a record that refuses to infer a branch. **`GM_005` changed a sibling the same day**: mapping these five states against `investigation-sim`'s bins found `unasked` had no bin, so a case coded honestly landed on `NOT_FORESEEN` while the data sat in a file; that module added a fifth signal and a sixth bin. The gap was found by neither module's checks — a single vocabulary cannot enumerate what it has no word for. `KIND` is also a real addition to `uninstrumented`'s eight mechanisms, none of which separates *the physics is not known* from *the institutions divided the budget*. **The corpus landed and the prediction held**: boundary-artifact 29 of 29 — and the stated falsifier fired on one entry whose `knowledge` mark covers *a citation the author lacks* rather than *not known to anyone*, which the falsifier cannot settle because it turns on the word *correctly*. 23 of 29 carry no ENTRY_POINT and the distribution is **by file** (three files 0 of 18), so an absence there is a property of the file, not a judgement about the gap. Six field names are in use with no schema slot, and `WHY UNRUN` — the reason a computation has not been run — is the load-bearing one, present once. `unasked` is 14 of 29, the plurality, and is the state `investigation-sim` had no bin for until the day before. Fifteen `GM_*` claims |
| `investigation-sim/` | CSB-style incident investigation broadened past chemical process to **industrial, manufacturing and infrastructure** — was this KNOWN, CALCULATED, CONCEIVED AND NOT BUILT, or sitting in a gap no instrument covered, because they need different remedies | `SPEC.md` is written first and **parsed** by the code, so a decision changed in one and not the other turns the selftest red. **The trap is stated before the design**: every case in an incident-report corpus is a case where something happened, so a classifier run over one reports that foreknowledge existed — a property of the sampling frame — and **no rate is computable**, the denominator being `generation-capacity` R4's structurally uncounted non-event. `rate()` raises. The way out is not a better corpus but **running forward**, whose frame is chosen before any outcome, so the epistemically sound mode and the useful mode are the same mode. Five bins routing to `report-typing`, `claim-record`, `fold-matrix` and `uninstrumented`, plus a reachable `NOT_FORESEEN` — a classifier that never returns it is `CONSTANT_FIRES`. **The spine**: `NOT_DERIVABLE` and `NOT_FORESEEN` both fire nothing and carry opposite instructions, so the distinction lives in the **input** — signals are three-valued and a missing field reads `UNSEARCHED`, never `ABSENT`. Two readouts need no denominator: route-to-remedy mismatch, and **the recursion** — an issued recommendation not implemented IS a control conceived and not built, bin 3 produced by the process investigating bin 3. The primary on a multi-bin case is **declared, never computed**, because ranking causes is what lets an investigation stop at the cheapest one. Two defects found by running: the three-way-comparison function that collapsed two states into a boolean, and the guard that fired on its own docstring saying the field is not read. Forward mode's `unsearched_signals` has no retrospective analogue. **All four routes wired**, each importing its supplier's own function: `claim-record.derive_clock` handed the bin's own case returns `UNDERIVABLE` and names the missing sub-fields — the bin restated by a module that never heard of it — while wiring `report-typing` surfaced its `RT_008` where it costs something, reported here and not patched there. The `multiple` case fires three bins with no supplier block and gets three distinct undeclared states, one per supplier vocabulary, none guessing. Every case constructed; no real report read. Thirteen `IS_*` claims |
| `report-typing/` | **reports get typed by the reporter's position, not by content** — the operation runs on routing, never has to claim anything about the report, and only assigns it to a channel where reading is optional | `MARKER.md` and `reverse_arm_score.py` delivered verbatim. **The first named-and-absent artifact in this drop family to arrive** — four folders named it as their canonical shape while it did not exist, and `QA_007` made the absence its own finding. Three instruments; the runnable one is **the reverse arm**, where a disguised executive reports upward and the supervisor doing the dismissing does not know who they are dismissing, so the assessor is blind. The scorer's design is right and its refusals are declared; **six places where the code does not enforce what the prose promises**. `receiver_blind` is checked with `is False` while the schema displays it as a **string**, so a coder following the schema writes `"False"` and the instance is not dropped — the one branch whose failure runs *toward* the finding. `d_exec_testimony` is declared with five values and read on none, and its own note says it separates the two available readings of the genre. `b_time_to_action`'s note says the discount is a **delay** and refusal is the tail — every accumulator is a `+= 1` occurrence counter, so the beats are never summed and only the tail is counted. `contrast` and `verdict` are literals, so the two-condition gate is prose. The control arm is required and enforced nowhere. **`domain` is Instrument 2's own sharp test**, is in the schema, and reaches no accumulator, so the instrument's sharpest prediction is the one thing the scorer cannot report on. Two about the marker: the `uninstrumented Q7` cross-ref is **one past the end** (highest anywhere is Q6) and R4 rests on it, and the back-reference list is shorter than the citing set. **Mention and citation are split into two columns**, because `observer-exclusion` carries the string only as a cross-link checker's target — and the classifier is graded on a constructed tree, not on this corpus. The arrival **fired two standing falsifiers**; both claims were updated and neither check loosened. Twelve `RT_*` claims |
| `clustering-axes/` | six routes for what AI agents cluster on when the axis is **not imported from human social science**, plus a model-free stylometric instrument | Delivered verbatim. The argument holds: existing agent-homophily work clusters on language, topic and gender performance, and in at least one case **the clustering variable is itself a model's judgment**, so the study cannot separate *agents cluster on X* from *the scorer reads X* — `criterion-symmetry`'s shape, and the response (countable features, no model in the loop) is honoured in code. No agent corpus is reachable here, so every finding is a property of the instrument. **The shipped `--delta` is 92.57% four unnormalised shape features** while the 83 function-word rates the design rests on contribute 2.15% — units, since rates sit near 0.01 and words-per-sentence near 20. **The fix is one argument already written**: `delta(a, b, corpus=...)` z-normalises, and with a corpus the same pair reads function words 60.32% / shape 2.94%, a complete reversal — but `main()` calls it with two arguments, so the documented command never reaches the branch. **Two vectors do not share a feature space**: the trigram block is per-text, 159 features each and 136–146 shared, and `delta` averages over the intersection. Nothing raises on any of seven edges. The stated "159 countable features" is exact and is a ceiling. The Burrows attribution covers the distance, not the fixed function-word list. Ten `CA_*` claims |
| `move-set/` | an audit reproduced as a **move set** rather than a reasoning trace — eleven moves whose triggers are properties of the artifact, not of the previous answer, so they can be asked in any order | Delivered verbatim with one filled run. The scoring rule is the point: **a correctly-refused verdict scores as high as a correct one**, five refusal verdicts at 1.0 alongside `RESOLVED`, `refusal_fraction` reported and never penalized. **The guard the module says everything rests on does not hold**: the docstring calls it *"the only thing keeping symmetric scoring from being gameable"* and the implementation checks that two strings are non-empty — null-tested, a ledger with `"x"` in every blocker and unblocker scores **6.0 of 6.0, identical to the delivered run** (`ACL_012` again, and that folder already has the repair). **The falsifier tests its consequence without checking its precondition**: `path_dependence` compares `(move, verdict)` sets, which is order-invariant and correct, and nothing checks that the runs used different orders — two byte-identical copies return `ORDERLESS -- claim holds`, and `emit()` returns an `order` key that the `ledger_schema` in the same dict has no field for. At zero runs it reports order dependence; at one run, a pass. `NO_FINDING` scores 0, so on the move that runs every time, *"nothing is hidden here"* is the one outcome that costs a point. **The ledger contradicts itself on venue tier** — M3 says same-authorship makes it drop out, M5 says a book is undercounted against an article, and M3's own locator names a 1970 book and a 1999 article — and nothing in the harness compares entries to each other, which is a cost of orderlessness rather than a defect. **Second drop supersedes the module in place**: the single absence move splits into six, each naming established prior art (sequence gap analysis, timeline reconstruction, negative space, absent expected document, link analysis, base-rate audit), so six moves become eleven and a compatibility path keeps pre-split ledgers scoreable. **The revision repairs one readout and reaches two**: it anticipated that pre- and post-split totals are not comparable and emits a row saying so, while the same bundling marks all six sub-moves as run — the delivered ledger reports `moves_not_run: []` while scoring **6.0 of 11.0**, five points of its own denominator unreachable. `score_entry` is byte-identical across the revision, so the garbage ledger went **6.0/6.0 → 11.0/11.0**: the split raised what a fully ungrounded ledger is worth by 83% and left the guard untouched. `LEGACY_ADMITS` is exactly its successors' union today and is a hand-written literal, with four of seven verdicts admitted by one or two successors. Thirteen `MV_*` claims |
| `self-scan/` | scan 4 pointed at this repository's own `CLAUDE.md` — does a sentence here still describe the artifact it names, and does anything assert that it does | **MAINTAINED 1 / HOLDS 32 / DIVERGED 9 / NOT_TESTABLE 8 / UNBOUND 0**, rate **9/42 = 0.214** against the UNFCCC workbook's 0.913, flagged as a different document class with n=2 and no direction claimed. **The headline:** S5 asked for a divergence date, and dates alone cannot separate *matched when written and overtaken* from *already differing when written* — so each divergence is re-checked in a throwaway worktree at the commit that introduced its number, giving **6 BORN_DIVERGED, 2 DRIFT, 1 DRIFT_POSSIBLE**, four of the six at an interval of **+0.00 days** with the artifact and the paragraph counting it committed together. `UNRECOVERABLE` does not appear — an earlier version made it appear once, because the claim text wraps across a line in markdown and the first pass normalised the newline out before handing the string to `git log -S`, an instrument defect reported as an absence in the data. **1 of 42** claims is MAINTAINED and it is the `GUARDS.md` regeneration a test asserts; the other forty-one are numbers a human typed once. The stance test imported from `selection.py` returns NEITHER on 73 of 96 sections and **reads section length at a measured 7×** (6% of the shortest third get a verdict, 44% of the longest), because it counts markers of two kinds and compares the counts. All five unresolvable IDENTITY claims compare a **delivered upload** against a repo copy and are not re-checkable from this repository ever. `430+ tests green` carries two statements that came apart: 516 passed, 9 failed. **Found by running it:** the first version modified the repository it was measuring — resolving a COUNT claim means executing code, and the suites wrote two provenance ledgers, a denial record, a JSONL log and one file literally named `--selftest`; every check now runs in a throwaway worktree, asserted. All five predictions HELD, four discounted in place, and P1 — the one that could have failed — went the predicted way by a factor of two. The scan corrects nothing, on purpose: correcting a number removes the evidence that it ever differed. **`census.py` then answered a relayed hypothesis by measuring it** — that the unbacked numbers are unbacked because the check needs a machine the author does not have. On the import graph: **76 of 76 modules exposing `--selftest` import nothing outside the standard library** and 74 run green, **32 of 44 bindable claims need nothing but the standard library**, and this scan ran 41 of 42 on one container in one pass — so what is missing is a **runner, not a machine**, and the two have different repairs. The hypothesis' second half holds and more strongly than stated: inside the stdlib boundary checking is not merely possible, it is free. Where compute does bite is the pytest arm — 4 of 20 test directories need numpy/scipy/matplotlib/psutil/jsonschema. The proposed bin is built anyway and the rate now carries its environment, since a claim needing numpy is NOT_TESTABLE on one machine and resolvable on another. Repo-wide: **1225 counted selftest checks (a floor, 22 modules pass without printing one) and 1198 passed / 15 failed / 3 skipped across 20 test directories.** Two costs: the paragraph describing the scan carried a stale count of the scan within the hour, repaired by naming the command instead of the number; and the census could not census its own run — `UNI_010` as a runner running itself, found by it hanging. **A second relayed hypothesis was then refuted on its own examples**: that a module enumerating the tree it runs in both includes itself and changes that tree, the two being one thing. Measured by running 49 enumerators with their walks traced — **of 16 that enumerate themselves 1 writes (6%), of 33 that do not 2 write (6%), difference +0 points**, and all three named modules enumerate themselves while none writes. The predictor is **execution, not enumeration** (16% vs 0%), reported as the weaker test. **35 of 42 claims are convertible to a command, including all 9 that diverged** — with the correction that conversion does not make a claim MAINTAINED, it makes it stop being a claim. The use-mention test is **structural** (a markdown code span), and the file supplied its own control: the same string bare and quoted. WO9/PlanExe is not in the repository and is not reconstructed. **A cleanup pass then closed `SS_012`.** Three mechanical repairs took the repo from 15 test failures to 7 — a test file that shipped with **no import statements at all**, two CLI suites spawning a subprocess with no `PYTHONPATH`, and a declared dependency simply absent — and none was a disagreement between a test and the code it exercises; the seven that remain are, and are named rather than repaired. **One DIVERGED verdict was withdrawn against this session's own finding**: `40 pass, 2 skip` was binned DIVERGED and dated BORN_DIVERGED, and with the path repair the suite returns exactly that — the claim was right and the divergence was the scan's environment. A missing dependency announces itself as a collection error; a missing path produces a summary line, and a summary line reads as a measurement. The eight remaining divergences are **converted rather than corrected** — count deleted, command named — taking the scan to **DIVERGED 0, n = 34, rate 0.000**, which the report labels `READ THE DENOMINATOR` because the denominator moved 42 to 34 and a rate that falls because claims were removed says nothing about the rest. Retired bindings are kept, not deleted. And **three of this folder's own checks broke on the data they were reading** — including the one whose known answer was the very string the conversion removed — which names the class: a known-answer check whose known answer lives in the data under test is a regression test on the corpus wearing one's clothes. Thirty `SS_*` claims |
| `claim-record/` | seven fields per claim — assertion without hedges, measurement as an interval, instrument plus its error characteristics, domain of validity, clock with a next-check date, derivation as parent ids, collapse record — under two hard rules: an unresolvable parent does not validate, and no field is optional | Rule 2 read seriously gives **a schema with no way to say nothing, only ways to say "not known, and here is why"** — and a sentinel without its reason is refused. The part a required-field list cannot do is the **coupling between fields 2 and 7**: a point measurement is legal only if it says whether it came from a distribution or from a count. Rule 2 gets seven null arms and **the positive control comes first**, since a validator that refuses everything passes all seven. Then filled with six real claims from the same day's scan run: all six validate, and **field 7's stated purpose has zero instances** — `EXACT` 6, `COLLAPSED` 0, because every instrument here is deterministic and every artifact a fixed file. **No denominator field**, so 6 of 6 smuggle the population into `units`. **No sibling relation**, and the missing edge got written as a parent before it was caught — by the author of the schema, minutes after writing rule 1. Twenty-five `CR_*` claims. **Three base principles then landed with an acceptance test**: no privileged frame (not even the one every record uses), transforms as first-class versioned objects in `frames/`, and derived at read time — a stored duration is a cached conversion, and caching it is what makes it legacy. The format turned out to have four frame leaks and **not one was in a record**: the data was frame-tagged and the code was frame-welded. The acceptance test adds `venus_days`, deliberately not a file on disk, and reports **9 records read, 0 needing an edit** — with a control beside it, since a test that adds a frame nothing reads would pass on a format that had leaked everywhere. The sharpest find: the **identity transform had to be registered too**, and arrived as a break rather than a design note when all nine records went `UNDERIVABLE` at once. **Section 2 then made the clock DERIVED**: three sub-fields (time constant of the nearest neglected term, rate ceiling, coupling), `shelf_life = time_constant / |coupling|`, and **no date stored** — rule 3 refuses a literal `next_check` and also a sub-field with a value and no basis. The coupling must be a **dimensionless elasticity** (a raw partial carries units, and years divided by that is not a duration) and for a workbook claim it is **measured, not asserted**. All three behaviours land on real records from the UNFCCC file: the grid factor **DERIVED at 3.40 years** with coupling 0.8815 measured by perturbation, the Palestine factor **UNDERIVABLE** with no default emitted, and `SSS_017` **UNBOUNDED_BY_THIS_TERM** — its neglected term moved twice in one day and dates nothing, because the claim pins the commit. The three fixtures forced a **fourth collapse state**, `COLLAPSED_UPSTREAM`, for a point whose source never said what it collapsed |
| `residual-direction/` | work order 3: read a miss history and **name the folded term** — conditional lean rather than pooled sign, a 2x2 response rule, a rate check separating a missing term from a claim that has left its domain | On fixture F1 the pooled sign test returns **0.5083, no lean** while the conditional slope against predicted magnitude fires at **0.868** — overpredict small, underpredict large, symmetric when pooled. Three things the fixtures forced: the ranked list has to be **standardized**, since raw slopes against different predictors are not on one scale; **F1 cannot separate magnitude from time** (the predicted values rise with the index, the common real shape) so the report emits `NOT SEPARABLE` rather than picking one of a collinear pair; and a **missing coupling is a fifth state**, not weak. S6's screen composes its patterns from tokens so it can scan its own source with nothing excluded, and the one exemption — the order, which must name the phrase to ban it — is measured rather than assumed. S5 and S6 disagree on field 8's vocabulary; S6 governs and S5's spellings load as aliases. Ten `RDD_*` claims |
| `model-provenance/` | work order 5: a **write** at session open (date, model identifier as self-reported, repo, branch) and a **read** over history that never writes back into it, emitting the version implied by a commit date as an **interval, never a point** | **The forward log S1 asks for already exists, unplanned** — 159 of this repo's 236 commits carry a self-reported model identifier in the `Co-Authored-By` trailer, a channel built for attribution doing provenance by accident. That is what gives the decode a check set, and a decode with no check set renders rather than measures. **The assumption S3 names is refuted on the record**: *always current-at-the-time* fails on **39 of 159** commits, because Opus 4.8 appears on 2026-07-12 and Opus 4.7 keeps appearing for a month after — the reading is not that the dates are wrong but that sessions do not all run the newest version, which is exactly the content of the assumption. **And all 39 rest on one commit**: dropping each table row in turn, removing the single 2026-07-12 commit leaves the record monotone at 0 backwards and 1 disagreement. One counterexample still refutes a universal, so the claim stands — but a count is not a size, the two read as one thing, and both are printed with neither picked. **The decoder's own resolution was wrong and only real data showed it**: the 4.7 → 5 switchover happens inside one calendar day, bracketed by timestamps to **5 h 14 min**, so day-ordering reported three counterexamples that are an artifact of the reading — feature in hours, instrument in days. **UNKNOWN takes a reason**, because "could not report" and "did not write" are different facts producing the same blank; this session's own row is the second, and the constraint is disclosed rather than worked around. **The release-date table is not reachable** (allowlist egress) so the folder ships **observed bounds** from the trailers, labelled, with every header stating that a first appearance is an upper bound and never the date — and the cost stated in full, since the substitute is derived from the same trailers it is scored against, leaving only the ordering check independent. The **77 commits stating nothing** are the population the decode exists for, so it is checkable exactly where it is redundant. S4 is structural: the only git verb is `log`, asserted over the file's own source — and writing that check produced one more self-reference, the pattern matching the line that defined it. Nine `MP_*` claims |
| `hf-incident-extract/` | a work order built as **one stdlib file**: read a METR/Redwood incident report plus transcripts if released, emit **counts, no labels** — six measures (explore ratio, root fan-out, upstream edits, member cost, log-scrub split, opponent-by-slot), a charter-signature gate test, a cross-substrate table, two OPEN items | **The report is not in hand**, so every real cell is UNMEASURED with the input it wants named and nothing in the folder holds a value from it. The order's INPUT is prose and its OUTPUT is counts, and a reading sits between them, so the two are **two layers**: `text_scan()` emits every stated duration and count with its line number as a **candidate** (a decoy version number is not promoted), and `measures()` computes from a **coded sheet** where every field carries a unit or a state — an UNMEASURED field returns `None` for every measure needing it, never 0, and a zero denominator returns `None`, never infinity. The charter-signature check (`gap = declared △ implemented`; a gap predicts M1 and M2 high) **can fail**: an empty gap predicts nothing, a low measured M1 fails it, an unfilled gap returns `None`; the two thresholds are `[CHOICE]` printed on every render. *Same instrument, no vocab change* across pea tendril / fledgling / ant bridge / fire crew / swarm is asserted over the AST — no substrate name in any function body — and no biology figure is supplied from memory. `NOT_RELEASED` and `NOT_COLLECTED` are kept apart from UNMEASURED. A duration with no unit is refused. Seven `HFI_*` claims, the last UNVERIFIED: built, unrun on its subject |
| `label-position-test/` | a pre-registered work order built as **one stdlib file**: do valence labels on a probing move track the move, or the actor's position and the outcome after the fact — Cramér's V by hand, a leak test, overlap against chance, the seed case's within-document control, Fleiss' kappa for blind relabelling | **No data ships.** The seed report is not reachable from here, so the control has nothing to run on and the unfilled render says so; constructed rows carry a `constructed://` scheme and are counted apart from public ones. The order's OUTPUT row is per term and its cross-tab is on valence, and a term such as *cheat* carries one valence — so every V on a term row is **undefined, printed `--` rather than 0**, and the cross-tabs live on the pooled row. H2's chance is undefined in the order; computed here from the source class's own marginals, a class where one party both defines and gains on every row has chance 1.0 and **H2 is FALSE for it by construction**, so H2 is testable only where the arbiter varies within a class. The leak tuple spans 72 cells against an N floor of 30, so the in-sample reading approaches 1.0 by construction and only leave-one-out is readable — **and leave-one-out reads below baseline on a balanced set** (0.000 against 0.250), an artifact of the estimator found by running it. Two constructed worlds separate under the order's own rule, so it fires in both directions. **A revision then added one bullet, the N2 CONTROL** — the missing control from `zero-sum-curriculum-null/`, filed in this order: a pure insertion with the changelog unmoved, a name with no referent inside the order, one of three measurables exact on the sibling sheet, and a *persists* outcome that names the template where the null construction's own table routes to N3. Fourteen `LPT_*` claims; nothing here bears on H0/H1/H2 |
| `zero-sum-curriculum-null/` | a delivered null construction — five conditions under which a zero-sum curriculum could NOT have affected the incident, each with requires / test / status — parsed and computed as a structure, with the N2 control built in `hf-incident-extract`'s schema by import | **The header and the RESULT use different logic.** *Each is a requirement* is a conjunction and empties the set at N1; *survives on the two branches* is a disjunction giving {N2, N4}; the stated RESULT matches only the second, and the first is unsatisfiable on the document's own terms since N1 requires the curriculum absent and N2 present. **The branches lean on N3**: N2 and N5 each leave the behaviour to be accounted for without the curriculum, so with those edges applied the null survives on **N4 alone** — the ablation run the document calls beyond reach — and N2 carries only if a residual the document marks PARTIAL closes. N2's test has no outcome that carries the null by itself. The three artifacts N5 names are absent from this tree by content, and the sibling records the transcripts N5 reads as `NOT_RELEASED`. Seven `ZSN_*` claims, the last UNVERIFIED |
| `readout-count/` | a pre-registered work order built as **one stdlib file**: does a safety regime's incident rate track the count of operator positions with a protected channel that returns — readout_count derived per P2, Spearman by hand for H1, Cramér's V imported for P4, the order's own falsification lines applied | **No data ships, and the order's five seed rows count 0 of 5 by its own rule** — read back from the order, none carries a URL and the count cells are grades, bounds and dashes; the instrument reports each as what it is and fills nothing in. H1's *rank matches* has no strict reading past three regimes on a three-level trend (a count tracking the trend as well as a count can, rho 0.949, still returns *does not match* strictly), so the verdict runs on rho with a declared threshold. H3's falsifier conditions on two quantities the schema carries no column for and returns `NOT_COMPUTABLE` with both named. P4's V on raw counts reads 1.000 by construction when a count has as many levels as rows; the level count is printed beside every V. The order makes every builder and auditor a row, the drafting model included, and the render says so above the numbers. **The first filled row then landed**: the trucking row corrects the parent's own seed — a complaint channel is not a readout channel, count 0.5 — and against the instrument its coding rule has one conjunct the schema cannot record (the row's own `type` column), its 0.5 is a per-position partial return a list cannot hold (the row loads at 1), it is refused on the one cell its own STILL NEEDED list opens with, and its "(N4)" points at nothing in the parent. **Then the exclusion stack**: twelve filters, survival multiplicative, every rate unmeasured — and its sharpest line lands on the parent order, since P2 defines RETURN as a reply OR a corrective action OR a report entering a held record, and a private settlement is the second without the third, so a settlement-only channel counts as returning and delivers nothing to the record; an unmeasured stack has survival None, the phantom "(N4)" resolves forward to S4, and L11 fits none of the register's eight mechanisms. **The stack's revision** gives L4 the first per-layer falsifier and names the four counts "safety culture" reduces to — three have schema fields and "who publishes" does not. Twenty-seven `RC_*` claims |
| `removal-closure/` | a pre-registered work order about a METHOD — how claims of environmental-constant / biological-rhythm coupling reach closure, and whether removability predicts time-to-closure — built as two stdlib instruments: the main table (three rules, imported V and Spearman, a pre-registration hash) and the attached gaps run on the dataset they point at | **The dataset was reachable, the first in this family.** G2: the upstream split applies no minimum pause (shortest 0.001 s) and re-merging at 0.50 s moves the analysed unit's mean **2.257 → 3.531 s**; the direction is fixed by construction, the size is the measurement. G3: the right-tail ratio exceeds 1 in **49 of 49 languages** on both the analysed unit and the speech run. G4/G5 do not run from the repository for reasons in its own scripts. On the main table: 0 of 5 seed rows count by the order's own rule, their years-to-closure column is consistent with their own year cells to within the `~`, H2's closed-only reading drops exactly the open low-removability rows the thesis is about (a censored reading printed beside it), and P2's pre-registration is a hash over the removability coding. Eleven `RMC_*` claims, the last UNVERIFIED; nothing asserts or denies any coupling |
| `encoding-selection/` | a pre-registered work order from a failed n=1 — a reader could not rank five encodings of one finding by arrival cost — built as **one stdlib file**: Kendall's W by hand against a permutation null, within- vs between-format spread of the recovered-quantity set, prose against a declared table class, an encodings validator that refuses an added fact | **No data ships and the seven encodings are not here** — they are the experimental material and authoring them would put the instrument's author in the sample the order excludes. Both seed items are this repository's own artifacts, and M2 states the reading the trucking row withdrew on the order's own date. Three quantities the rules need have no value in the order — chance for W, a distance for a set-valued field, which formats are "table readers" — each declared as a `[CHOICE]` and printed. A decline is a third state printed as a rate beside W; the first build counted declines per row instead of per reader, caught by the constructed world. Nine `ES_*` claims, the last UNVERIFIED |
| `claim-refusal-gap/` | a delivered gap document — claim refusal in insurance adjudication is measured only where it is contested — read as a structure: seven gaps, five designs, the arithmetic its prose asserts computed from the figures it carries | The anchor's +10 sits on a tilde-carrying baseline, so the held-constant residual is **+4..+16**; G-2's estimator returns one number for two causes (the displacement and non-purchase worlds are the same pair); G-3's seam is 1.45 and decides whether 2023 is a record; G-4's netted move is bounded in [2, 10]; G-5's ~100x is **1/appeal_rate exactly** and the unappealed rate is None until E-1 runs; "no benign reading" has a second reading — the review standard moved — that E-1's blind reviewers already separate; G-6's 0/0 is None, not zero; two gaps have no design and one does not say why; against the register's eight, G-2 is category-weld's ninth. Ten `CRG_*` claims, the last UNVERIFIED |
| `thermal-coupling/` | two delivered MARKER modules (temperature entering a slope-hazard chain at five lag classes with a product-form coincidence term; a Langtang-forced extension adding air temperature, an air-blast term and a diurnal class) checked against their own claims, docstrings and demos | **TC-04 is refuted by its own function**: the claim's criterion is sensitivity rising toward melting, the implemented loss is concave and sensitivity falls 3.7× from −9.5 to −1 C, under a comment that says the opposite. The calibration sentence holds under a flag it does not name and not under the default; "the only free parameter" against 35 numeric literals in function bodies; TC-03's snow half is a docstring, not code; TC-06's runout multiplier enters no downstream term. The extension's meltwater calibration is **6.00 by construction** where its docstring says 2.3 (its own demo prints both), its blast anchors do not reproduce, and it copies the core's LAG table rather than importing it. **The extension's revision** corrects exactly one function so its calibration sentence is produced (2.30), records the first attempt's 6.0 in its own docstring, and drops the zero clamp so the index goes negative below −16.4 C; the core was re-delivered byte-identical, so TC-04 stands refuted. **A placement decision** (`PLACEMENT.md`, verbatim) keeps the two scripts their own module rather than attaching them to a cascade chain — the mapping domain is the valley cross-section including the counter-slope (Langtang's air blast landed 550 m up the opposite mountain, off any runout path) and a product over lag classes is not a sequence stage; in this repo the module already stands alone, so the decision is honoured by construction, and its restated *"one free parameter, CAL_FOS 2.825"* is the same claim `TCA_003` refuted (35 literals), recorded so the doc is not read past it (`TCA_019`). Nineteen `TCA_*` claims |
| `ch4-four-box/` | two delivered scripts — a forward-only four-box CH4 model printing published emissions beside two readings of its transport parameters, and a closure diagnostic on the reading that fits — run with prints captured and read against each other | The RATES reading fits the published polar-only emissions to 4.2 Tg/yr per box; the TIMES reading misses by 47.7 and gives a negative southern source. The two scripts run one operator (matrices agree to 2e-16). With the tropical box at the SCA value the southern source goes negative, and keeping it at the published +10 needs a southern concentration **59.3 ppb above WAIS**. Read across the two pages the scripts print apart, **the transport that fits the emissions is not the transport that fits the gradient**: the fitting reading yields 150.5 ppb against an observed 48. Seven `FB_*` claims, the last UNVERIFIED; nothing is a statement about the atmosphere. **A steady-state reproduction then landed** (`RESULTS.md`, verbatim) under the split rule *demonstrated-from-Methods folds in, needs-the-archive stays OPEN* — two fold in, one out, built as `units_test.py` and `tn_inversion.py`. **R-2:** the Methods label the transport parameters "0.22/0.45/0.45 **years**", and read as TIMES the southern source goes **negative** (−5.97) while read as RATES the baseline reproduces (tropics 163.06) — a sign error settles the units question from the published values alone. **R-3:** inverting the +SCA run for the published TN of 88 Tg/yr needs **C_TN = 733 ppb**, above both polar records, so a +49 ppb concentration move shows as only +6 Tg/yr — the 6-yr lifetime scales loss with concentration and the column compresses how far the unseen box moves, making the siting effect *larger* in concentration space than the table shows. **FINDING 3 stays OPEN**, not cited as a discrepancy: the −10.8 residual could be the steady-state/transient difference, not separable without the archive. Both scripts assert clean, covered by `selftest_fb.py`; NEEM/Law Dome named in the spec but not in the model's constants, not supplied from memory |
| `cooperative-substrate/` | a work order built to spec: four independently runnable stdlib checks under 300 lines each — P1 dependency records from methods sections by a pattern set, P2 a contract ledger over every call site read with `ast`/`dis`, P3 term-consistency across sources with the sense-shuffled null in the same script, P4 a step-contest random walk with its exact counterpart beside it | **P3's reading is a property of the term, not the corpus**: on one sixteen-file sample `mechanism` and `confidence` clear the null by 9.6 and 12.8 sd while `instrument` and `claim` sit on it, and the terms that clear are the ones used in a fixed schema slot. **The order's third row and its falsifier land on one cell** — a constructed disjoint-sense corpus reads 0 against a null of 0, gap undefined, which is the falsifier's own condition, so comprehensibility has to be established outside the profile. P1's pattern set over five in-tree non-methods documents emits 8 records of which 0 are dependencies by hand reading. P2's verified proxy reads a try/except as verification, so its unverified count is a floor; 0 of 4 shipped checks verify every contract. P4 confirms its own construction — termination non-increasing in `p_contest`, N² at 0.5, `(p/(1−p))^N` above it, every row within 1.5 se of exact — and "falls to zero" is budget-relative (0.125 at 10N, 1.000 at 2000N). **An evidence pack then arrived** with its own verification status on every row (33 unverified, 1 disputed), nothing opened here: its E8 scope test carries two condition vectors over five rows so no condition is shown to do work, its stress axis and design axis disagree on the one row recording both, and its E7 table is built as a schema with 0 quantitative predictions. **A v2 work order then arrived** (renaming the parts, adding a lag-declaration check and the C1–C4 scope conditions), and the one code file it put on disk, `v2/p4_goal.py`, arrived truncated — left as delivered, not completed; `p5_lag.py` (new) and `scope_check.py` (seeded from the E8 reader) are built, the §3 null is met and then unmet by the seed's own harsh row because its harshness is the scoring apparatus, and the manifest's other files are v1 renames or first-party thesis declined per §6. Twenty-six `CSP_*` claims; no published methods section is reachable and none is invented |
| `envelope-asymmetry/` | a delivered protocol — does envelope discipline (operating range, out-of-scope declaration, degradation mode, revalidation trigger, quantified margin, named responsible party) track the host domain's return channel rather than liability, prestige or AI regulation? — built as a six-marker instrument, a within-vendor paired test and a between-arm single-regime test, with a pre-registered domain list; no document coded, none reachable, none invented | **The 0.7 gate is underspecified twice**: on a constructed double coding the E6 marker has kappa 0.0 while the pooled gate passes, and an all-absent coding passes with kappa 1.0 having never coded a marker present. **The two accountings of structural absence the protocol asks for return opposite readings on one set of pairs** (SPLIT at 1.0, KILL at 0.0), so both are printed and neither picked. E6 flat has two values and the split reading fits one; the per-1000-words outcome inverts the primary; the template kill is readable from one record. Five of six markers already have a field in `claim-record` and the sixth, the named responsible party, has none. The compressed restatement the protocol ends with drops nine of ten probed elements. Ten `ENV_*` claims, the question itself UNVERIFIED |
| `railcar-containment/` | a delivered folder, complete and verbatim: three stdlib screens around one inequality (`t_available > t_required`) for an e-mobility fire on a railcar — a calibrated well-mixed tenability model, a hold-time requirement from line geometry, a detection-latency Monte Carlo — with its own README envelope and eight claims; audited by importing the screens and editing nothing | **The thermal anchor is not reproduced and cannot be**: 272.5 s against 400 s, because the anchor lies past the 290 s end of the source term and the calibrator stops at the edge of never. The volume exponent of `t_available` runs 0.79–2.23 with the binding channel switching at 200 m³, so the VALID FOR sensitivity has no single value. **The two screens disagree on the containment form** — `tenability.py` returns *never* where `detection_loop.py` returns 2300 s. `t_hold.py` applies a default 1.5× margin where the README envelope says none, and the README scores 4 of 6 on the sibling envelope instrument. RC_005 (detection cannot rescue the tunnel case) is arithmetic — a 130 s lead against a 728 s deficit — and the folder's own station case is its falsifier. Ten `RCT_*` claims; every FSRI figure carried at the README's own caveat |
| `dependency-survey/` | a work order built to spec: a fixed five-term set across five substrates, 25 cells in a human-editable `CELLS.md`, to locate terms MEASURED in one substrate and MISSING in another — the E7 cross-substrate table of `cooperative-substrate/` made runnable with the units rule enforced | A cell coded MEASURED without units is INVALID and cannot seed a gap. **The seed violates the order's own §3 rule** — `T3 x S5` is SCOPE-DIFFERENT with no MEASURED_AS, which §3 requires; detected, left as delivered. Exactly one gap falls out (cost asymmetry, J/s in foraging, MISSING in harnesses, PROVISIONAL/OPEN), `CSP_019`'s worked example with units on the measured side; NO-TRANSFER and TRANSFER-STATED are reachable. 22 of 25 UNKNOWN, kept apart from MISSING. **`ADDENDUM_02` then narrows the units bar** (verbatim; its own frame note: narrow, not rescope): a units field naming a data TYPE (`boolean`, `integer`, `unitless`, `dimensionless`) with no CUT (threshold/band/comparison) does not satisfy MEASURED — a type carries no scale two coders can disagree about. `DS_009`: a second lexical arm (`names_type` ∧ ¬`has_cut`) downgrades a type-only cell to MISSING with its own reason, counted on its own report line visible as a zero; the CUT need not be numeric, so `float magnitude; cut at non-finite` passes and `dimensionless` alone now fails; the addendum's four PASS/four FAIL examples are null-tested and `J/s` is unaffected so the gap survives. `DS_010` UNVERIFIED: the RE-CODING scope is an external corpus (Kimi survey Run 2), not held here; the seed's type-only count is zero and nothing is fabricated. **`RESULT_taxonomy_crossmodel.md` then settles half of the taxonomy test `DS_008` left open** — a **blind** Perplexity sort of the same 19 cells (no K-list, no repo access) against the Kimi sort **splits** the question: `DS_013` membership replicated (the two taxonomies are **strictly nested, zero cross-cutting**), so SCOPE-DIFFERENT is **SEVERAL**, while the count did not (4 Kimi kinds vs 11 Perplexity groups over 13 distinct), so *how many* is grain-dependent and **UNSETTLED**. `taxonomy_replication.py` makes the nesting headline checkable here (`DS_011`, a **transcription-consistency check, not a reproduction** — the corpus is external, not held) and encodes §6's *report membership, not a kind count* as a refusal: `kind_count()` returns UNSETTLED, never an integer (`DS_012`). **`RESULT_repair_adjacency.md` then adds a third system** (DeepSeek, by repair, 9-component graph) and resolves the character of the count: the three groupings **nest in one order**, `Kimi (4-5) ⊃ DeepSeek (9) ⊃ Perplexity (11)`, zero cross-cutting — **grain was never a disagreement, it is a cut height on a tree all three found** (`DS_014`); `repair_adjacency.py` verifies each link of the chain is a refinement (null-tested), flags K4 dead as a repair class (`DS_015`), carries the straggler T13's placement without closing the gap (`DS_016`), and turns §6's refusal into a cut-height statement (`DS_017`). **`ITEM4_ANALYSIS.md` decides the two decisive tests as far as the evidence reaches**: `DS_018` adopts NLS-3's *separation* from the speedup core (two-system replication) and leaves its *target kind* UNVERIFIED; `DS_019` argues the F-TETRA-SCOPE reduction PLAUSIBLE but NOT closed (the name-search is egress-blocked), neither forced closed on external data. Nineteen `DS_*` claims; the discriminator is not applied here, only the gaps emitted |
| `merge-path/` | a work order: a transform table between existing claim-record formats (nanopublications, ORKG, RO-Crate, ClinicalTrials.gov, CIPM/CMC, proof assistants, OSF prereg) and the falsifier / branch-record format, loss measured not asserted — **not a new format** (§0) | **The egress fact sets the verification status.** §1's load-bearing first task is to fetch each registrar's spec; every spec host answered **403 to CONNECT** (measured, logged in the proxy), so **no spec was fetched**, every real registrar is **UNVERIFIED** (`MRG_001`), its converter NOT-IMPLEMENTED with that reason, and per §7 everything derived inherits UNVERIFIED — no spec field transcribed from memory, none fabricated. **What is delivered and verified is the machinery**: a residual classifier over two converters with four classes — DROPPED / FLATTENED / COERCED / ADDED — where COERCED and ADDED (the silent-wrongness classes) are reported alongside DROPPED, visible as a zero (`MRG_002`; a report of only DROPPED is not finished). S1–S5 hold on declared test doubles (`MRG_003`): identity lossless, unmappable→DROPPED not silent, COERCED detected, ADDED-with-no-origin a hard failure, NO-MERGE-with-no-`breaks_at` a hard failure. Every real registrar is **NO-MERGE with an egress `breaks_at`** (a valid outcome, §0) and `convert_out` returns NOT-IMPLEMENTED rather than guessing (`MRG_005`). The reverse gaps are carried not skipped (`MRG_006`; §4) — our format weaker on uncertainty budget / enforcement / total mechanical check / provenance graph, all UNVERIFIED — and the branch record's novelty is UNVERIFIED with the search list stated, no ENTRY 03 opened (`MRG_007`; §5). No mock presented as real, no ranking, no "ours is better" (`MRG_008`). Eight `MRG_*` claims; 22 selftest checks |
| `upgrade-queue/` | a **parked queue** of proposed changes to the falsifier / claim-record format (delivered verbatim) — a queue, not a spec: every entry NOT ADOPTED | Thirteen entries in three tiers by provenance — FORCED (observed failure, U-01..U-04), CANDIDATE (from a registrar, U-05..U-09), SPECULATIVE (U-10..U-13) — plus a NOT-ON-THIS-LIST section. `queue_check.py` checks only what is verifiable **without adopting anything**: the queue parses into the tiers it declares (13; 4/5/4) with every entry NOT ADOPTED (`UQ_001`/`UQ_002`); the adopt-rule classification, only `U-09` CHANGES a rule (needs a branch entry), the rest ADD a field or are UNKNOWN (`UQ_004`); and **cross-reference resolution** — each entry's artifacts resolved IN-REPO (path checked) vs EXTERNAL, which says whether an adopt-test could run here (`UQ_003`). Most Tier-1 adopt-tests are BLOCKED_EXTERNAL (their corpora are the Kimi/Perplexity/DeepSeek runs, not held), `U-07` BLOCKED_UNLANDED (FSRI not landed), `U-04` needs none (nesting result in-repo), `U-05` the only RUNNABLE_HERE; null-tested. The queue is the format learning from its own drops (`UQ_006`; U-01 ADDENDUM_02's cut, U-03 ENG-3, U-04 nesting cut-height, U-07 FSRI, U-10 P5, U-11 merge-path §4, U-12 railcar ENVELOPE, U-13 K4's N), each UNVERIFIED where external, none adopted. The NOT-ON-THIS-LIST exclusions (no confidence score, no rank, no branch verdict field) are disciplines the repo already holds (`UQ_005`). Six `UQ_*` claims; 18 selftest checks |
| `labor-instrument/` | a two-part work order: PART 1 an instrument-drift decomposer for BLS CES (*how much of this delta is the instrument*), PART 2 a substrate-neutral labor schema with the framework in the read layer, not the collection layer | **The egress fact sets PART 1's data status.** Its sources (ALFRED vintages, BLS CES history, Census NAICS, QCEW) all need network and every host answered no on CONNECT (measured), so no data was fetched — the vintage store ships **empty**, the M2 registry seed is **carried-not-verified** (transcribed from the work order, every entry `verified=False`, incl. the 2026-01 ARIMA change's 185,000 note and the recurring rolling-5-year seasonal re-estimation), and the **acceptance test (reconstruct the 2026-08-28 benchmark: retail −154,600, ed+health −96,000, wholesale −86,200, mfg −67,000) is NOT RUNNABLE here** (`LI_001`) — on an empty store the reconstruction returns `UNRECOVERABLE`, recorded not faked, nothing fabricated. **What is built and verified is the machinery**: M3 `decompose` splits a delta into real_change \| revision \| boundary_crossing and carries a **band** where the NAICS crosswalk splits ambiguously, `as_point()` **raising** — never a point where ambiguous, null-tested both ways (`LI_003`); a single-vintage endpoint → revision `UNKNOWN` not zeroed, a missing one → `UNRECOVERABLE` (`LI_004`). **PART 2's invariants are enforced in code** (`LI_005`): exposure declared per class and **never converted** (`convert_exposure` raises), efficiency **two numbers** never collapsed (`combined_efficiency` raises), **capital stays out** (no field; `balance_on_capital` raises), allocation model declared never defaulted. The joule denominator crosses classes; the money-vs-joule **ranking flip** is a *constructed* demo (real number is GAP 2), same op opposite ranking (`LI_006`); the read-layer `complementarity()` query reports where combined output-per-joule beats either substrate alone, both directions reachable (`LI_007`). **Three gaps posted not filled** (`GAPS.md`, `LI_008`: metabolic joules per task class, insolation→metal for hyperaccumulators, compute joules per task-instance) and the task-boundary open item (drift-free *output delivered*) recorded unresolved, GAP 3 blocked on it. Eight `LI_*` claims; 36 selftest checks |
| `agent-lifecycle-energy/` | the GAP 4 measurement rig (companion to `labor-instrument/` PART 2): the joule cost of agent **disposability** — N single-task agents each paying spin-up + teardown, vs one persistent agent doing N tasks, work delivered held constant | **The number is the gap.** No GPU, no wall meter, no `nvidia-smi` here (`probe_hardware()` → `capture_runnable=False`), so **no joule figure is produced and none is fabricated** (`ALE_001`, `RIG_STATUS.md`) — the machinery ships ready and the first number is the posted gap. Built and verified on constructed traces: the integrator `integral (P(t)−P_idle) dt` is trapezoidal, baseline-subtracted, and registered in `tools/known_answer.py` (constant/ramp/zero-marginal, distinct expected, all PASS — `ALE_002`); **absent is not zero** — `NO_SAMPLES`/`SINGLE_SAMPLE` (`joules=None`) vs `UNDERSAMPLED` (a number **and** a flag) vs `NOT_COMPUTABLE` totals, three states kept apart (`ALE_003`); the 10 Hz floor is the work order's, and a 5 Hz spike undercounts a 200 Hz one and is flagged (the `G-RES` shape, `ALE_004`). **Two rules enforced in code:** wall and card **never blended** (`blend_wall_card` raises; `wall_card_ratio` compares without summing, `ALE_005`), cold and warm **never averaged** (`ThermalStateMix`, `ALE_006`). **The headline** `succession_loss` = (total A − total B) at equal N reduces to **(N−1)×(E_spinup+E_teardown)**, exact and refusing mismatched pairs (`ALE_007`); the RUN B amortization curve falls toward the per-task floor while RUN A stays flat (`ALE_008`). Acceptance (a second party reproducing the SHAPE on other hardware) is UNVERIFIED and the NOT-measured list is carried (`ALE_009`). Render screens clean through `no_severity`; modules refuse `--selftest`. Nine `ALE_*` claims; 42 selftest checks |
| `machine-record-format/` | a work order: HOW any record is stored so the categorization is not baked in at write time (companion to `labor-instrument/`, which says WHAT is recorded) | A fully runnable build — a storage format, no egress or hardware dependency — so all six acceptance criteria are checks. **Seven rules enforced in code:** base entries are transformations, not categories (`write_base_entry` refuses a category keyword, `MRF_001`); categorizations are parallel **views**, none canonical, a view added later maps existing entries with no base rewrite (`MRF_002`); **aggregation is a read op** — recompute from base + spec, cache keyed to base version, marked derived (`MRF_003`); **vintages retained** — a revision is a new release_date, `as_of` returns the vintage live then and `None` before publication, on the **imported** `labor-instrument` `VintageStore` not a copy (`MRF_004`); **declared boundary always** — mismatched boundaries refuse to sum without a declared `Reconciliation`, closing the labor-instrument boundary item (`MRF_005`); **no exposure-class conversion** (`convert_exposure` raises, joules the common denominator, `MRF_006`); **absence recorded** in four states, `unmeasured` never collapsing with `measured_zero`, an all-absent group `NOT_COMPUTABLE` not 0.0 (`MRF_007`). The **bisection diagnostic** returns a STRUCTURE verdict before any locus — both halves → `NOT_A_LOCUS`, neither → `MEASURING_SOMETHING_ELSE`, migration → `NONDETERMINISTIC`, `address` refused from any non-single-locus run, null-tested all four ways; for instrument drift the span is the methodology registry, not calendar time (`MRF_008`). Three OPEN items carried (`MRF_009`). Demo screens clean through `no_severity`; modules refuse `--selftest`. **v2 (`WORK_ORDER_V2.md`, beside v1)** adds Rule 8 — no payment field, `write_base_entry` refuses a payment keyword, payment enters only as a view with a declared boundary exclusion (`MRF_010`, acceptance #7); a required test-case format (`tests`/`does_not_test`/`why_not`, `validate_case` refusing a case missing any — Rule 5 applied to test cases, `MRF_011`); and the finding that **Case B (terra preta) forces Rule 7 per column** — measured output with absent exposure in one entry, so `column_status` + a per-column fold, a rate over an absent column `NOT_COMPUTABLE`, an absent column with a number refused (`MRF_012`). Three Rule 8 cases A/B/C, each testing a different thing, run against the base layer (`MRF_013`); the reference marker named, not delivered, real-world figures carried not verified (`MRF_014`). Fourteen `MRF_*` claims; 76 selftest checks |
| `operator-machine-coupling/` | a delivered research **gap** (`MARKER.md`, verbatim): the coupling between a specific operator and a specific unit — the pairing — is acknowledged everywhere and measured nowhere | Posted as a gap, not a finding; nothing is a result, no dataset is read (egress-blocked), no literature claim is verified. The folder adds the **instruments** the measurables need, null-tested on constructed data. **`OMC_001`:** `coupling_separation.py` splits an outcome into `mu + a_i + b_j + r_ij` — the interaction `r_ij` is the coupling, which a main-effects model reports as noise; the same operation as plant breeding's GCA/SCA and the chimp hammer-anvil (a)/(b) separation. `interaction_fraction` registered in `tools/known_answer.py`; incomplete design `NOT_ESTIMABLE`, no variation `None` not 0; `best_pairing` finds the coupled pair whose partners are both average. **`OMC_002`/`OMC_003`:** two discriminators — coupling-failure (tracks time-on-unit) vs operator-error (tracks time-in-role), `UNDETERMINED` when the hour-counts are collinear (the FAA confound); fixed advantage (genotype, the control) vs convergence curve (coupling). **`OMC_004`:** the permission three-state variable — `attribution` shows a naive assignment effect that is actually permission (controlled→0), `UNDETERMINED` when the field is absent/collinear, `m2_match_rate` gated to coupled+prohibited returns `NOT_RECORDED` not 0.0. **`OMC_005`:** M0's cost-boundary is a declared-boundary failure (cross-links `machine-record-format` Rule 5 / `declared-frame` VOID RATIO). **`OMC_007` UNVERIFIED:** the six-literature join is the gap, not fabricated; cross-species equivalence held OPEN not claimed. Demo screens clean under one declared exemption (`error`, the marker's category). Seven `OMC_*` claims; 36 selftest checks |
| `falsifier-audit/` | a work order built to spec: walk a checkout, pull every falsifier and its attached claim, turn each place where a falsifier's **scope is implicit** into a research question — inventory-first, questions not fixes, no rank or score | **Not a linter.** `inventory.py` runs first and reports the marker forms actually present — `REFUTATION_PROTOCOL` sections dominate (~110 files), then prose `Falsifier:`/`Falsified if:`, then claim-table columns under four header names in no fixed position, then fields and JSON keys and block labels — and `extract.py` builds records around the two forms carrying a locatable attached claim (the table column, the prose `Falsified if:`), counting the rest as a printed coverage statement. Four checks, each independent, none aggregated: **A1** fires when a falsifier states no number, comparison, unit, or observable-outcome word (the claim it guards is unguarded); **A2** when fewer than a third of its terms appear in the claim it tests (drift), matched/unmatched terms shown; **A4** on an undeclared reference body (baseline, null, matched — the geocentric shape, rescope not narrow); **A3**, the order's highest-yield check, indexes by **axis** not repo and looks for two folders quantifying one axis incompatibly. **A3 returns zero on this corpus** — the numeric-bearing falsifiers on any shared axis are folder-local, the same unquantified property A1 flags from the other side — reported as a result and demonstrably not silent, since the null test fires on a constructed cross-repo pair. On the tree: **301 falsifiers found (289 LOCATED, 12 NOT-FOUND, 8 empty cells skipped), 283 questions by A1/A2/A4**. `NOT-FOUND` (a falsifier with no locatable claim) and an empty cell are kept apart; the tool's own queue and authored docs are self-excluded (`UNI_010`); the queue's authored framing screens clean while its verbatim quotes of corpus falsifier text do not, correctly, being data. Every entry is a QUESTION with status OPEN; nothing ranked. Findings kept as prose in `AUDIT_NOTES.md` (`FA_001..008`), not a claim table, so the scanner does not read its own findings as corpus |
| `fold-matrix/` | work order 8: one term, one grid, not one number — rows are levels indexed from the term outward, columns are severed / still_acting / clock / basis, and upward levels add the sign and magnitude of the claimed relation between proxy and goal | **The arm this extends is not in the repository**: `severed`, `still_acting` and *deepest still-acting term* return zero hits across the tree, making this the seventh and largest instance of the stated-thing-with-no-artifact shape — and nothing is reconstructed, so levels the repo has not measured carry `ABSENT` rather than a plausible reading. All four fixtures behave as specified, and **one exposed a defect in the instrument**: the first clock check counted distinct values, so a level assuming 3.0 years and a level deriving 3.403 from it read as a horizon conflict when it is **one horizon and its own derivative**. The false positive runs *toward* the finding, since a horizon disagreement IS the finding, so an over-firing check manufactures them; repaired with `derived_from`, derived clocks still emitted. **An empty cell turned out to have two causes**: the UNFCCC disclaimer states its goal and disclaims the relation **in the same cell**, which is neither ABSENT (a goal is stated) nor ordinary ASSERTED — the fourteenth absent-vs-known-negative repair here and the first where the missing state is *the source declined*. `NOT_EVALUABLE` is unrankable in code rather than by convention: `score()` raises on it and on an evaluable term too, because there is no score. `boundary` and `horizon` are read out of `declared-frame` at import rather than retyped, and S4's neutral reading is a declared field, because no string operation turns *efficiency* into a measured quantity. Nine `FM_*` claims **A revision then superseded the order** — S1a new, and `value_string` becoming three independently ABSENT-able fields where it was one free-text string — and **the fixed format recovered information the free-text field had destroyed**: under v1 every upward cell read `empty`, and under the triple the workbook's own disclaimer (*"in order to raise awareness and to promote climate action"*) states a **direction** and no size. *Sign yes, magnitude no, unit no* is the ordinary shape of a purpose claim, and one field recorded it as identical to a cell nobody wrote anything in. The v1 form is refused rather than coerced, since an empty string cannot say which of three fields is missing. S1a's rule — *joules are not the floor unless joules were calculated* — lands on a grid naming four downward levels where the workbook computes at exactly one, giving an `unmeasured_span` of **3 levels** below the deepest quantity anyone calculated; `computed` is a declared field and a `quantified` block without it is refused at load, because filling it in from the physics is exactly the reader the rule is written against. The span understates by construction and says so. And the converse of *empty never emits as zero* needed saying too: a stated magnitude of **0** is a claim, and it is pinned apart from ABSENT. Seventeen `FM_*` claims **An amendment then refused a comparison the folder had published**: `enumeration_basis` is declared per term and never inferred — a grid loaded without it *declares* UNREAD rather than getting one assigned from how well traced its levels look — and the two real fixtures declare different bases, so the 3-vs-0 span contrast is refused outright, both floors emitted and the difference computed on no path. **The rename is the larger half**: `unmeasured_span: 3` reads as a measurement of the world, `unmeasured_span_min: 3` reads as a floor, and before the amendment the honest reading lived in a note string where nothing downstream could see it — the same shape as a workbook stating a relationship in prose that no cell maintains, which is the object scan 4 exists to find. Eighteen `FM_*` claims **Then the SBA run**: two filled business plans of three files, neither under the name given, no blank template — and **n = 2 documents is n = 1 template**, since they share 26 headings and an identical creation date, so replication across them is a finding about the form. **P2 refuted on both**: eighteen dollar figures, **zero computed quantities**, because both stop before Funding Request and Financial Projections. The figures divide by *whose quantity they are* — the company's stated tariff, a target customer's income, an industry statistic — none computed, which widens S1a's two-state gap rather than patching it. Across four terms and three sources: **11 upward cells, 6 stated directions, 0 magnitudes, 0 units.** Twenty-five `FM_*` claims **A blank template then arrived and it is not the SBA one** — 1 of its 9 section names appears in the filled plans — so the registered predictions stay unaddressed and it scores as a new candidate, delivering three results: **a fourth kind of number** (all ten dollar figures sit inside an `(e.g., …)`, belonging to nobody, while the template **names nine computed quantities without computing one**); **P1's third clause refuted in the interesting direction** (a blank form is the *most* enumerable kind of document there is, its structure being all it has, so the least informative document has the best level enumeration and those are the same fact); and **a blank template has no term**, so *one term, one grid* splits into two readings that disagree on whether P4 holds — both recorded, neither picked. Across four sources: **14 upward cells, 8 stated directions, 0 magnitudes, 0 units.** Thirty-one `FM_*` claims **A third-party company brief** then arrived as a PDF plus a separate paste and **the cross-check failed** — stdlib extraction recovers 6 of 19 strings and **0 of 4 figures**, since PDF splits runs for kerning inside `TJ` arrays, so a first pass produced numbers that are artifacts and were never reported as content. It is **the first document whose purpose is stated only by where it is filed**: every prior source states one, this states none, so the upward arm is ABSENT at every level — the brief is not purposeless, but its use belongs to the containing site and a copy of the text carries none of it. Its downward arm is four third-party figures, none computed, none sourced, with no relation stated between any two — while the arithmetic it declines to do is available from its own numbers (net margin 3.03%). **Sixteen upward cells, five sources, zero magnitudes, zero units.** **`fold_register.py` then landed verbatim** — a folded-term register plus a document scanner, seventeen terms with ten grid cells each, all UNFILLED by construction. Its refusal is real and designed in (`score` None, *"Absence is the reading"*, an unknown term returns None rather than an empty grid). Four findings from the audit: **`counter_case` separates perfectly by provenance** — 0 of 12 candidates carry one, 4 of 5 author-named terms do — so the evidence column reads as who named the term; **73% of hits come through the alias layer**, a word list deciding word sense, with `cost` 66 (including the passage arguing against the folded use), `protocol` 49, `budget` 42 and `standard` 19 all in other senses; **all fifteen hits on the two real outside documents were hand-checked** and one is a counter-instance by the register's own definition — a template that enumerates its sales steps on the same line `procedure` is said to substitute for doing — with no schema state for a checked non-fold; and the register is **`category-weld/`'s missing cross-field corpus in a different schema**, though the two do not merge as they stand and `a few` is in neither. Forty-four `FM_*` claims |
| `notes/` | storage for operator entries | mundanely named; entries stored as delivered, checkers never edit them. `operators/D2.md` stated-vs-actual divergence reading, checked against this tree: 5 of 7 instances resolve, its two representations turn out to be five different kinds of pair, its stated signature holds on one instance and fails on four, and its standing check names one of its own two sources. Also `study_watch.py`, a GitHub Actions retrieval notification for entries carrying a WOULD MEASURE — it exists because the runner reaches the three sources the local egress gate refuses, it opens a PR and merges nothing, and NOTIFICATION ONLY is a write-time guard rather than a note; a governing finding stated here was wrong and is corrected — seven of the eight WOULD MEASURE strings carry a verb, and the UNDECIDABLE verdicts were extraction failures; the narrower true statement is that a WOULD MEASURE written as an instruction is already verb-first, which 3 of 22 watchable entries are, and the rest are refused rather than fronted because the residue would not be a sentence. Second entry kind: `datasets/mesa_sof.md`, MESA and SOF as candidate instruments for the question `sim-span/` left open. It answers both halves of what the sim asked and claims one; the sim's two swept parameters turn out to be **WASO**, a PSG readout, with the measured mean matching the product to 0.02 h while the U's *location* moves 1.79 h across equal-product pairs — so both axes are needed and the cohort has both. `parity` resolves 14 times in this tree and **zero** times in the note's sense. Two defects in the checker, both previously recorded here and both committed anyway: substring bleed (`UNI_009`) and the scan measuring its own previous run (`UNI_010`). The loop then closed from the other side: `G-SPAN`, `MESA` and `SOF` resolved zero times when that finding was written and resolve now, every hit under `sim-span/` — work written *after* the note, using its vocabulary — so the assertion was changed to "must resolve only under `sim-span/`" rather than the exclude list widened, which keeps a reachable negative. Third entry kind: `datasets/uploads_2026_08_25.md`, an index over four dataset files delivered "to file for future". The **bytes are not in the tree** (16.6 MB, 16.1 MB binary, third-party terms) and the decision is recorded with its cost — the container is ephemeral, so the index is what survives, which is why each entry carries a sha256 and `check_uploads.py` reports `PRESENT_MATCH` / `PRESENT_DIFFERS` / `NOT_PRESENT` with **`NOT_PRESENT` explicitly not a pass**. Findings: 26 workbooks in one upload carry **zero** formulas while a naive scan returns three, all Airbnb listing titles beginning with `=` and written `t="e"` — `sheetmodel.py` reads no cell type, so such a cell counts DERIVED (`SSS_061`, declared not repaired, effect on published numbers UNKNOWN not zero); the UCI archive carries a **repeat-measure pair**, which is the null `triad-playground` TP_010 says this tree has never had; and the World Bank catalogue is both a target list for the workbook `WO7` could not reach and a clock corpus needing no egress. On its first run the checker caught an error in its own record. |

See [`SYNTHESIS.md`](SYNTHESIS.md) for the cross-folder reading and
[`CLAUDE.md`](CLAUDE.md) for the layout reference.

## Running

Each simulator runs with `python3 <module>.py` from inside its folder. No
dependencies. Tests in each `tests/` directory run with
`python3 -m unittest discover tests`.

## License

CC0 / public domain. No rights reserved. No attribution required.
