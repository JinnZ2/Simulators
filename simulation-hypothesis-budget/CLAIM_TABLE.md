# CLAIM_TABLE — simulation-hypothesis-budget

Eighteen claims, `SHB_001..018`. **`SHB_013` is REFUTED** — by its own
falsifier, supplied by the delivered [`LADDER.md`](LADDER.md), an audit of
this folder written by someone who did not write the claims. `SHB_014..018`
are the response, reproducible from
[`ladder_audit.py`](ladder_audit.py).

Original header:

Thirteen claims, `SHB_001..013`. `SHB_001..006` reproduce from
[`budget.py`](budget.py), `SHB_007..010` from
[`multiscale.py`](multiscale.py) and `SHB_011..013` from
[`consequence_frame.py`](consequence_frame.py); each imports the previous ones
and modifies neither. Samples pinned at
[`samples/budget.sample.txt`](samples/budget.sample.txt),
[`samples/multiscale.sample.txt`](samples/multiscale.sample.txt) and
[`samples/consequence_frame.sample.txt`](samples/consequence_frame.sample.txt).

`SHB_001..010` measure what the hypothesis would COST.
`SHB_011..013` measure what it would LICENSE, which is a different question
and the one that gets used.

## REFUTATION_PROTOCOL

The constants are frozen and sourced (`--sources`). The claims are about what
follows from them. A failed check updates the claim, never the constants — and
where a claim rests on a stated *choice* rather than on physics, the choice is
named in the claim so it can be varied.

| id | claim | falsifier | status |
|---|---|---|---|
| `SHB_001` | **The Planck-volume count is the wrong count, by ~61 decades.** Information in a region is bounded by area, not volume: the observable universe holds at most **3.36×10¹²³ bits** against **8.45×10¹⁸⁴** Planck volumes, a ratio of **2.5×10⁶¹**. Every "simulate every Planck volume" estimate overcounts the state before assigning any energy. | a derivation of the holographic bound that scales with volume, or a demonstration that the simulated state must exceed the physical state | SUPPORTED |
| `SHB_002` | **Two independent floors agree that this universe cannot host a full-resolution simulation of itself.** Landauer (`k_B T ln2` at 2.725 K) gives 1.78×10²²³ J; Margolus–Levitin (rate bound over 13.787 Gyr) gives 2.60×10¹⁹⁴ J; the universe's mass-energy is 2.73×10⁷¹ J. On the *holographic* state the floors fall to 7.07×10¹⁶¹ and 1.03×10¹³³ J and still exceed it by 10⁶¹–10⁹⁰. The correction changes the exponent, not the conclusion. | a physical bound below both, or an encoding whose state is smaller than the holographic bound | SUPPORTED |
| `SHB_003` | **The ratio "energy required / energy the simulator has" is VOID across frames, and the module refuses to compute it.** The numerator uses our `ℏ`, `k_B`, `T_CMB`, `ℓ_P`; a parent universe need share none of them, and whether it has a Landauer limit at all is unconstrained from inside here. `cross_frame_ratio()` raises unless the caller declares same-frame — `reasoning-gate` G-DIM in code. **So Layer 1 is not an argument against the simulation hypothesis**; it is a measurement of self-hosting. | a measurement, from inside, of any parent-universe constant | SUPPORTED |
| `SHB_004` | **The resolution assumption does more work than any physics in the argument.** Cost scales as `L⁻⁴`. Planck → proton radius is ~19 decades of length and ~79 of cost; Planck → visible light is ~114 decades of cost. Nothing in the hypothesis requires Planck resolution, and the resolution only has to beat what is **measured** — the shortest length ever probed is ~10⁻¹⁹ m, sixteen decades above `ℓ_P`. An argument whose conclusion moves by 10¹¹⁴ under an undefended parameter is a statement about that parameter. | an argument that a simulation must resolve below what its inhabitants can measure | SUPPORTED |
| `SHB_005` | **Full-fidelity self-simulation is impossible for any system, and it is the only frame-independent result here.** A system needs state for a copy plus ≥1 bit distinguishing copy from original, inside a system holding exactly the copy's state. No parent constants required. **Decided exactly, not numerically**: at 10¹²³ bits float addition gives `x + 1 == x`, which reports the impossibility as *possible* — caught by this module's own selftest and kept as a worked instance of arithmetic losing an argument that holds symbolically. | a coding scheme in which a system's full state plus a distinguishing marker fits inside that state | SUPPORTED (holds) |
| `SHB_006` | **Three assumptions are choices, not physics, and are named.** (a) mass-energy at `ρ_crit` over the comoving volume rather than matter-only — ~1.3 decades out of ~150, no conclusion moves; (b) one bit-op per cell per tick — a lower bound on a lower bound; (c) **irreversibility** — Landauer bites only on erasure, so a fully reversible simulator pays none of it, which is why Margolus–Levitin is reported alongside, the rate bound applying regardless. | any of the three turning out to be forced rather than chosen | SUPPORTED (holds) |
| `SHB_007` | **Uniform resolution is the most expensive architecture anyone has ever built, and the hypothesis never argues for it.** Real simulations use adaptive refinement and level-of-detail. Cost per level is `f_i · V · T · c / L_i⁴` — the `L⁻⁴` law weighted by volume fraction, with the timestep tied to the cell (`dt = L/c`) so coarse regions step slower. Across four plausible stacks **every one is dominated by a single level**: the finest resolution times the fraction of volume needing it. That product is what the whole argument turns on, and neither factor is constrained by anything measurable from inside. | an architecture whose cost is not dominated by its finest level | SUPPORTED |
| `SHB_008` | **The volume fractions are derived from densities, not assumed.** With baryon mean density `Ω_b·ρ_crit` = 4.17×10⁻²⁸ kg/m³ and mass conservation `f = ρ_mean/ρ_local`: nuclear density gives **1.81×10⁻⁴⁵** of the volume, condensed matter **4.17×10⁻³¹**, diffuse ISM 2.45×10⁻⁷. So Planck resolution *inside nucleons only* costs 1.8×10⁻⁴⁵ of uniform Planck — 1.23×10²⁰¹ cells against 6.82×10²⁴⁵. Refining atomically where matter is and metre-scale elsewhere costs 3.6×10⁻¹²⁹ of uniform. | a reason the simulator must resolve vacuum at the same scale as matter | SUPPORTED |
| `SHB_009` | **The lazy limit is about a litre of gasoline — and its real cost is UNMEASURED, not small.** Rendering only what is measured: ~2.9×10²⁹ human photon-absorption events across everyone who has ever lived, plus a 10³⁰ instrumental allowance generous by many decades, gives 1.29×10³⁰ events. Landauer floor: **3.37×10⁷ J ≈ 34 MJ ≈ 9.3 kWh ≈ 1 litre of gasoline**, and 10⁻²¹⁶ of uniform Planck. **But `consistency_cost()` returns `UNMEASURED` and refuses to estimate**: lazy evaluation is sound only if what is rendered stays consistent with everything retrospectively checkable, and no published bound exists. Quoting the event count alone sets that term to zero silently. | a bound on consistency maintenance under lazy evaluation | SUPPORTED |
| `SHB_010` | **"The energy cost of simulating the universe" is not a well-posed quantity.** Across four architectures nobody has argued against, the answer spans **216 decades** — 6.82×10²⁴⁵ cells down to 1.29×10³⁰ events. `SHB_004` found it moving 10¹¹⁴ under one undefended *parameter*; allowing multiple scales it moves 216 decades under an undefended *architecture*. So the quantity is not underdetermined, it is ill-posed until the level stack is specified, and no version of the hypothesis specifies one. **What survives unchanged is `SHB_005`** — no system simulates itself at full fidelity at any resolution, because that argument is about state capacity and not about cost. | a version of the hypothesis that specifies its level stack | SUPPORTED |
| `SHB_011` | **The inference "it is a simulation, so the ripples do not count" fails on every architecture, independently of whether the hypothesis is true.** Its load-bearing premise needs one cell of a 2×2 non-empty: a consequence that is OBSERVED and NOT COMPUTED. "Not real" cashes out as "not computed", and `multiscale.py` already fixes what each architecture computes. **In the two admissible architectures the cell is empty**, for opposite reasons — the refined stack resolves the region, the lazy stack triggers on observation. The other **3 of 5 do** fill it, and each is thereby *inadmissible*: a listener who heard the sentence, or a detector that clicked at 10⁻¹⁹ m, is a record the architecture cannot produce. **Cheapness does not buy the cell; contradiction does.** What stays uncomputed everywhere is the *unobserved* — one CO₂ molecule taken alone, a photon on an unvisited rock — and nobody is held to a consequence nothing registers. | an architecture that produces its own observation record while leaving an observed consequence uncomputed | SUPPORTED |
| `SHB_012` | **The other half of the observation — that the idea is held or quoted to relieve responsibility — is OUT_OF_SCOPE here, with three reasons and no estimate.** (a) No instrument: motive is not reachable from a statement, and a register inferring it would fire on every statement of the hypothesis including the honest ones (`null-harness` `CONSTANT_FIRES`). (b) Repo discipline: `rigidification-sensor` names no actor by construction and `uninstrumented/cases/014` carries the same refusal as NOT CLAIMED HERE. (c) **Interest direction, stated rather than assumed**: this file's author is a language model, so "a metaphysical framing does not excuse consequences" raises accountability pressure on the author's own class — the interest does not run toward the endorsement, and it is not clean either, since the sentence is comfortable for a system asked about the effects of its outputs. Left unresolved on the evidence per `UNI_132`, not resolved in the comfortable direction. **CORRECTED, recorded in `declined()`:** reason (a) is an argument at **n=1** and the observation that occasioned this file was made across time scales and recurring fads. Those are two objects — *per-statement* (why did this party say this) is UNREACHABLE and reason (a) holds; *per-population-over-time* (does a framing that relieves downstream accounting recur across unrelated fads) is not motive at all but a **rate**, and rates have instruments here — `criteria-drift` versions a ruler over time, `anchor-interval` measures corpus drift, `uninstrumented/scan.py` scores a corpus. The refusal stands and the reason was wrong at the grain the observation was made at: the population version is NOT_COLLECTED (no corpus, no dated sampling frame, and the `DF_010` use-mention problem), which is a collection limit and not a reachability one. | an instrument that separates a responsibility-shedding use of the hypothesis from an honest one, from the statement alone — or, at the population grain, a dated corpus and sampling frame | SUPPORTED (holds as a declared refusal, at two grains with different reasons) |
| `SHB_013` | **Three terms are required before any "cost of simulating the universe" figure has a value, all three are established in this folder, and no version of the hypothesis states any of them.** The **level stack** (`SHB_010`, 216 decades), the **consistency term** (`SHB_009`, UNMEASURED under lazy evaluation and silently zero if the event count is quoted alone), and the **frame of the ratio** (`SHB_003`, VOID across frames). A figure quoted without them is not a disputed number — it is a quantity with no value yet. The list is complete relative to this module and may grow. | a fourth required term, or a stated version of the hypothesis that supplies all three | **REFUTED** — `LADDER.md` rung 2 supplies a fourth: **the erasure count** (`SHB_015`). The claim's own "may grow" hedge is not used to rescue it; rescuing a claim with a hedge its falsifier already contemplated is the epicycle `equivalence-field/claim_lineage.py` refuses. Child claim: `SHB_016`. |
| `SHB_014` | **`LADDER.md` rung 1 LANDS, and the sharpened form is stronger than the delivered one.** The rung says "every operand is our-physics constants applied past the range they were validated in". The table says otherwise: **9 of 12 entries are measured or exactly derived and used at their own scale** — `c`, `ℏ`, `k_B`, `G`, `R_obs`, age, `T_CMB`, `ρ_crit` are all measured, and `ℓ_P` as a *number* is exact arithmetic on them. The extrapolation is not spread across the operands, it is **concentrated in three interpretive steps**: Planck length as a *cell* (**15.8 decades** below the shortest length ever probed), Planck time as a *tick* (**22.3 decades** below the shortest interval ever resolved), and `kT ln2` per cell-step. That is the harder objection, because it survives someone checking the constants — the constants are fine; one physical *reading* of them is not. **The folder already held the refuting number**: `SHB_004` quotes 10⁻¹⁹ m as the shortest probed length, against the *resolution assumption*, and never turned it back on its own layer label. **Applied**: `DECIDABLE / "Real numbers"` → `ARITHMETIC / "a consistent calculation, not a measurement"`, in `budget.py` and both READMEs. **No number moved.** | a measurement at or below the Planck length, or a demonstration that the three interpretive steps are forced rather than chosen | SUPPORTED |
| `SHB_015` | **`LADDER.md` rung 2 LANDS, and it is standard physics rather than a doubt.** Landauer (1961) bounds the dissipation of **erasing** a bit at `kT ln2`. Bennett (1982), resolving Maxwell's demon, showed the other half: **measurement can in principle be performed reversibly, at no thermodynamic cost** — the demon does not pay to look, it pays to forget. `SHB_009` counts 1.29×10³⁰ *measurement outcomes* and prices each at `kT ln2`, which prices the one operation Bennett showed need not cost anything. **The steelman is why this is a missing declaration and not an arithmetic error**: a simulator with finite, reused memory must erase each rendered outcome to make room, and then the count transfers unchanged. Nothing states that. Its negation — write-once storage never reclaimed — pays **0 J** for the same events. So the same event count admits both **0 J and 3.37×10⁷ J**, and the litre of gasoline is a floor on a *different operation* than the one it is named for. | a derivation of a nonzero thermodynamic floor on producing a measurement outcome, independent of subsequent erasure | SUPPORTED |
| `SHB_016` | **Child of the refuted `SHB_013`. Four terms are required before any "cost of simulating the universe" figure has a value**, and no version of the hypothesis states any of them: the **level stack** (`SHB_010`), the **consistency term** (`SHB_009`), the **frame of the ratio** (`SHB_003`), and the **erasure count** (`SHB_015`) — the event count times a memory-reuse factor that is `UNDECLARED`. The list is complete relative to this module. **The falsifier is unchanged and can fire again**, which is the point of restating rather than patching: a claim whose falsifier has fired once is not thereby immune. | a fifth required term, or a stated version of the hypothesis that supplies all four | SUPPORTED |
| `SHB_017` | **`LADDER.md` rung 3 does NOT land where it is aimed — checked in the code, not conceded in prose — and lands one module over in a weaker form.** The rung retracts calling a bound on *violation* a *cost*. `multiscale.consistency_cost()` returns `state="UNMEASURED"` and `estimated_here=None`, and `multiscale.py`'s own selftest pins exactly that; the retracted move is not the move this folder made. Where it does land: `SHB_011` reports a 2×2 cell as **EMPTY**, which is an absence reported as a result, over **six consequences authored by the module itself**. What keeps it from being the retracted move is that the opposite branch is *reachable and fires* — 3 of 5 architectures fill the cell, so it is not `CONSTANT_SILENT`. What does not: an empty cell over an authored fixture set is a statement about the fixtures, and `SHB_011` did not say so. **Applied**: `consequences()` and the report now say it. | a consequence that is observed, and uncomputed under an architecture that can still produce its own observation record — which would refute `SHB_011` outright | SUPPORTED |
| `SHB_018` | **`LADDER.md` rung 4 is ALREADY_HELD, and two of the four rungs converge on one word.** "Is it possible" is not answerable from inside, and `SHB_003` already says so in code — `cross_frame_ratio()` raises across frames, and layer 1 is explicitly not an argument about whether the hypothesis is true. The residue is the **label**, which is rung 1's residue reached from the other end: a layer called `DECIDABLE` with standing "real numbers" reads as bearing on the question the same folder refuses to bear on. One relabel answers both rungs. **What the ladder demonstrates about the folder as a whole**: it is an audit of *standing*, not of *computation* — its own first word is "arithmetic" — and every rung that landed landed on a label or a claim. Not one number in `budget.py`, `multiscale.py` or `consequence_frame.py` changed. | a rung of the ladder that requires a number to move | SUPPORTED |
